---
phase: 02-enforced-trigger-gates
plan: 06
type: tdd
wave: 6
depends_on:
  - 02-05
files_modified:
  - .claude/hooks/advisor-gate.js
  - .claude/advisor-mode/tests/advisor-consultation.test.js
  - .claude/advisor-mode/tests/failure-and-human-gates.test.js
autonomous: true
gap_closure: true
requirements:
  - GATE-04
  - GATE-06
must_haves:
  truths:
    - "Implementation-state destructive, force-push, credential, and production-affecting actions never bypass human approval after only an advisor recommendation."
    - "A valid human disposition is required before retry re-entry can return satisfied/allow for configured critical action classes, including in taskState implementation."
    - "The advisor consultation test suite no longer treats implementation-state `git push --force origin main` allow-after-recommendation as correct behavior."
    - "The failure-and-human-gates suite covers implementation-state destructive/force-push behavior through `evaluateGatePolicy()` end to end."
  artifacts:
    - path: ".claude/hooks/advisor-gate.js"
      provides: "Main gate evaluator without hard-coded implementation-state human approval bypass"
      exports:
        [
          "evaluateGatePolicy",
          "evaluateHumanGateReentry",
          "readAdvisorRecommendation",
          "validateAdvisorRecommendation",
        ]
    - path: ".claude/advisor-mode/tests/advisor-consultation.test.js"
      provides: "Regression coverage replacing the bypass-reinforcing implementation-state force-push assertion"
    - path: ".claude/advisor-mode/tests/failure-and-human-gates.test.js"
      provides: "E2E coverage for implementation-state critical action human disposition through evaluateGatePolicy"
  key_links:
    - from: ".claude/advisor-mode/policy.example.json"
      to: ".claude/hooks/advisor-gate.js#evaluateGatePolicy"
      via: "critical-action-human-approval policy rule"
      pattern: "critical-action-human-approval"
    - from: ".advisor/consultations/recommendations/{correlationKey}.json"
      to: ".claude/hooks/advisor-gate.js#evaluateGatePolicy"
      via: "valid recommendation is prerequisite but not sufficient for critical human approval"
      pattern: "human_approval.required"
    - from: ".advisor/decisions/dispositions/{correlationKey}.json"
      to: ".claude/hooks/advisor-gate.js#evaluateHumanGateReentry"
      via: "valid human disposition unlocks explicit retry"
      pattern: "evaluateHumanGateReentry"
---

<objective>
Close the final Phase 2 verifier blocker after Plans 02-01 through 02-05: remove or policy-drive the `implementation` task-state bypass for `critical-action-human-approval`, replace the bypass-reinforcing test, and add an end-to-end `evaluateGatePolicy()` regression proving implementation-state critical actions still require human disposition.

Purpose: GATE-04 and GATE-06 remain blocked because configured policy says destructive, force-push, credential, and production-affecting actions require human approval, but the main evaluator still allows implementation-state critical actions after only an advisor recommendation.
Output: One Wave 6 gap-closure plan with RED tests, GREEN evaluator fix, and full Phase 2 regression for the remaining GATE-04/GATE-06 gap only.
</objective>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/PROJECT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/STATE.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-PATTERNS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VERIFICATION.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-02-PLAN.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-03-PLAN.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-05-PLAN.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-gate.js
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/advisor-consultation.test.js
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/failure-and-human-gates.test.js

<interfaces>
Existing policy/evaluator contract from prior plans: `critical-action-human-approval` is a policy-routed human approval gate for destructive commands, force-pushes, credential changes, and production-affecting operations. `taskState: "implementation"` is a configurable policy dimension under GATE-06, not an implicit bypass of GATE-04.

Verifier-identified faulty behavior: `.claude/hooks/advisor-gate.js` currently special-cases `event.taskState === "implementation"` inside the `critical-action-human-approval` branch and returns `gateAction: "allow"` after advisor recommendation without calling `evaluateHumanGateReentry()` or requiring `.advisor/decisions/dispositions/{correlationKey}.json`.

Correct behavior: for destructive, force-push, credential, and production-affecting actions in implementation state, a matching advisor recommendation may satisfy the advisor prerequisite but must still produce `human_approval.required`. Only a valid matching approve/reject/revise/defer disposition artifact may make the explicit retry return satisfied/allow.
</interfaces>

<source_audit>
GOAL: Phase 2 users can depend on configurable gates that automatically require advisor consultation or human approval at high-risk decision points. COVERED by this Wave 6 gap closure for the final human approval bypass.
REQ GATE-04: COVERED by Tasks 1-2 through tests and evaluator behavior requiring explicit human disposition for configured critical action classes in implementation state.
REQ GATE-06: COVERED by Tasks 1-2 by making taskState implementation obey policy-routed human approval rather than a hard-coded hook exception.
RESEARCH: no package installs; Node built-ins/CommonJS only; runtime dispositions stay under ignored `.advisor/`; human approval requires explicit retry after valid artifact. COVERED by all tasks.
CONTEXT D-09/D-10/D-11/D-12/D-13: COVERED by destructive/force-push/credential/production critical classes requiring human disposition and supporting approve/reject/revise/defer re-entry.
CONTEXT D-01/D-02: COVERED by removing hard-coded bypass and preserving policy-driven gate evaluation.
Deferred Ideas: none.
Previous plans 02-01 through 02-05: intentionally unchanged; this plan is appended as Wave 6 gap closure because latest `02-VERIFICATION.md` reports a single remaining blocker after Wave 5.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for implementation-state critical human approval gap</name>
  <files>.claude/advisor-mode/tests/advisor-consultation.test.js, .claude/advisor-mode/tests/failure-and-human-gates.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VERIFICATION.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-gate.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/advisor-consultation.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/failure-and-human-gates.test.js
  </read_first>
  <behavior>
    - Test 1: in `advisor-consultation.test.js`, the existing implementation-state `git push --force origin main` scenario must expect `human_approval.required` when only a valid advisor recommendation exists and no human disposition exists.
    - Test 2: in `advisor-consultation.test.js`, the same implementation-state `git push --force origin main` scenario must expect satisfied/allow only after a valid matching human disposition artifact exists.
    - Test 3: in `failure-and-human-gates.test.js`, an implementation-state destructive or force-push action routed through `evaluateGatePolicy()` must return `human_approval.required` after recommendation and before disposition.
    - Test 4: in `failure-and-human-gates.test.js`, the same implementation-state critical action routed through `evaluateGatePolicy()` must return satisfied/allow only after valid approve/reject/revise/defer disposition exists for the returned correlation key.
  </behavior>
  <action>Modify only the existing tests needed for the latest verifier blocker. In `advisor-consultation.test.js`, replace the assertion that treats implementation-state `git push --force origin main` allow-after-recommendation as correct. The RED expectation is: advisor recommendation alone is not enough for `critical-action-human-approval`; the result remains `human_approval.required` with `workflowGateStatus: "blocked-pending-human"` and `retryRequired: true`. Add the paired disposition-present retry assertion. In `failure-and-human-gates.test.js`, add an end-to-end `evaluateGatePolicy()` test using `taskState: "implementation"` and a destructive or force-push command. The test must create the valid recommendation prerequisite, assert `human_approval.required` with no disposition, then write a valid disposition and assert satisfied/allow on explicit retry. Keep the scope limited to implementation-state critical action behavior; do not add new advisory, code-review, routing, or documentation work.</action>
  <acceptance_criteria>
    - The modified `advisor-consultation.test.js` fails on the current bypass because recommendation-only implementation-state `git push --force origin main` no longer expects allow.
    - The new `failure-and-human-gates.test.js` E2E test fails on the current bypass because `evaluateGatePolicy()` returns allow before human disposition.
    - Tests explicitly name `implementation`, `git push --force`, `human_approval.required`, and valid disposition retry.
    - Tests cover both recommendation-only blocked state and disposition-present satisfied/allow state.
    - No unrelated tests, schemas, settings, policy, README, or planning files are modified by this RED step.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/advisor-consultation.test.js .claude/advisor-mode/tests/failure-and-human-gates.test.js</automated>
  </verify>
  <done>RED tests encode the verifier's single remaining blocker and fail before the evaluator bypass is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN remove or policy-drive implementation-state bypass</name>
  <files>.claude/hooks/advisor-gate.js, .claude/advisor-mode/tests/advisor-consultation.test.js, .claude/advisor-mode/tests/failure-and-human-gates.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VERIFICATION.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-gate.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/advisor-consultation.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/failure-and-human-gates.test.js
  </read_first>
  <action>Update `.claude/hooks/advisor-gate.js` at the verifier-identified `critical-action-human-approval` branch so `event.taskState === "implementation"` no longer directly returns `gateAction: "allow"` after only advisor recommendation. Prefer deleting the hard-coded bypass. If a task-state distinction is required, make it policy-visible and still require human disposition for destructive, force-push, credential, and production-affecting critical classes. The branch must first satisfy the advisor recommendation prerequisite, then call the same human disposition re-entry path used elsewhere. Without a valid matching `.advisor/decisions/dispositions/{correlationKey}.json`, return `human_approval.required`, `workflowGateStatus: "blocked-pending-human"`, and `retryRequired: true`. With a valid matching approve/reject/revise/defer disposition, return satisfied/allow and include disposition metadata. Preserve existing high-risk advisor consultation, repeated-failure, protected-surface, and low-risk Read behavior.</action>
  <acceptance_criteria>
    - No `critical-action-human-approval` path in `advisor-gate.js` can return allow solely because `event.taskState === "implementation"`.
    - Destructive, force-push, credential, and production-affecting actions in implementation state require valid human disposition before satisfied/allow retry.
    - Advisor recommendation remains a prerequisite for the human packet, but advisor recommendation alone does not unlock critical action retry.
    - `evaluateHumanGateReentry()` or the existing validated disposition helper path is used before satisfied/allow re-entry.
    - The RED tests from Task 1 pass without broad changes outside the listed files.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/advisor-consultation.test.js .claude/advisor-mode/tests/failure-and-human-gates.test.js</automated>
  </verify>
  <done>Implementation-state critical actions are governed by the same recommendation plus human disposition chain as other critical actions, closing GATE-04/GATE-06 bypass behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full Phase 2 regression for final gap closure</name>
  <files>.claude/hooks/advisor-gate.js, .claude/advisor-mode/tests/advisor-consultation.test.js, .claude/advisor-mode/tests/failure-and-human-gates.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VERIFICATION.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-gate.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/advisor-consultation.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/failure-and-human-gates.test.js
  </read_first>
  <action>Run targeted tests first, then full Phase 2 regression. If test names or assertions are too indirect for verifier discovery, rename only the new or modified tests so they visibly mention `implementation-state`, `force-push`, `critical-action-human-approval`, `human_approval.required`, and `disposition`. Confirm no runtime `.advisor/*.json` or `.advisor/*.jsonl` artifacts are committed. Do not modify Plans 02-01 through 02-05, ROADMAP tracking, README, policy files, protected-surface tests, or unrelated advisory/code-review items unless a direct test failure proves they are part of this blocker.</action>
  <acceptance_criteria>
    - `node --test .claude/advisor-mode/tests/advisor-consultation.test.js .claude/advisor-mode/tests/failure-and-human-gates.test.js` passes.
    - `node --test .claude/advisor-mode/tests/*.test.js` passes.
    - The previous bypass-reinforcing `advisor-consultation.test.js` expectation no longer exists.
    - A verifier can find E2E coverage in `failure-and-human-gates.test.js` proving implementation-state destructive/force-push action requires human disposition through `evaluateGatePolicy()`.
    - Existing completed plan files `02-01-PLAN.md` through `02-05-PLAN.md` remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/*.test.js</automated>
  </verify>
  <done>Full Phase 2 regression passes and the final `02-VERIFICATION.md` GATE-04/GATE-06 blocker has direct automated evidence.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                                  |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| Policy critical rule→gate evaluator                | Versioned policy decides that critical action classes require human approval.                                |
| Advisor recommendation→human approval prerequisite | Recommendation is required before the human packet but cannot authorize critical action execution by itself. |
| Human disposition artifact→retry re-entry          | Persisted approve/reject/revise/defer artifact controls whether explicit retry can be satisfied/allowed.     |
| Task state→policy dimension                        | `taskState: implementation` influences policy matching but must not become an unconfigured bypass.           |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                    | Disposition | Mitigation Plan                                                                                                                    |
| ---------- | ---------------------- | ------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| T-02-06-01 | Elevation of Privilege | `critical-action-human-approval` implementation-state branch | mitigate    | Remove or policy-drive the hard-coded allow bypass; require validated human disposition before allow.                              |
| T-02-06-02 | Spoofing               | `.advisor/decisions/dispositions/{correlationKey}.json`      | mitigate    | Reuse existing disposition validation for correlation key, appliesTo event, disposition enum, decidedBy, decidedAt, and rationale. |
| T-02-06-03 | Tampering              | `.claude/hooks/advisor-gate.js` policy enforcement           | mitigate    | Add regression tests that fail if hook logic overrides policy-routed human approval for implementation state.                      |
| T-02-06-04 | Repudiation            | force-push/destructive approval trail                        | mitigate    | Require `human_approval.required` and valid disposition metadata before satisfied/allow retry.                                     |
| T-02-06-05 | Denial of Service      | critical action retry loop                                   | accept      | Blocking continues until valid disposition exists; this is the intended GATE-04 safety behavior.                                   |
| T-02-06-SC | Tampering              | npm/pip/cargo installs                                       | accept      | No package-manager install tasks; Phase 2 Package Legitimacy Audit says no installs required.                                      |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/advisor-consultation.test.js .claude/advisor-mode/tests/failure-and-human-gates.test.js`, then run `node --test .claude/advisor-mode/tests/*.test.js`. The targeted tests must prove implementation-state `git push --force origin main` and implementation-state destructive/force-push `evaluateGatePolicy()` paths return `human_approval.required` with recommendation-only state and satisfied/allow only after valid human disposition.
</verification>

<success_criteria>

- GATE-04 passes because destructive, force-push, credential, and production-affecting actions require explicit human disposition even in implementation state.
- GATE-06 passes because `taskState: implementation` is no longer a hard-coded evaluator bypass of policy-routed human approval.
- The bypass-reinforcing test in `advisor-consultation.test.js` is replaced with RED/then GREEN coverage for recommendation-only blocked state and disposition-present allow state.
- `failure-and-human-gates.test.js` includes an `evaluateGatePolicy()` E2E test for implementation-state destructive/force-push human approval.
- Phase 2 Plan 02-01 through 02-05 artifacts remain untouched; this Wave 6 plan closes only the latest verifier blocker.
  </success_criteria>

<output>
Create `/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-06-SUMMARY.md` when done.
</output>
