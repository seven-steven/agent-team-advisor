---
phase: 02-enforced-trigger-gates
plan: 05
type: tdd
wave: 5
depends_on:
  - 02-03
  - 02-04
files_modified:
  - .claude/advisor-mode/tests/failure-and-human-gates.test.js
  - .claude/advisor-mode/tests/protected-surface.test.js
  - .claude/hooks/advisor-gate.js
  - .claude/advisor-mode/policy.example.json
autonomous: true
gap_closure: true
requirements:
  - GATE-02
  - GATE-04
  - GATE-06
  - SAFE-04
must_haves:
  truths:
    - "Persisted repeated-failure signatures from `.advisor/state/failure-signatures.json` are consumed by `evaluateGatePolicy()` so the next matching PreToolUse attempt automatically requires advisor consultation at threshold 2."
    - "`trackFailure()` and `evaluateGatePolicy()` use the same `normalizeFailureSignature()` implementation, so the exact key written by the PostToolUse tracker is the exact key read by the PreToolUse gate."
    - "Configured critical action classes, including destructive commands, force-pushes, credential changes, and production-affecting operations, route to `gateAction: human-approval` instead of advisor-consultation only."
    - "A valid persisted human disposition changes the next `evaluateGatePolicy()` retry result from `human_approval.required` to satisfied/allowed re-entry for critical actions."
    - "Protected-surface human approval retries use the same recommendation/disposition chain and unlock after a valid matching disposition artifact exists."
    - "End-to-end tests exercise `evaluateGatePolicy()` for repeated-failure escalation, critical human-approval retry, and protected-surface retry."
  artifacts:
    - path: ".claude/advisor-mode/tests/failure-and-human-gates.test.js"
      provides: "End-to-end TDD coverage for persisted repeated-failure state and critical action human-approval retry through evaluateGatePolicy"
    - path: ".claude/advisor-mode/tests/protected-surface.test.js"
      provides: "End-to-end TDD coverage for protected-surface disposition retry through evaluateGatePolicy"
    - path: ".claude/hooks/advisor-gate.js"
      provides: "Main PreToolUse policy evaluator wired to persisted repeated-failure state and persisted human dispositions"
      exports:
        [
          "evaluateGatePolicy",
          "evaluateHumanGateReentry",
          "readDisposition",
          "validateDisposition",
          "readAdvisorRecommendation",
          "classifyActionClass",
        ]
    - path: ".claude/advisor-mode/policy.example.json"
      provides: "Policy rules routing critical action classes to human approval"
  key_links:
    - from: ".advisor/state/failure-signatures.json"
      to: ".claude/hooks/advisor-gate.js#evaluateGatePolicy"
      via: "exact `normalizeFailureSignature` export reused from advisor-failure-tracker.js"
      pattern: "failure-signatures\\.json"
    - from: ".claude/advisor-mode/policy.example.json"
      to: ".claude/hooks/advisor-gate.js#evaluateGatePolicy"
      via: "critical action class rules with gateAction human-approval"
      pattern: "human-approval"
    - from: ".advisor/decisions/dispositions/{correlationKey}.json"
      to: ".claude/hooks/advisor-gate.js#evaluateGatePolicy"
      via: "evaluateHumanGateReentry before rebuilding human approval packet"
      pattern: "evaluateHumanGateReentry"
---

<objective>
Close the Phase 2 verifier gaps without reworking existing Plans 02-01 through 02-04: connect persisted repeated-failure state back into the main PreToolUse gate, route configured critical action classes to human approval, and make `evaluateGatePolicy()` consume valid disposition artifacts on explicit retry for both critical actions and protected surfaces.

Purpose: Phase 2 currently records the right runtime artifacts but leaves key state transitions disconnected from the main evaluator. This gap-closure plan makes the implemented state artifacts authoritative for the next gated attempt.
Output: End-to-end tests plus targeted policy/evaluator fixes for GATE-02, GATE-04, GATE-06, and SAFE-04.
</objective>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/PROJECT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/STATE.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-PATTERNS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VERIFICATION.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-03-PLAN.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-04-PLAN.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-gate.js
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-failure-tracker.js
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/policy.example.json
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/failure-and-human-gates.test.js
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/protected-surface.test.js

<interfaces>
Existing `advisor-failure-tracker.js` exports `normalizeFailureSignature`, `updateFailureState`, `buildFailureEvent`, `trackFailure`, and `main`. `trackFailure()` writes `.advisor/state/failure-signatures.json`; `evaluateGatePolicy()` must read that persisted state instead of depending on caller-injected `event.failureCount`.
`evaluateGatePolicy()` must import and call the exact `normalizeFailureSignature()` implementation exported by `.claude/hooks/advisor-failure-tracker.js` when deriving the lookup key for `.advisor/state/failure-signatures.json`. Do not duplicate the algorithm in `advisor-gate.js`; do not use an equivalent local implementation. The acceptance contract is that the key written by `trackFailure()` is byte-for-byte the key read by `evaluateGatePolicy()` for the next matching attempt.
Existing `advisor-gate.js` exports `evaluateGatePolicy`, `evaluateHumanGateReentry`, `readDisposition`, `validateDisposition`, `buildDecisionPacket`, `readAdvisorRecommendation`, `validateAdvisorRecommendation`, and classifiers. Human approval re-entry must be checked inside `evaluateGatePolicy()` before constructing a fresh `human_approval.required` packet.
Existing policy rules include `destructive-command` as critical advisor consultation and `protected-surface-human-approval` as human approval. Gap closure must add or revise policy rules so destructive commands, force-pushes, credential changes, and production-affecting operations map to `gateAction: "human-approval"` per D-10/GATE-04.
</interfaces>

<source_audit>
GOAL: Phase 2 users can depend on configurable gates that automatically require advisor consultation or human approval at high-risk decision points. COVERED by Tasks 1-2 through persisted state and re-entry wiring.
REQ GATE-02: COVERED by Tasks 1-2 through persisted repeated-failure state consumption in `evaluateGatePolicy()`.
REQ GATE-04: COVERED by Tasks 1-2 through policy human-approval rules for critical action classes and disposition retry unlocking.
REQ GATE-06: COVERED by Task 2 through policy-driven action/failure dimensions rather than external injected `failureCount`.
REQ SAFE-04: COVERED by Tasks 1-2 through protected-surface end-to-end retry unlocking.
RESEARCH: no package installs; Node built-ins/CommonJS only; runtime state stays under ignored `.advisor/`; protected surfaces reuse Plan 02/03 chains. COVERED by all tasks.
CONTEXT D-05/D-06/D-07/D-08: COVERED by persisted normalized signature threshold 2 and advisor consultation signal without direct host blocking for repeated failure.
CONTEXT D-09/D-10/D-11/D-12/D-13: COVERED by critical action human approval plus persisted approve/reject/revise/defer retry behavior.
CONTEXT D-14/D-15/D-16/D-17: COVERED by protected-surface retry wiring without changing path-class-first classification.
Deferred Ideas: none.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED end-to-end tests for verifier gaps</name>
  <files>.claude/advisor-mode/tests/failure-and-human-gates.test.js, .claude/advisor-mode/tests/protected-surface.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VERIFICATION.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-gate.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-failure-tracker.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/policy.example.json
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/failure-and-human-gates.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/protected-surface.test.js
  </read_first>
  <behavior>
    - Test 1: call `trackFailure()` twice with the same failure payload, then call `evaluateGatePolicy()` for the next matching PreToolUse attempt without `failureCount`; expect `advisor_consultation.required` based on `.advisor/state/failure-signatures.json`.
    - Test 1a: capture the signature key written by `trackFailure()` and assert that `evaluateGatePolicy()` derives and reads the same key via the exact shared `normalizeFailureSignature()` implementation; the test must fail if `advisor-gate.js` uses a duplicated or drifted normalizer.
    - Test 2: create a valid advisor recommendation for a destructive or force-push action, call `evaluateGatePolicy()` and expect `human_approval.required`; write a valid approve/reject/revise/defer disposition for the returned `correlationKey`, then call `evaluateGatePolicy()` again and expect satisfied/allow re-entry instead of a new `human_approval.required` packet.
    - Test 3: repeat Test 2 for a credential or production-affecting action class declared in `policy.example.json`.
    - Test 4: in `protected-surface.test.js`, create the recommendation and matching disposition for a protected critical surface, then retry through `evaluateGatePolicy()` and expect satisfied/allow re-entry instead of `human_approval.required`.
  </behavior>
  <action>Add end-to-end tests to the existing Node `node:test` files named in `<files>`. In `failure-and-human-gates.test.js`, use the existing temp-root helpers and `advisor-failure-tracker.trackFailure()` to populate `.advisor/state/failure-signatures.json`; do not pass `failureCount` into `evaluateGatePolicy()` for the repeated-failure assertion. Assert that the persisted key written by `trackFailure()` is the exact normalized signature key that `evaluateGatePolicy()` uses for lookup by importing the single `normalizeFailureSignature()` export from `.claude/hooks/advisor-failure-tracker.js`; this test must not accept a separately implemented equivalent normalizer. Add a critical action retry test that goes through `evaluateGatePolicy()` from recommendation to `human_approval.required` to persisted disposition to satisfied retry. In `protected-surface.test.js`, add the same disposition retry assertion through `evaluateGatePolicy()` for the protected critical branch. Keep helper-isolation tests, but ensure these new tests fail on the current implementation described in `02-VERIFICATION.md`.</action>
  <acceptance_criteria>
    - Repeated-failure test fails unless `evaluateGatePolicy()` reads `.advisor/state/failure-signatures.json`.
    - Repeated-failure test fails unless the signature key written by `trackFailure()` is the exact key read by `evaluateGatePolicy()` via the same `normalizeFailureSignature()` implementation exported by `.claude/hooks/advisor-failure-tracker.js`.
    - Critical action tests fail unless destructive, force-push, credential, and production-affecting classes route to `human-approval`.
    - Human approval retry tests fail unless `evaluateGatePolicy()` consumes valid persisted dispositions before re-emitting `human_approval.required`.
    - Protected-surface retry test fails unless the protected critical branch uses the same re-entry logic.
    - Tests use only Node built-ins and existing exported hook functions.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/failure-and-human-gates.test.js .claude/advisor-mode/tests/protected-surface.test.js</automated>
  </verify>
  <done>Verifier gaps are represented as failing end-to-end tests through `evaluateGatePolicy()`, including exact shared signature-key reuse from `trackFailure()` to `evaluateGatePolicy()`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN main evaluator state wiring and critical human-approval policy</name>
  <files>.claude/hooks/advisor-gate.js, .claude/advisor-mode/policy.example.json, .claude/advisor-mode/tests/failure-and-human-gates.test.js, .claude/advisor-mode/tests/protected-surface.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-gate.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-failure-tracker.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/policy.example.json
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/failure-and-human-gates.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/protected-surface.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VERIFICATION.md
  </read_first>
  <action>Update `evaluateGatePolicy(rawEvent, options = {})` in `.claude/hooks/advisor-gate.js` so it imports and calls the exact `normalizeFailureSignature()` export from `.claude/hooks/advisor-failure-tracker.js` to compute the current event's repeated-failure lookup key. Do not implement a second normalizer in `advisor-gate.js`; do not use an equivalent local copy. Read `.advisor/state/failure-signatures.json` from the same `root` option and apply the persisted count for the key produced by that shared normalizer before falling back to `event.failureCount`. Add a small pure helper such as `readPersistedFailureCount(event, options)` if needed and export it only if tests need direct verification, but that helper must call the shared `normalizeFailureSignature()` export rather than normalizing independently. Add or update tests proving that a signature key written by `trackFailure()` is the exact key read by `evaluateGatePolicy()`. Update the human-approval branch in `evaluateGatePolicy()` so, after a valid matching recommendation exists and before calling `buildDecisionPacket()` again, it builds the same packet identity/correlation context and calls `evaluateHumanGateReentry()` with the packet's `dispositionPath`; when status is satisfied, return an allow/satisfied result with the disposition metadata and do not emit a fresh `human_approval.required`. Apply this same re-entry check to the protected-surface human-approval rule path. Update `.claude/advisor-mode/policy.example.json` so destructive command and force-push patterns, credential-control actions, and production-affecting/shared-production actions have explicit human-approval rules per D-10/GATE-04; keep advisor-consultation rules where they are prerequisite to obtaining the recommendation, but make critical action classes escalate to `gateAction: "human-approval"` after recommendation exists. Preserve GATE-05 by not adding `Read` to PreToolUse matchers or low-risk escalation. Preserve Phase 2 architecture by using policy data, not hard-coded one-off action names in the hook.</action>
  <acceptance_criteria>
    - `evaluateGatePolicy()` reads persisted repeated-failure state from `.advisor/state/failure-signatures.json` and no longer requires caller-injected `failureCount` for threshold-2 escalation.
    - `evaluateGatePolicy()` and `trackFailure()` use the exact same `normalizeFailureSignature()` implementation exported by `.claude/hooks/advisor-failure-tracker.js`; no duplicated or equivalent normalizer exists in `advisor-gate.js`.
    - A signature key written by `trackFailure()` is the exact key read by `evaluateGatePolicy()` for the next matching attempt, proven by an end-to-end test.
    - Repeated-failure escalation returns an advisor-consultation signal for the next matching attempt and retains D-08 behavior by not turning the PostToolUse tracker itself into a blocking hook.
    - `policy.example.json` routes destructive commands, force-pushes, credential changes, and production-affecting/shared-production classes to human approval.
    - Critical action retry through `evaluateGatePolicy()` returns satisfied/allow after valid approve/reject/revise/defer disposition artifacts and matching recommendation artifacts exist.
    - Protected-surface critical retry through `evaluateGatePolicy()` returns satisfied/allow after valid disposition instead of rebuilding `human_approval.required`.
    - Existing high-risk advisor consultation and low-risk read-only tests continue to pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/failure-and-human-gates.test.js .claude/advisor-mode/tests/protected-surface.test.js .claude/advisor-mode/tests/advisor-consultation.test.js .claude/advisor-mode/tests/runtime-semantics.test.js</automated>
  </verify>
  <done>All verifier-reported runtime wiring gaps are fixed in the main evaluator and covered by end-to-end tests, including exact shared normalization from `trackFailure()` to `evaluateGatePolicy()`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full Phase 2 regression and gap evidence</name>
  <files>.claude/advisor-mode/tests/failure-and-human-gates.test.js, .claude/advisor-mode/tests/protected-surface.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VERIFICATION.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/failure-and-human-gates.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/protected-surface.test.js
  </read_first>
  <action>Run the full Phase 2 test suite and ensure the new end-to-end assertions specifically cover the two verifier spot-check failures: `trackFailure() + evaluateGatePolicy()` without injected `failureCount`, and `evaluateGatePolicy()` retry after valid human disposition for both critical actions and protected surfaces. Ensure the repeated-failure test explicitly states that `trackFailure()` writes the normalized signature key and `evaluateGatePolicy()` reads that same key via the shared `normalizeFailureSignature()` implementation. If a test name or assertion is too indirect for future verifier inspection, rename the test to include `evaluateGatePolicy` and the relevant gap phrase, for example `evaluateGatePolicy reads persisted repeated failure state using tracker signature key` and `evaluateGatePolicy unlocks protected human approval retry after disposition`.</action>
  <acceptance_criteria>
    - `node --test .claude/advisor-mode/tests/*.test.js` passes.
    - Test names or assertions make each verifier gap directly discoverable.
    - Test names or assertions make exact `trackFailure()` to `evaluateGatePolicy()` signature-key reuse directly discoverable.
    - No runtime `.advisor/*.json` or `.advisor/*.jsonl` artifacts are committed.
    - Existing Plans 02-01 through 02-04 remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/*.test.js</automated>
  </verify>
  <done>Full Phase 2 regression passes and gap closure evidence is clear enough for plan-checker/verifier.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                         |
| ------------------------------------------- | ----------------------------------------------------------------------------------- |
| PostToolUse failure state→PreToolUse gate   | Persisted `.advisor/state/failure-signatures.json` influences next gate evaluation. |
| Policy critical classes→human approval gate | Versioned policy decides which critical action classes require human disposition.   |
| Recommendation artifact→human packet        | A read-only advisor recommendation is prerequisite input for human approval.        |
| Disposition artifact→workflow re-entry      | Persisted approve/reject/revise/defer artifacts control retry unblocking.           |
| Protected path policy→human approval retry  | Protected Advisor Mode surfaces use the same disposition chain as critical actions. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                | Disposition | Mitigation Plan                                                                                                                                                         |
| ---------- | ---------------------- | ---------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-GC-01 | Tampering              | `.advisor/state/failure-signatures.json` | mitigate    | Read only normalized signature/count data using the same normalizer as `trackFailure()`; tests require matching next event and threshold 2 before escalation.           |
| T-02-GC-02 | Spoofing               | human disposition artifacts              | mitigate    | Reuse `evaluateHumanGateReentry()` validation for correlation key, appliesTo event, enum, decidedBy, decidedAt, and rationale before allowing retry.                    |
| T-02-GC-03 | Elevation of Privilege | critical action policy routing           | mitigate    | Policy routes destructive/force-push/credential/production-affecting classes to human approval; high severity critical actions cannot remain advisor-consultation only. |
| T-02-GC-04 | Repudiation            | protected-surface retry decisions        | mitigate    | Preserve `protected-surface.review` audit label and return disposition metadata on satisfied re-entry.                                                                  |
| T-02-GC-05 | Denial of Service      | repeated-failure state read errors       | mitigate    | Missing/unreadable state yields count 0 unless event/policy is otherwise gated; policy-load/classification/write hard-stop behavior from Plan 02 remains unchanged.     |
| T-02-GC-SC | Tampering              | npm/pip/cargo installs                   | accept      | No package-manager install tasks; Phase 2 Package Legitimacy Audit says no installs required.                                                                           |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/failure-and-human-gates.test.js .claude/advisor-mode/tests/protected-surface.test.js .claude/advisor-mode/tests/advisor-consultation.test.js .claude/advisor-mode/tests/runtime-semantics.test.js`, then run `node --test .claude/advisor-mode/tests/*.test.js`. The failure-and-human-gates coverage must prove that `trackFailure()` writes a signature key that `evaluateGatePolicy()` reads using the same `normalizeFailureSignature()` implementation exported by `.claude/hooks/advisor-failure-tracker.js`.
</verification>

<success_criteria>

- GATE-02 passes because persisted failure signatures influence the next PreToolUse `evaluateGatePolicy()` call without injected `failureCount`.
- The repeated-failure key contract passes because `trackFailure()` and `evaluateGatePolicy()` share the exact `normalizeFailureSignature()` implementation and the written key is the read key.
- GATE-04 passes because configured critical action classes route to human approval and valid approve/reject/revise/defer dispositions unlock explicit retry through the main evaluator.
- GATE-06 passes because failure count and critical class behavior are policy/runtime driven rather than manually injected.
- SAFE-04 passes because protected-surface recommendation/disposition retry is closed through `evaluateGatePolicy()`.
- No existing completed plan is rewritten; gap closure is isolated to this supplemental plan's target files.
  </success_criteria>

<output>
Create `/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-05-SUMMARY.md` when done.
</output>
