---
phase: 02-enforced-trigger-gates
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/advisor-mode/tests/runtime-semantics.test.js
  - .claude/hooks/advisor-runtime-probe.js
  - .planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
  - .planning/phases/02-enforced-trigger-gates/02-VALIDATION.md
autonomous: false
requirements:
  - GATE-01
  - GATE-04
  - GATE-06
must_haves:
  truths:
    - "Executor has a verified local contract for Claude Code hook blocking output before gate implementation depends on it."
    - "Human approval workflow waits for an explicit disposition artifact before re-entry can proceed."
    - "Phase 2 research no longer carries unresolved runtime semantics as an implementation-time unknown."
  artifacts:
    - path: ".claude/advisor-mode/tests/runtime-semantics.test.js"
      provides: "TDD coverage for hook output contract, blocked-pending-human state, missing-disposition behavior, and disposition re-entry validation"
    - path: ".claude/hooks/advisor-runtime-probe.js"
      provides: "Disposable hook probe used to empirically verify Claude Code blocking/wait semantics"
      exports: ["buildBlockingProbeOutput", "buildDispositionProbeOutput", "main"]
    - path: ".planning/phases/02-enforced-trigger-gates/02-RESEARCH.md"
      provides: "Formal resolved status for runtime hook semantics after the probe is executed"
    - path: ".planning/phases/02-enforced-trigger-gates/02-VALIDATION.md"
      provides: "Nyquist-compliant validation map with runtime semantics as Wave 1 prerequisite"
  key_links:
    - from: ".claude/hooks/advisor-runtime-probe.js"
      to: "Claude Code hook runtime"
      via: "PreToolUse probe output with blocking workflow metadata"
      pattern: "human_approval\.required"
    - from: ".advisor/decisions/dispositions/{correlationKey}.json"
      to: "re-entry gate state"
      via: "validated disposition artifact"
      pattern: "approve|reject|revise|defer"
---

<objective>
Close the runtime semantics blocker before implementation plans depend on it: prove the hook output shape, blocked-pending-human state, and disposition re-entry contract that later advisor and human gates must use.

Purpose: Resolve RESEARCH runtime uncertainty early and prevent downstream plans from assuming Claude Code blocking/wait behavior that has not been verified.
Output: Runtime semantics tests, a disposable hook probe, and updated Phase 2 research/validation artifacts that mark the blocking contract as resolved.
</objective>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/PROJECT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/STATE.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-PATTERNS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VALIDATION.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/settings.json
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-boundary-check.js

<interfaces>
Runtime gate states used by every later plan are canonical: `blocked-pending-advisor`, `blocked-pending-human`, and `satisfied`.
Human disposition artifacts live at `.advisor/decisions/dispositions/{correlationKey}.json` and contain `correlationKey`, `disposition`, `decidedBy`, `decidedAt`, `rationale`, and `appliesTo.event`.
Allowed dispositions are exactly `approve`, `reject`, `revise`, and `defer`.
No Phase 2 plan may treat a missing disposition as approval or as a soft warning.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED runtime semantics contract tests</name>
  <files>.claude/advisor-mode/tests/runtime-semantics.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-PATTERNS.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-boundary-check.js
  </read_first>
  <behavior>
    - Test 1: `buildBlockingProbeOutput` returns `hookSpecificOutput.hookEventName: "PreToolUse"`, event `human_approval.required`, `blocking: true`, and `workflowGateStatus: "blocked-pending-human"`.
    - Test 2: missing `.advisor/decisions/dispositions/{correlationKey}.json` keeps `reentryAllowed: false` and does not produce `workflowGateStatus: "satisfied"`.
    - Test 3: disposition values are accepted only when exactly `approve`, `reject`, `revise`, or `defer` and the `correlationKey` matches the blocked event.
    - Test 4: mismatched, malformed, stale, or absent disposition artifacts keep the workflow blocked.
  </behavior>
  <action>Create `.claude/advisor-mode/tests/runtime-semantics.test.js` using Node built-ins. Import future exports from `.claude/hooks/advisor-runtime-probe.js`. Use temp `.advisor/decisions/dispositions` directories so no runtime artifacts are committed. Encode D-09 through D-13 as workflow state-machine behavior, not host permission behavior.</action>
  <acceptance_criteria>
    - Test file exists and imports `.claude/hooks/advisor-runtime-probe.js` directly.
    - Tests fail before `.claude/hooks/advisor-runtime-probe.js` exists.
    - Tests require `blocking: true`, `blocked-pending-human`, and no re-entry without valid disposition.
    - Tests enumerate exactly `approve`, `reject`, `revise`, and `defer`.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/runtime-semantics.test.js</automated>
  </verify>
  <done>Runtime semantics RED tests exist and fail for missing probe implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN disposable Claude Code hook probe</name>
  <files>.claude/hooks/advisor-runtime-probe.js, .claude/advisor-mode/tests/runtime-semantics.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-boundary-check.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/runtime-semantics.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
  </read_first>
  <action>Create `.claude/hooks/advisor-runtime-probe.js` as a CommonJS fail-open PreToolUse probe. Export `buildBlockingProbeOutput(event, paths)`, `buildDispositionProbeOutput(event, disposition)`, and `main()`. The probe must read a representative tool event, derive a deterministic `correlationKey`, compute `.advisor/decisions/dispositions/{correlationKey}.json`, return `human_approval.required` with `blocking: true` and `workflowGateStatus: "blocked-pending-human"` when disposition is missing, and return `workflowGateStatus: "satisfied"` with `reentryAllowed: true` only for a valid matching disposition artifact.</action>
  <acceptance_criteria>
    - Probe uses only Node built-ins and CommonJS exports.
    - Probe exits 0 on malformed stdin or missing tool name.
    - Missing disposition is blocked-pending-human.
    - Valid matching approve/reject/revise/defer disposition is re-entry allowed with satisfied status.
    - Invalid or mismatched disposition remains blocked.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/runtime-semantics.test.js</automated>
  </verify>
  <done>Runtime semantics tests pass against the disposable hook probe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real Claude Code blocking/wait semantics and resolve research</name>
  <files>.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md, .planning/phases/02-enforced-trigger-gates/02-VALIDATION.md</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-runtime-probe.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/runtime-semantics.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VALIDATION.md
  </read_first>
  <action>Run a disposable real Claude Code hook smoke using `.claude/hooks/advisor-runtime-probe.js` as the PreToolUse hook for a harmless fixture action. Capture the observed runtime behavior: whether `hookSpecificOutput`, `blocking: true`, exit-code behavior, or a combination creates the wait state. Confirm missing disposition keeps the workflow blocked and a matching `.advisor/decisions/dispositions/{correlationKey}.json` with one of `approve|reject|revise|defer` permits re-entry. Update `02-RESEARCH.md` Open Questions to formal resolved status with the observed contract and update `02-VALIDATION.md` to `nyquist_compliant: true` with this plan as the Wave 1 runtime gate. If the real runtime contradicts the probe contract, stop execution and report BLOCKED instead of continuing to Plan 02.</action>
  <what-built>Disposable runtime probe and automated state-machine tests.</what-built>
  <how-to-verify>
    1. Use a throwaway workspace or branch and configure the probe as a PreToolUse hook for a harmless Bash fixture.
    2. Trigger the fixture with no disposition file and confirm Claude Code waits or blocks at the workflow decision point.
    3. Write `.advisor/decisions/dispositions/{correlationKey}.json` with a matching `correlationKey` and disposition `approve`, then re-trigger the same fixture and confirm re-entry reaches `workflowGateStatus: "satisfied"`.
    4. Repeat with `reject`, `revise`, and `defer` to confirm each is captured as an explicit disposition rather than implicit approval.
    5. Verify `02-RESEARCH.md` and `02-VALIDATION.md` record the observed contract and no longer leave runtime semantics unresolved.
  </how-to-verify>
  <acceptance_criteria>
    - Real Claude Code smoke confirms missing disposition stays blocked.
    - Real Claude Code smoke confirms matching approve/reject/revise/defer dispositions are captured and persisted.
    - `02-RESEARCH.md` marks hook blocking/wait semantics resolved by this plan, not deferred to implementation.
    - `02-VALIDATION.md` marks runtime semantics as a Wave 1 prerequisite and `nyquist_compliant: true`.
    - Execution stops if observed runtime semantics cannot support D-11.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/runtime-semantics.test.js</automated>
    <human-check>Real Claude Code hook smoke confirms D-11 wait-for-disposition semantics and research/validation artifacts record the exact observed contract.</human-check>
  </verify>
  <resume-signal>Type "approved" only after real runtime semantics are verified and research/validation files are updated, or describe the mismatch.</resume-signal>
  <done>Runtime semantics blocker is closed before advisor producer, high-risk gate, repeated-failure, human approval, or protected-surface plans execute.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                           |
| ------------------------------------ | --------------------------------------------------------------------- |
| Claude Code runtime→probe hook       | Real hook payload and blocking semantics enter the local contract.    |
| Probe hook→runtime disposition files | Local ignored disposition artifacts control re-entry state.           |
| Research artifact→downstream plans   | Resolved semantics become implementation constraints for later plans. |

## STRIDE Threat Register

| Threat ID | Category          | Component                                               | Disposition | Mitigation Plan                                                                                                    |
| --------- | ----------------- | ------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-02-01   | Spoofing          | `.advisor/decisions/dispositions/{correlationKey}.json` | mitigate    | Require matching `correlationKey`, allowed disposition enum, decidedBy, decidedAt, rationale, and appliesTo event. |
| T-02-02   | Tampering         | runtime hook semantics assumption                       | mitigate    | Empirically verify real Claude Code behavior before dependent plans run.                                           |
| T-02-03   | Repudiation       | human disposition capture                               | mitigate    | Persist explicit approve/reject/revise/defer artifact and reject missing or malformed disposition.                 |
| T-02-04   | Denial of Service | blocking hook probe                                     | mitigate    | Probe is disposable, fail-open on malformed input, and used only to resolve runtime semantics.                     |
| T-02-SC   | Tampering         | npm/pip/cargo installs                                  | accept      | No package-manager install tasks in this plan; Package Legitimacy Audit says no installs required.                 |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/runtime-semantics.test.js` and complete the real Claude Code hook smoke before any later Phase 2 plan executes.
</verification>

<success_criteria>

- GATE-04/D-11 runtime semantics are empirically verified before implementation depends on them.
- Disposition persistence/re-entry contract is explicit and tested.
- RESEARCH and VALIDATION no longer leave runtime semantics unresolved.
  </success_criteria>

<output>
Create `/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-01-SUMMARY.md` when done.
</output>
