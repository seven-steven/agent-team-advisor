---
phase: 02-enforced-trigger-gates
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/advisor-mode/tests/runtime-semantics.test.js
  - .claude/hooks/advisor-runtime-probe.js
  - .planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
  - .planning/phases/02-enforced-trigger-gates/02-VALIDATION.md
autonomous: false
requirements:
  - GATE-01
  - GATE-04
  - GATE-06
must_haves:
  truths:
    - "Executor has a verified local contract for Claude Code PreToolUse host enforcement using supported permissionDecision and exit-code semantics before later gate plans depend on it."
    - "Human approval remains workflow state plus persisted disposition artifacts; it is not modeled as unsupported host blocking metadata."
    - "A missing disposition keeps the workflow path blocked locally and requires explicit retry after a valid artifact exists; no Phase 2 plan assumes automatic in-hook wait-and-resume semantics."
    - "Phase 2 research no longer carries unresolved runtime semantics as an implementation-time unknown."
  artifacts:
    - path: ".claude/advisor-mode/tests/runtime-semantics.test.js"
      provides: "TDD coverage for supported PreToolUse decision output, ignored custom workflow metadata, explicit retry/disposition state, and valid disposition matching"
    - path: ".claude/hooks/advisor-runtime-probe.js"
      provides: "Disposable hook probe used to empirically verify Claude Code PreToolUse decision semantics"
      exports:
        ["buildPermissionDecisionOutput", "evaluateDispositionState", "main"]
    - path: ".planning/phases/02-enforced-trigger-gates/02-RESEARCH.md"
      provides: "Formal resolved status for runtime hook semantics after the probe is executed"
    - path: ".planning/phases/02-enforced-trigger-gates/02-VALIDATION.md"
      provides: "Nyquist-compliant validation map with supported host decision semantics as the Wave 1 prerequisite"
  key_links:
    - from: ".claude/hooks/advisor-runtime-probe.js"
      to: "Claude Code hook runtime"
      via: "PreToolUse hookSpecificOutput.permissionDecision and exit-code behavior"
      pattern: "permissionDecision"
    - from: ".advisor/decisions/dispositions/{correlationKey}.json"
      to: "workflow re-entry state"
      via: "validated disposition artifact plus explicit retry"
      pattern: "approve|reject|revise|defer"
---

<objective>
Close the runtime semantics blocker before implementation plans depend on it: prove the supported Claude Code PreToolUse host contract, separate host enforcement from local workflow state, and record the explicit retry/disposition model that later advisor and human gates must use.

Purpose: Resolve RESEARCH runtime uncertainty early and prevent downstream plans from assuming Claude Code will honor unsupported custom hook metadata.
Output: Runtime semantics tests, a disposable hook probe, and updated Phase 2 research/validation artifacts that mark the supported host contract as resolved.
</objective>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/PROJECT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/STATE.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-PATTERNS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VALIDATION.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/settings.json
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-boundary-check.js

<interfaces>
Host-enforced PreToolUse decisions must use supported fields: `hookSpecificOutput.permissionDecision`, `permissionDecisionReason`, `updatedInput`, and `additionalContext`, with hook exit-code semantics recorded from the real runtime smoke.
Custom workflow fields such as `workflowGateStatus`, `reentryAllowed`, `correlationKey`, and `dispositionPath` may be stored as local Advisor Mode state or artifacts, but no Phase 2 plan may assume Claude Code itself enforces them.
Human disposition artifacts live at `.advisor/decisions/dispositions/{correlationKey}.json` and contain `correlationKey`, `disposition`, `decidedBy`, `decidedAt`, `rationale`, and `appliesTo.event`.
Allowed dispositions are exactly `approve`, `reject`, `revise`, and `defer`.
No Phase 2 plan may treat a missing disposition as approval, as a soft warning, or as an automatic host wait state.
AskUserQuestion is a workflow-layer interaction surface, not the hook host contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED supported host-decision and explicit-retry runtime tests</name>
  <files>.claude/advisor-mode/tests/runtime-semantics.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-PATTERNS.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-boundary-check.js
  </read_first>
  <behavior>
    - Test 1: `buildPermissionDecisionOutput` returns `hookSpecificOutput.hookEventName: "PreToolUse"`, a supported `permissionDecision`, `permissionDecisionReason`, and no requirement for custom host-enforced workflow keys.
    - Test 2: custom workflow metadata is modeled separately from the supported host decision payload and is not required for the host contract tests to pass.
    - Test 3: missing `.advisor/decisions/dispositions/{correlationKey}.json` keeps local workflow state `blocked-pending-human` with `retryRequired: true` and does not produce local `workflowGateStatus: "satisfied"`.
    - Test 4: disposition values are accepted only when exactly `approve`, `reject`, `revise`, or `defer` and the `correlationKey` plus `appliesTo.event` match the blocked event.
    - Test 5: mismatched, malformed, stale, or absent disposition artifacts keep the workflow path blocked locally and require explicit retry after correction.
  </behavior>
  <action>Create `.claude/advisor-mode/tests/runtime-semantics.test.js` using Node built-ins. Import future exports from `.claude/hooks/advisor-runtime-probe.js`. Use temp `.advisor/decisions/dispositions` directories so no runtime artifacts are committed. Encode supported PreToolUse host semantics separately from local Advisor Mode workflow state, and encode D-09 through D-13 as explicit retry/disposition behavior rather than automatic host waiting behavior.</action>
  <acceptance_criteria>
    - Test file exists and imports `.claude/hooks/advisor-runtime-probe.js` directly.
    - Tests fail before `.claude/hooks/advisor-runtime-probe.js` exists.
    - Tests require supported PreToolUse decision fields and reject reliance on custom host-enforced workflow metadata.
    - Tests enumerate exactly `approve`, `reject`, `revise`, and `defer`.
    - Tests require explicit retry after valid disposition instead of automatic host wait-and-resume.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/runtime-semantics.test.js</automated>
  </verify>
  <done>Runtime semantics RED tests exist and fail for missing probe implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN disposable Claude Code host-contract probe</name>
  <files>.claude/hooks/advisor-runtime-probe.js, .claude/advisor-mode/tests/runtime-semantics.test.js</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-boundary-check.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/runtime-semantics.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
  </read_first>
  <action>Create `.claude/hooks/advisor-runtime-probe.js` as a CommonJS fail-open PreToolUse probe. Export `buildPermissionDecisionOutput(decision, context)`, `evaluateDispositionState(event, paths)`, and `main()`. The probe must read a representative tool event, derive a deterministic `correlationKey`, compute `.advisor/decisions/dispositions/{correlationKey}.json`, emit only supported host decision fields for Claude Code runtime testing, keep missing disposition as local blocked state with explicit retry required, and return local `satisfied` state only for a valid matching disposition artifact.</action>
  <acceptance_criteria>
    - Probe uses only Node built-ins and CommonJS exports.
    - Probe exits 0 on malformed stdin or missing tool name.
    - Host decision payload uses supported PreToolUse fields only.
    - Missing disposition is local blocked-pending-human with retry required.
    - Valid matching approve/reject/revise/defer disposition unlocks explicit retry state.
    - Invalid or mismatched disposition remains blocked locally.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/runtime-semantics.test.js</automated>
  </verify>
  <done>Runtime semantics tests pass against the disposable host-contract probe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real Claude Code host semantics and resolve research</name>
  <files>.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md, .planning/phases/02-enforced-trigger-gates/02-VALIDATION.md</files>
  <read_first>
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/hooks/advisor-runtime-probe.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.claude/advisor-mode/tests/runtime-semantics.test.js
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-RESEARCH.md
    - /home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-VALIDATION.md
  </read_first>
  <action>Run a disposable real Claude Code hook smoke using `.claude/hooks/advisor-runtime-probe.js` as the PreToolUse hook for harmless fixture actions. Capture the observed runtime behavior: which supported `permissionDecision` values are honored, how exit 0 versus exit 2 behave, and that unsupported custom workflow metadata is ignored for host enforcement. Confirm the local disposition model still requires explicit retry after a valid `.advisor/decisions/dispositions/{correlationKey}.json` with one of `approve|reject|revise|defer`. Update `02-RESEARCH.md` and `02-VALIDATION.md` to formal resolved status with the observed host contract. If the real runtime contradicts the supported decision contract needed by Plan 02, stop execution and report BLOCKED instead of continuing to Plan 02.</action>
  <what-built>Disposable host-contract probe and automated explicit-retry state-machine tests.</what-built>
  <how-to-verify>
    1. Use a throwaway workspace or branch and configure the probe as a PreToolUse hook for a harmless Bash fixture.
    2. Trigger the fixture with supported decision outputs and confirm which decisions Claude Code actually honors.
    3. Trigger the fixture with unsupported custom workflow metadata and confirm it is ignored for host enforcement.
    4. Write `.advisor/decisions/dispositions/{correlationKey}.json` with a matching `correlationKey` and disposition `approve`, then explicitly re-trigger the same fixture and confirm the local state reaches `workflowGateStatus: "satisfied"`.
    5. Repeat with `reject`, `revise`, and `defer` to confirm each is captured as explicit disposition state rather than implicit approval.
    6. Verify `02-RESEARCH.md` and `02-VALIDATION.md` record the observed host contract and no longer leave runtime semantics unresolved.
  </how-to-verify>
  <acceptance_criteria>
    - Real Claude Code smoke confirms the supported PreToolUse decision contract needed by later plans.
    - Real Claude Code smoke confirms unsupported custom workflow metadata is not treated as host enforcement.
    - Local disposition flow confirms missing disposition stays blocked until explicit retry after a valid artifact exists.
    - `02-RESEARCH.md` marks runtime semantics resolved by this plan, not deferred to implementation.
    - `02-VALIDATION.md` marks supported host semantics as a Wave 1 prerequisite and `nyquist_compliant: true`.
    - Execution stops if observed runtime semantics cannot support the Plan 02 gate contract.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/runtime-semantics.test.js</automated>
    <human-check>Real Claude Code hook smoke confirms the supported host decision contract and research/validation artifacts record the exact observed semantics.</human-check>
  </verify>
  <resume-signal>Type "approved" only after real runtime semantics are verified and research/validation files are updated, or describe the mismatch.</resume-signal>
  <done>Runtime semantics blocker is closed before advisor producer, high-risk gate, repeated-failure, human approval, or protected-surface plans execute.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                |
| ------------------------------------ | -------------------------------------------------------------------------- |
| Claude Code runtime→probe hook       | Real hook payload and supported host decision semantics enter local probe. |
| Probe hook→runtime disposition files | Local ignored disposition artifacts control workflow re-entry state.       |
| Research artifact→downstream plans   | Resolved semantics become implementation constraints for later plans.      |

## STRIDE Threat Register

| Threat ID | Category          | Component                                               | Disposition | Mitigation Plan                                                                                                    |
| --------- | ----------------- | ------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-02-01   | Spoofing          | `.advisor/decisions/dispositions/{correlationKey}.json` | mitigate    | Require matching `correlationKey`, allowed disposition enum, decidedBy, decidedAt, rationale, and appliesTo event. |
| T-02-02   | Tampering         | runtime hook semantics assumption                       | mitigate    | Empirically verify real Claude Code host behavior before dependent plans run.                                      |
| T-02-03   | Repudiation       | human disposition capture                               | mitigate    | Persist explicit approve/reject/revise/defer artifact and reject missing or malformed disposition.                 |
| T-02-04   | Denial of Service | blocking hook probe                                     | mitigate    | Probe is disposable, fail-open on malformed input, and used only to resolve runtime semantics.                     |
| T-02-SC   | Tampering         | npm/pip/cargo installs                                  | accept      | No package-manager install tasks in this plan; Package Legitimacy Audit says no installs required.                 |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/runtime-semantics.test.js` and complete the real Claude Code hook smoke before any later Phase 2 plan executes.
</verification>

<success_criteria>

- GATE-04 and GATE-06 runtime semantics are empirically verified before implementation depends on them.
- Supported Claude Code host decision semantics are separated from local workflow state.
- Disposition persistence/re-entry contract is explicit and tested as an explicit retry path.
- RESEARCH and VALIDATION no longer leave runtime semantics unresolved.
  </success_criteria>

<output>
Create `/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/02-enforced-trigger-gates/02-01-SUMMARY.md` when done.
</output>
