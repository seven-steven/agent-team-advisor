---
phase: 03-verdict-handoff-and-verification-evidence
plan: 04
type: tdd
wave: 4
depends_on: [03-03]
files_modified:
  - .claude/advisor-mode/final-review.js
  - .claude/advisor-mode/verification-evidence.schema.json
  - .claude/advisor-mode/tests/verification-evidence.test.js
autonomous: true
requirements: [AUDT-02, GATE-03]
user_setup: []
must_haves:
  truths:
    - "User can capture verification evidence for guarded work with command entries, exit statuses, concise summaries, changed files, and residual risks."
    - "Each verification-evidence artifact is an immutable snapshot tied to one final-review/completion decision."
  artifacts:
    - path: ".claude/advisor-mode/verification-evidence.schema.json"
      provides: "strict verification evidence artifact schema"
      contains: "commands"
    - path: ".claude/advisor-mode/final-review.js"
      provides: "recordVerificationEvidence and validateVerificationEvidence exports"
      exports: ["recordVerificationEvidence", "validateVerificationEvidence"]
    - path: ".claude/advisor-mode/tests/verification-evidence.test.js"
      provides: "TDD coverage for immutable verification evidence snapshots"
  key_links:
    - from: ".claude/advisor-mode/final-review.js"
      to: ".advisor/evidence/verification/{correlationKey}.json"
      via: "recordVerificationEvidence writes immutable snapshot"
      pattern: "verification"
    - from: ".claude/advisor-mode/final-review.js"
      to: ".advisor/audit/events.jsonl"
      via: "verification evidence audit event"
      pattern: "verification.evidence.recorded"
---

<objective>
Create the verification evidence capture slice.

Purpose: satisfy AUDT-02 and D-13 through D-16 by making guarded-task verification factual, concise, and immutable.
Output: verification evidence schema, record/validate functions, audit event append behavior, and tests.
</objective>

## Phase Goal

**As a** user, **I want to** make completion decisions from structured advisor verdicts, concise context packets, executor rationale, and verification evidence, **so that** I can complete guarded work with auditable confidence.

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
@.claude/advisor-mode/final-review.js
@.claude/hooks/advisor-failure-tracker.js
@.claude/advisor-mode/tests/scaffold-layout.test.js

<interfaces>
Phase 2 audit writer pattern appends concise JSON lines to `.advisor/audit/events.jsonl`.
Phase 3 evidence artifacts must use `.advisor/evidence/verification/{correlationKey}.json` and remain separate from `.planning/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED verification evidence snapshot contract</name>
  <files>.claude/advisor-mode/tests/verification-evidence.test.js, .claude/advisor-mode/verification-evidence.schema.json, .claude/advisor-mode/final-review.js</files>
  <read_first>
    - .claude/advisor-mode/final-review.js
    - .claude/hooks/advisor-failure-tracker.js
    - .claude/advisor-mode/tests/scaffold-layout.test.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
  </read_first>
  <behavior>
    - `recordVerificationEvidence(input, { root })` writes one artifact per guarded task at `.advisor/evidence/verification/{correlationKey}.json` per D-13.
    - Each command entry requires `command`, `exit_status`, `summary`, and `timestamp` per D-14.
    - Package-level fields include `changed_files`, `residual_risks`, `created_at`, and final-review correlation fields.
    - General development commands are rejected when `purpose` is not a verification purpose per D-15.
  </behavior>
  <action>Write failing node:test coverage in `.claude/advisor-mode/tests/verification-evidence.test.js` using temp repo fixtures. Create `.claude/advisor-mode/verification-evidence.schema.json` as a strict Draft 2020-12 schema with `artifact_type: "verification-evidence"`, `correlationKey`, `commands`, `changed_files`, `residual_risks`, and `created_at`. Tests must assert verification command purposes are limited to `test`, `typecheck`, `lint`, `smoke`, `assertion`, or `manual-check-summary`, and that a second write for the same correlation key does not overwrite the existing snapshot per D-16.</action>
  <acceptance_criteria>
    - Source assertion: `.claude/advisor-mode/verification-evidence.schema.json` contains required fields `commands`, `changed_files`, and `residual_risks`.
    - Behavior assertion: RED run fails before `recordVerificationEvidence` and `validateVerificationEvidence` are implemented.
    - Behavior assertion: test includes an attempted overwrite and expects an immutable snapshot error or non-overwrite result.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/verification-evidence.test.js` exits non-zero during RED.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/verification-evidence.test.js</automated>
  </verify>
  <done>Failing tests encode command-level evidence, package summaries, verification-only filtering, and immutable snapshot behavior from D-13 through D-16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN verification evidence recorder</name>
  <files>.claude/advisor-mode/final-review.js, .claude/advisor-mode/tests/verification-evidence.test.js</files>
  <read_first>
    - .claude/advisor-mode/final-review.js
    - .claude/advisor-mode/verification-evidence.schema.json
    - .claude/advisor-mode/tests/verification-evidence.test.js
    - .claude/hooks/advisor-failure-tracker.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
  </read_first>
  <action>Implement `recordVerificationEvidence(input = {}, options = {})` and `validateVerificationEvidence(artifact)` in `.claude/advisor-mode/final-review.js`. Accept only command entries with `purpose` in `test`, `typecheck`, `lint`, `smoke`, `assertion`, or `manual-check-summary`; require `command`, numeric or string `exit_status`, concise `summary`, and `timestamp`; require package-level `changed_files` and `residual_risks`; write `.advisor/evidence/verification/{correlationKey}.json` only if it does not already exist; append `.advisor/audit/events.jsonl` event `verification.evidence.recorded`. Do not store full stdout/stderr logs or development commands.</action>
  <acceptance_criteria>
    - Behavior assertion: valid command-level evidence validates and writes exactly one JSON artifact.
    - Behavior assertion: general development command purpose returns `ok: false` or throws a controlled validation error before writing.
    - Behavior assertion: second write for the same correlation key leaves the original artifact unchanged.
    - CLI assertion: `.advisor/audit/events.jsonl` contains one `verification.evidence.recorded` line after successful write.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/verification-evidence.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/verification-evidence.test.js</automated>
  </verify>
  <done>Guarded work has immutable, concise verification evidence snapshots suitable for final review freshness checks.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                              |
| ----------------------------------------- | ------------------------------------------------------------------------ |
| executor command output→evidence artifact | Verification result summaries enter durable local evidence.              |
| evidence artifact→final gate              | Completion gate later trusts evidence freshness and changed-file fields. |

## STRIDE Threat Register

| Threat ID | Category               | Component                      | Disposition | Mitigation Plan                                                                                |
| --------- | ---------------------- | ------------------------------ | ----------- | ---------------------------------------------------------------------------------------------- |
| T-03-09   | Information Disclosure | recordVerificationEvidence     | mitigate    | Store concise summaries only; exclude full stdout/stderr and general development command logs. |
| T-03-10   | Tampering              | verification evidence artifact | mitigate    | Refuse overwrite for same correlation key; later rounds create new artifacts per D-16.         |
| T-03-11   | Repudiation            | evidence audit event           | mitigate    | Append `verification.evidence.recorded` with correlation key and artifact path.                |
| T-03-SC   | Tampering              | npm/pip/cargo installs         | accept      | No package-manager install task exists in this plan.                                           |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/verification-evidence.test.js`.
</verification>

<success_criteria>

- AUDT-02 evidence fields are captured.
- D-13 one artifact per guarded task is implemented.
- D-14 command/package fields are enforced.
- D-15 excludes development commands.
- D-16 immutable snapshot behavior is tested.
  </success_criteria>

<output>
Create `.planning/phases/03-verdict-handoff-and-verification-evidence/03-04-SUMMARY.md` when done.
</output>
