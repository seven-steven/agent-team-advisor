---
phase: 03-verdict-handoff-and-verification-evidence
plan: 02
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - .claude/advisor-mode/final-review.js
  - .claude/advisor-mode/verdict.schema.json
  - .claude/advisor-mode/tests/verdict-handoff.test.js
  - .claude/agents/advisor-reviewer.md
autonomous: true
requirements: [VERD-01, GATE-03]
user_setup: []
must_haves:
  truths:
    - "User can receive a schema-valid advisor final verdict with status, risk, confidence, blocking findings, recommended actions, and validation checklist."
    - "Only PASS is directly completable; CONCERNS, FAIL, and BLOCKED require executor follow-up before completion proceeds."
  artifacts:
    - path: ".claude/advisor-mode/verdict.schema.json"
      provides: "structured advisor final verdict contract"
      contains: "validation_checklist"
    - path: ".claude/advisor-mode/final-review.js"
      provides: "validateVerdict and normalizeRecommendedActions exports"
      exports: ["validateVerdict", "normalizeRecommendedActions"]
    - path: ".claude/advisor-mode/tests/verdict-handoff.test.js"
      provides: "TDD coverage for final verdict validation and non-PASS follow-up requirement"
    - path: ".claude/agents/advisor-reviewer.md"
      provides: "advisor prompt for final verdict fields"
  key_links:
    - from: ".claude/advisor-mode/final-review.js"
      to: ".claude/advisor-mode/verdict.schema.json"
      via: "verdict schema validation"
      pattern: "verdict.schema.json"
    - from: ".claude/agents/advisor-reviewer.md"
      to: ".claude/advisor-mode/verdict.schema.json"
      via: "matching output field names"
      pattern: "validation checklist"
---

<objective>
Create the structured advisor final verdict slice.

Purpose: satisfy VERD-01 and the D-01/D-03/D-04 completion-review contract before executor follow-up and final gate logic consume verdicts.
Output: extended verdict schema, validator/normalizer functions, advisor output contract update, and verdict handoff tests.
</objective>

## Phase Goal

**As a** user, **I want to** make completion decisions from structured advisor verdicts, concise context packets, executor rationale, and verification evidence, **so that** I can complete guarded work with auditable confidence.

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
@.claude/advisor-mode/verdict.schema.json
@.claude/agents/advisor-reviewer.md
@.claude/advisor-mode/tests/advisor-agent.test.js

<interfaces>
Existing verdict fields: `status`, `risk`, `confidence`, `blocking_findings`, `recommended_actions`, `verification_guidance`.
Status enum remains `PASS`, `CONCERNS`, `FAIL`, `BLOCKED`; D-04 says only `PASS` allows direct completion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED final verdict validation contract</name>
  <files>.claude/advisor-mode/tests/verdict-handoff.test.js, .claude/advisor-mode/verdict.schema.json, .claude/advisor-mode/final-review.js</files>
  <read_first>
    - .claude/advisor-mode/verdict.schema.json
    - .claude/advisor-mode/tests/advisor-agent.test.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
  </read_first>
  <behavior>
    - `validateVerdict(verdict)` accepts a final-review verdict with required `status`, `risk`, `confidence`, `blocking_findings`, `recommended_actions`, `validation_checklist`, `correlationKey`, `context_packet_ref`, and `created_at`.
    - `validateVerdict(verdict)` rejects malformed status/risk/confidence values and missing validation checklist items.
    - `validateVerdict(verdict)` returns `direct_completion_allowed: true` only for `status: "PASS"`; all other statuses return false per D-04.
  </behavior>
  <action>Write failing node:test coverage in `.claude/advisor-mode/tests/verdict-handoff.test.js` before implementation. Extend `.claude/advisor-mode/verdict.schema.json` instead of replacing it, preserving the existing required verdict-first fields and adding final-review fields `correlationKey`, `context_packet_ref`, `validation_checklist`, and `created_at`. Normalize `recommended_actions` in tests as action objects with stable `id` and `description` so later executor decisions can key per recommendation. Do not add package dependencies.</action>
  <acceptance_criteria>
    - Source assertion: `.claude/advisor-mode/verdict.schema.json` still contains the existing `status`, `risk`, `confidence`, `blocking_findings`, `recommended_actions`, and `verification_guidance` fields.
    - Source assertion: schema contains `validation_checklist`, `correlationKey`, and `context_packet_ref`.
    - Behavior assertion: RED run fails before `validateVerdict` is implemented or before schema supports final-review fields.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/verdict-handoff.test.js` exits non-zero during RED.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/verdict-handoff.test.js</automated>
  </verify>
  <done>Failing tests define the final verdict schema and D-04 PASS-only completion rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN verdict validator and advisor instructions</name>
  <files>.claude/advisor-mode/final-review.js, .claude/agents/advisor-reviewer.md, .claude/advisor-mode/tests/verdict-handoff.test.js</files>
  <read_first>
    - .claude/advisor-mode/final-review.js
    - .claude/agents/advisor-reviewer.md
    - .claude/advisor-mode/tests/verdict-handoff.test.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
  </read_first>
  <action>Implement `validateVerdict(verdict)` and `normalizeRecommendedActions(recommended_actions)` in `.claude/advisor-mode/final-review.js`. `normalizeRecommendedActions` must convert string actions to `{ id: "rec-001", description: "..." }` style objects and preserve existing object IDs. `validateVerdict` must require concrete checklist entries in `validation_checklist`, return validation errors without throwing for malformed input, and compute `direct_completion_allowed` exactly from `status === "PASS"` per D-04. Update `.claude/agents/advisor-reviewer.md` so the output contract names risk level, confidence, blocking findings, recommended actions, and validation checklist per VERD-01 while preserving read-only tools.</action>
  <acceptance_criteria>
    - Behavior assertion: PASS verdicts return `ok: true` and `direct_completion_allowed: true`.
    - Behavior assertion: CONCERNS, FAIL, and BLOCKED verdicts return `ok: true` and `direct_completion_allowed: false`.
    - Behavior assertion: missing or empty `validation_checklist` returns `ok: false`.
    - Source assertion: `.claude/agents/advisor-reviewer.md` still contains `tools: Read, Grep, Glob` and contains `validation checklist`.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/verdict-handoff.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/verdict-handoff.test.js</automated>
  </verify>
  <done>Structured final verdicts validate and non-PASS statuses are explicitly marked as requiring executor follow-up before completion.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                             |
| --------------------------- | ----------------------------------------------------------------------- |
| advisor→executor            | Advisor-produced verdict data enters executor completion decision flow. |
| verdict artifact→final gate | Final gate later relies on verdict status and freshness fields.         |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                              |
| --------- | ---------------------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------------- |
| T-03-03   | Tampering              | verdict.schema.json    | mitigate    | Strict schema extension preserves required fields and rejects malformed final verdicts.      |
| T-03-04   | Elevation of Privilege | advisor-reviewer.md    | mitigate    | Preserve `tools: Read, Grep, Glob`; advisor only returns recommendations and verdicts.       |
| T-03-05   | Repudiation            | validateVerdict        | mitigate    | Require `correlationKey`, `context_packet_ref`, `created_at`, and checklist evidence fields. |
| T-03-SC   | Tampering              | npm/pip/cargo installs | accept      | No package-manager install task exists in this plan.                                         |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/verdict-handoff.test.js` and inspect advisor tools remain read-only.
</verification>

<success_criteria>

- VERD-01 structured verdict fields validate.
- D-01/D-03 final-review verdict is distinct from earlier consultations through correlation/context fields.
- D-04 PASS-only direct completion rule is encoded for downstream gate use.
  </success_criteria>

<output>
Create `.planning/phases/03-verdict-handoff-and-verification-evidence/03-02-SUMMARY.md` when done.
</output>
