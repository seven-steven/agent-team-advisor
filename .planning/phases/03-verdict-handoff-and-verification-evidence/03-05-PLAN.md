---
phase: 03-verdict-handoff-and-verification-evidence
plan: 05
type: tdd
wave: 5
depends_on: [03-04]
files_modified:
  - .claude/advisor-mode/final-review.js
  - .claude/hooks/advisor-final-review-gate.js
  - .claude/settings.json
  - .claude/advisor-mode/tests/final-review-gate.test.js
autonomous: true
requirements: [GATE-03, VERD-02, AUDT-02]
user_setup: []
must_haves:
  truths:
    - "User can require a fresh advisor final review before non-trivial work is marked complete."
    - "Freshness is tied to current evidence and changed files, so stale prior advisor consultations cannot satisfy completion."
    - "Non-PASS verdicts require executor-decision artifacts before completion proceeds."
  artifacts:
    - path: ".claude/hooks/advisor-final-review-gate.js"
      provides: "completion-time final-review hook gate"
      exports: ["evaluateFinalReviewGate"]
    - path: ".claude/advisor-mode/final-review.js"
      provides: "isFinalReviewFresh and recordFinalReviewState exports"
      exports: ["isFinalReviewFresh", "recordFinalReviewState"]
    - path: ".claude/settings.json"
      provides: "Stop hook wiring for final review gate"
      contains: "advisor-final-review-gate.js"
    - path: ".claude/advisor-mode/tests/final-review-gate.test.js"
      provides: "TDD coverage for final review freshness and Stop hook blocking"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/advisor-final-review-gate.js"
      via: "Stop hook command"
      pattern: "advisor-final-review-gate.js"
    - from: ".claude/hooks/advisor-final-review-gate.js"
      to: ".advisor/state/final-review.json"
      via: "freshness state read"
      pattern: "final-review.json"
    - from: ".claude/hooks/advisor-final-review-gate.js"
      to: ".advisor/evidence/verification/{correlationKey}.json"
      via: "evidence ref freshness check"
      pattern: "verification"
    - from: ".claude/hooks/advisor-final-review-gate.js"
      to: ".advisor/decisions/executor/{correlationKey}.json"
      via: "non-PASS executor decision requirement"
      pattern: "executor"
---

<objective>
Create the completion-time final review gate slice.

Purpose: satisfy GATE-03 by requiring fresh advisor final review for explicit non-trivial task state and binding completion to current verdict, evidence, and executor decision artifacts.
Output: final-review gate hook, freshness functions, Stop hook wiring, and final-review gate tests.
</objective>

## Phase Goal

**As a** user, **I want to** make completion decisions from structured advisor verdicts, concise context packets, executor rationale, and verification evidence, **so that** I can complete guarded work with auditable confidence.

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
@.claude/settings.json
@.claude/hooks/advisor-boundary-check.js
@.claude/hooks/advisor-gate.js
@.claude/advisor-mode/final-review.js
@.claude/advisor-mode/tests/boundary.test.js

<interfaces>
Required upstream exports from earlier Phase 3 plans:
- `validateVerdict(verdict)` from 03-02.
- `validateExecutorDecision(artifact, verdict)` from 03-03.
- `validateVerificationEvidence(artifact)` from 03-04.
Final review gate must use explicit task/workflow state per D-02, not fuzzy heuristics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED final-review freshness gate contract</name>
  <files>.claude/advisor-mode/tests/final-review-gate.test.js, .claude/hooks/advisor-final-review-gate.js, .claude/advisor-mode/final-review.js</files>
  <read_first>
    - .claude/hooks/advisor-boundary-check.js
    - .claude/hooks/advisor-gate.js
    - .claude/advisor-mode/final-review.js
    - .claude/advisor-mode/tests/boundary.test.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
  </read_first>
  <behavior>
    - `evaluateFinalReviewGate(event, { root })` returns `gateAction: "block"` for explicit `taskState: "non-trivial-completion"` when no fresh final verdict exists per D-01 and D-02.
    - Prior high-risk advisor consultations do not satisfy final completion review per D-03.
    - `status: "PASS"` with matching verification evidence and unchanged changed-file fingerprint returns `gateAction: "allow"`.
    - `CONCERNS`, `FAIL`, and `BLOCKED` require a valid executor-decision artifact before `gateAction: "allow"` per D-04 and VERD-02.
  </behavior>
  <action>Write failing node:test coverage in `.claude/advisor-mode/tests/final-review-gate.test.js` using temp repo fixtures. Tests must require `.claude/hooks/advisor-final-review-gate.js` and assert missing verdict, stale verdict, PASS fresh verdict, non-PASS missing executor decision, and non-PASS with executor decision cases. Create `.claude/hooks/advisor-final-review-gate.js` with a CommonJS export placeholder for `evaluateFinalReviewGate` only as needed for RED. Do not rely on heuristic keywords in transcript; use explicit `taskState: "non-trivial-completion"` or `requiresFinalReview: true` inputs per D-02.</action>
  <acceptance_criteria>
    - Behavior assertion: RED run fails before `evaluateFinalReviewGate`, `isFinalReviewFresh`, and `recordFinalReviewState` are implemented.
    - Behavior assertion: test fixture includes stale changed files/evidence ref mismatch and expects block.
    - Behavior assertion: test fixture includes an earlier `.advisor/consultations/recommendations/*.json` artifact and expects it not to satisfy completion.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/final-review-gate.test.js` exits non-zero during RED.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/final-review-gate.test.js</automated>
  </verify>
  <done>Failing tests encode D-01 through D-04 final-review gate freshness semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN final-review hook and freshness state</name>
  <files>.claude/advisor-mode/final-review.js, .claude/hooks/advisor-final-review-gate.js, .claude/advisor-mode/tests/final-review-gate.test.js</files>
  <read_first>
    - .claude/advisor-mode/final-review.js
    - .claude/hooks/advisor-final-review-gate.js
    - .claude/advisor-mode/tests/final-review-gate.test.js
    - .claude/hooks/advisor-boundary-check.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
  </read_first>
  <action>Implement `recordFinalReviewState(input = {}, options = {})` and `isFinalReviewFresh(input = {}, options = {})` in `.claude/advisor-mode/final-review.js`. State file path is `.advisor/state/final-review.json`. Bind freshness to `correlationKey`, `verdict_ref`, `verification_evidence_ref`, `executor_decision_ref` when required, `changed_files`, `changed_files_fingerprint`, and `reviewed_at`. Implement `.claude/hooks/advisor-final-review-gate.js` with `evaluateFinalReviewGate(event, options)` and stdin main using the established hook timeout/fail-open parsing pattern. For explicit non-trivial completion without fresh review, return a blocking Stop-hook output with concise context telling the executor to build a context packet, obtain a fresh advisor final verdict, record verification evidence, and record executor decisions for non-PASS statuses.</action>
  <acceptance_criteria>
    - Behavior assertion: explicit non-trivial completion with no final review blocks.
    - Behavior assertion: earlier consultation recommendation artifacts alone block and state why final review is required.
    - Behavior assertion: PASS final verdict tied to matching evidence and changed-file fingerprint allows.
    - Behavior assertion: non-PASS final verdict blocks until matching executor-decision artifact validates.
    - Source assertion: `.claude/hooks/advisor-final-review-gate.js` exports `evaluateFinalReviewGate` and uses `hookEventName: "Stop"` in hook output.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/final-review-gate.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/final-review-gate.test.js</automated>
  </verify>
  <done>Final review freshness is enforced for explicit non-trivial completion and respects PASS-only direct completion plus executor-decision requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Stop hook idempotently</name>
  <files>.claude/settings.json, .claude/advisor-mode/tests/final-review-gate.test.js</files>
  <read_first>
    - .claude/settings.json
    - .claude/advisor-mode/tests/final-review-gate.test.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
  </read_first>
  <action>Extend `.claude/settings.json` idempotently with a `Stop` hook entry that runs `"/usr/bin/node" "$CLAUDE_PROJECT_DIR"/.claude/hooks/advisor-final-review-gate.js` with timeout 5. Preserve all existing `SessionStart`, `PostToolUse`, and `PreToolUse` entries exactly except for adding the new Stop hook. Add or extend final-review-gate test coverage to assert settings contain exactly one command referencing `advisor-final-review-gate.js` after the wiring helper or implementation step runs.</action>
  <acceptance_criteria>
    - Source assertion: `.claude/settings.json` contains a `Stop` hook command referencing `.claude/hooks/advisor-final-review-gate.js`.
    - Source assertion: existing `advisor-gate.js`, `advisor-boundary-check.js`, and `advisor-failure-tracker.js` hook entries remain present.
    - Behavior assertion: test asserts no duplicate `advisor-final-review-gate.js` commands.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/final-review-gate.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/final-review-gate.test.js</automated>
  </verify>
  <done>Completion-stage final review is wired through project-local Claude Code settings without disrupting prior Phase 1/2 hooks.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                |
| ---------------------------------------- | -------------------------------------------------------------------------- |
| Claude Code Stop event→local hook        | Completion lifecycle payload controls whether guarded completion proceeds. |
| local state/evidence→completion decision | Final review freshness state and evidence artifacts authorize completion.  |

## STRIDE Threat Register

| Threat ID | Category    | Component                    | Disposition | Mitigation Plan                                                                                              |
| --------- | ----------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------ |
| T-03-12   | Spoofing    | final-review state           | mitigate    | Bind state to correlation key, verdict ref, evidence ref, changed files fingerprint, and reviewed timestamp. |
| T-03-13   | Tampering   | advisor-final-review-gate.js | mitigate    | Validate artifacts through final-review.js functions and block stale/missing mismatched evidence.            |
| T-03-14   | Repudiation | non-PASS completion          | mitigate    | Require executor-decision artifact for CONCERNS/FAIL/BLOCKED before allow.                                   |
| T-03-SC   | Tampering   | npm/pip/cargo installs       | accept      | No package-manager install task exists in this plan.                                                         |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/final-review-gate.test.js` and then `node --test .claude/advisor-mode/tests/*.test.js`.
</verification>

<success_criteria>

- GATE-03 is enforced for explicit non-trivial completion state.
- D-01 requires fresh final review.
- D-02 uses explicit workflow state.
- D-03 prevents reuse of prior consultations.
- D-04 requires executor-decision follow-up for non-PASS statuses.
  </success_criteria>

<output>
Create `.planning/phases/03-verdict-handoff-and-verification-evidence/03-05-SUMMARY.md` when done.
</output>
