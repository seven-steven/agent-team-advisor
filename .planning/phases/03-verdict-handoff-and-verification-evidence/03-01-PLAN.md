---
phase: 03-verdict-handoff-and-verification-evidence
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/advisor-mode/final-review.js
  - .claude/advisor-mode/context-packet.schema.json
  - .claude/advisor-mode/tests/context-packet.test.js
  - .claude/agents/advisor-reviewer.md
autonomous: true
requirements: [SAFE-02, VERD-01]
user_setup: []
must_haves:
  truths:
    - "User can send advisors a minimized context packet containing changed files, relevant diff excerpts, relevant errors, and explicit questions instead of the full transcript by default."
    - "Advisor verdict instructions answer explicit executor questions and request additional context when the minimized packet is insufficient."
  artifacts:
    - path: ".claude/advisor-mode/context-packet.schema.json"
      provides: "strict schema for minimized advisor handoff packets"
      contains: "changed_files"
    - path: ".claude/advisor-mode/final-review.js"
      provides: "buildContextPacket and validateContextPacket exports"
      exports: ["buildContextPacket", "validateContextPacket"]
    - path: ".claude/advisor-mode/tests/context-packet.test.js"
      provides: "TDD coverage for minimized packet whitelist and transcript exclusion"
    - path: ".claude/agents/advisor-reviewer.md"
      provides: "read-only advisor instructions for explicit questions and context expansion requests"
  key_links:
    - from: ".claude/advisor-mode/final-review.js"
      to: ".claude/advisor-mode/context-packet.schema.json"
      via: "schema-backed packet validation"
      pattern: "context-packet.schema.json"
    - from: ".claude/agents/advisor-reviewer.md"
      to: "context packet explicit questions"
      via: "prompt contract"
      pattern: "explicit questions"
---

<objective>
Create the Phase 3 minimized advisor context-packet slice.

Purpose: satisfy SAFE-02 and the locked four-part handoff decisions without forwarding full transcripts by default.
Output: schema, builder/validator functions, advisor prompt contract update, and tests proving the minimized packet behavior.
</objective>

## Phase Goal

**As a** user, **I want to** make completion decisions from structured advisor verdicts, concise context packets, executor rationale, and verification evidence, **so that** I can complete guarded work with auditable confidence.

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/PROJECT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/STATE.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
@.claude/advisor-mode/verdict.schema.json
@.claude/agents/advisor-reviewer.md
@.claude/advisor-mode/tests/advisor-agent.test.js

<interfaces>
Existing patterns to preserve:
- CommonJS runtime; export pure functions through module.exports from `.claude/advisor-mode/*.js`.
- Advisor reviewer frontmatter remains `model: opus` and `tools: Read, Grep, Glob`.
- Verdict statuses remain exactly `PASS`, `CONCERNS`, `FAIL`, `BLOCKED`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED minimized context packet contract</name>
  <files>.claude/advisor-mode/tests/context-packet.test.js, .claude/advisor-mode/context-packet.schema.json, .claude/advisor-mode/final-review.js</files>
  <read_first>
    - .claude/advisor-mode/tests/advisor-agent.test.js
    - .claude/advisor-mode/verdict.schema.json
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
  </read_first>
  <behavior>
    - `buildContextPacket(input)` returns an object with `packet_type: "advisor-final-review-context"`, `changed_files`, `relevant_diff_excerpts`, `relevant_errors`, `explicit_questions`, optional `verification_summary`, and `created_at`.
    - The default packet excludes `transcript`, `full_transcript`, `raw_log`, and arbitrary extra fields per D-05, D-06, and SAFE-02.
    - `validateContextPacket(packet)` rejects missing explicit questions per D-07 and rejects extra transcript/raw-log fields.
  </behavior>
  <action>Write failing node:test coverage in `.claude/advisor-mode/tests/context-packet.test.js` before implementation. Tests must require `.claude/advisor-mode/final-review.js`, call `buildContextPacket` and `validateContextPacket`, and assert the compact four-part bundle required by D-05 through D-07. Create `.claude/advisor-mode/context-packet.schema.json` as a strict Draft 2020-12 object schema with `additionalProperties: false`, required `changed_files`, `relevant_diff_excerpts`, `relevant_errors`, and `explicit_questions`, and no transcript/raw-log fields. Do not add external dependencies.</action>
  <acceptance_criteria>
    - Source assertion: `.claude/advisor-mode/context-packet.schema.json` has `additionalProperties: false` and required keys `changed_files`, `relevant_diff_excerpts`, `relevant_errors`, `explicit_questions`.
    - Behavior assertion: RED run fails before `buildContextPacket` and `validateContextPacket` are implemented.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/context-packet.test.js` exits non-zero during RED for missing exports or failing behavior.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/context-packet.test.js</automated>
  </verify>
  <done>Failing tests express the minimized packet whitelist and transcript exclusion contract from D-05 through D-07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN context packet builder and advisor prompt contract</name>
  <files>.claude/advisor-mode/final-review.js, .claude/agents/advisor-reviewer.md, .claude/advisor-mode/tests/context-packet.test.js</files>
  <read_first>
    - .claude/advisor-mode/final-review.js
    - .claude/agents/advisor-reviewer.md
    - .claude/advisor-mode/tests/context-packet.test.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
  </read_first>
  <action>Implement `buildContextPacket(input = {})` and `validateContextPacket(packet)` in `.claude/advisor-mode/final-review.js` using CommonJS and built-in Node only. Use concrete field names `packet_type`, `correlationKey`, `task_summary`, `changed_files`, `relevant_diff_excerpts`, `relevant_errors`, `explicit_questions`, `verification_summary`, and `created_at`. Include only whitelisted arrays/objects; ignore or reject `transcript`, `full_transcript`, `raw_log`, and arbitrary extras by default per D-05, D-06, and SAFE-02. Update `.claude/agents/advisor-reviewer.md` without adding tools: instruct the read-only advisor to answer `explicit_questions` from minimized packets and, per D-08, request specific additional context when the packet is insufficient rather than assuming or asking for a full transcript.</action>
  <acceptance_criteria>
    - Behavior assertion: valid packets with changed files, diff excerpts, errors, and explicit questions validate successfully.
    - Source assertion: `.claude/agents/advisor-reviewer.md` still contains `tools: Read, Grep, Glob` and contains the phrase `explicit questions`.
    - Source assertion: `.claude/advisor-mode/final-review.js` exports `buildContextPacket` and `validateContextPacket` through `module.exports`.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/context-packet.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/context-packet.test.js</automated>
  </verify>
  <done>Minimized context packets validate, exclude full transcript/raw logs by default, and advisor instructions require explicit context-expansion requests when needed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                         |
| ------------------------------ | ------------------------------------------------------------------- |
| executor→advisor               | Executor-curated context crosses into read-only advisor review.     |
| local runtime→schema validator | Runtime packet data is accepted only after strict field validation. |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                                      |
| --------- | ---------------------- | -------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-03-01   | Information Disclosure | buildContextPacket         | mitigate    | Whitelist packet fields, reject transcript/raw-log extras, and test full transcript exclusion per D-05/D-06/SAFE-02. |
| T-03-02   | Tampering              | context-packet.schema.json | mitigate    | Use strict Draft 2020-12 schema with `additionalProperties: false` and tests for malformed packets.                  |
| T-03-SC   | Tampering              | npm/pip/cargo installs     | accept      | No package-manager install task exists in this plan.                                                                 |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/context-packet.test.js` and confirm advisor reviewer remains read-only.
</verification>

<success_criteria>

- SAFE-02 compact packet is implemented with the D-05 four-part bundle.
- D-06 excludes full files by default in favor of diff excerpts/supporting excerpts.
- D-07 explicit questions are required.
- D-08 advisor prompt requests additional context explicitly instead of silently broadening defaults.
  </success_criteria>

<output>
Create `.planning/phases/03-verdict-handoff-and-verification-evidence/03-01-SUMMARY.md` when done.
</output>
