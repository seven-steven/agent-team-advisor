---
phase: 03-verdict-handoff-and-verification-evidence
plan: 03
type: tdd
wave: 3
depends_on: [03-02]
files_modified:
  - .claude/advisor-mode/final-review.js
  - .claude/advisor-mode/disposition.schema.json
  - .claude/advisor-mode/tests/disposition.test.js
autonomous: true
requirements: [VERD-02]
user_setup: []
must_haves:
  truths:
    - "User can see whether the executor accepted, rejected, or deferred each advisor recommendation."
    - "Executor rationale is recorded in a dedicated executor-decision artifact before completion proceeds."
  artifacts:
    - path: ".claude/advisor-mode/disposition.schema.json"
      provides: "strict executor-decision artifact schema"
      contains: "executor_decisions"
    - path: ".claude/advisor-mode/final-review.js"
      provides: "recordExecutorDecision and validateExecutorDecision exports"
      exports: ["recordExecutorDecision", "validateExecutorDecision"]
    - path: ".claude/advisor-mode/tests/disposition.test.js"
      provides: "TDD coverage for per-recommendation executor decisions"
  key_links:
    - from: ".claude/advisor-mode/final-review.js"
      to: ".advisor/decisions/executor/{correlationKey}.json"
      via: "recordExecutorDecision writes separate artifact"
      pattern: "executor"
    - from: ".claude/advisor-mode/final-review.js"
      to: "advisor verdict recommended_actions"
      via: "recommendation_id references"
      pattern: "recommendation_id"
---

<objective>
Create the executor follow-up rationale slice.

Purpose: satisfy VERD-02 and D-09 through D-12 by separating executor decisions from advisor verdict artifacts and recording a disposition per recommendation.
Output: executor-decision schema, record/validate functions, audit event append behavior, and tests.
</objective>

## Phase Goal

**As a** user, **I want to** make completion decisions from structured advisor verdicts, concise context packets, executor rationale, and verification evidence, **so that** I can complete guarded work with auditable confidence.

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
@.claude/advisor-mode/final-review.js
@.claude/hooks/advisor-gate.js
@.claude/advisor-mode/tests/init.test.js

<interfaces>
Plan 03-02 creates or extends `normalizeRecommendedActions(recommended_actions)` and `validateVerdict(verdict)` in `.claude/advisor-mode/final-review.js`.
Phase 2 artifact convention: human dispositions live under `.advisor/decisions/dispositions/{correlationKey}.json`; Phase 3 executor decisions must use a distinct path `.advisor/decisions/executor/{correlationKey}.json` per D-09.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED executor-decision per recommendation contract</name>
  <files>.claude/advisor-mode/tests/disposition.test.js, .claude/advisor-mode/disposition.schema.json, .claude/advisor-mode/final-review.js</files>
  <read_first>
    - .claude/advisor-mode/final-review.js
    - .claude/advisor-mode/tests/init.test.js
    - .claude/hooks/advisor-gate.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
  </read_first>
  <behavior>
    - `recordExecutorDecision({ correlationKey, verdict, decisions }, { root })` writes `.advisor/decisions/executor/{correlationKey}.json`, not the verdict file.
    - Each decision entry has `recommendation_id`, `disposition` enum `accepted|rejected|deferred`, `rationale`, `evidence_refs`, and `decided_at` per D-10 and D-11.
    - The artifact is written immediately after final verdict processing and before completion; tests assert it exists before any completion freshness state is marked satisfied.
  </behavior>
  <action>Write failing node:test coverage in `.claude/advisor-mode/tests/disposition.test.js` using temp repo fixtures. Create `.claude/advisor-mode/disposition.schema.json` as a strict Draft 2020-12 schema for a dedicated `executor-decision` artifact with fields `artifact_type`, `correlationKey`, `verdict_ref`, `executor_decisions`, and `created_at`. Tests must assert that the advisor verdict object/file is not mutated, that every recommendation has a matching decision, and that invalid dispositions are rejected. Use `accepted`, `rejected`, and `deferred` exactly per D-10.</action>
  <acceptance_criteria>
    - Source assertion: `.claude/advisor-mode/disposition.schema.json` contains enum values `accepted`, `rejected`, and `deferred`.
    - Behavior assertion: RED run fails before `recordExecutorDecision` and `validateExecutorDecision` exist.
    - Behavior assertion: tests check `.advisor/decisions/executor/{correlationKey}.json` and not `.advisor/decisions/dispositions/{correlationKey}.json` for executor decisions.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/disposition.test.js` exits non-zero during RED.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/disposition.test.js</automated>
  </verify>
  <done>Failing tests encode dedicated executor-decision artifacts and per-recommendation disposition requirements from D-09 through D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN executor-decision recorder</name>
  <files>.claude/advisor-mode/final-review.js, .claude/advisor-mode/tests/disposition.test.js</files>
  <read_first>
    - .claude/advisor-mode/final-review.js
    - .claude/advisor-mode/disposition.schema.json
    - .claude/advisor-mode/tests/disposition.test.js
    - .claude/hooks/advisor-failure-tracker.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
  </read_first>
  <action>Implement `recordExecutorDecision(input = {}, options = {})` and `validateExecutorDecision(artifact, verdict)` in `.claude/advisor-mode/final-review.js`. Write executor artifacts to `.advisor/decisions/executor/{correlationKey}.json` with `artifact_type: "executor-decision"`, `correlationKey`, `verdict_ref`, `executor_decisions`, and `created_at`. Require one decision per normalized recommendation ID, require non-empty rationale, require `evidence_refs` array, and append a concise `.advisor/audit/events.jsonl` event named `executor.final_review_decision.recorded`. Do not mutate advisor verdict artifacts. Do not implement Phase 05 audit exploration UI.</action>
  <acceptance_criteria>
    - Behavior assertion: accepted/rejected/deferred entries with rationale, evidence refs, and timestamps validate successfully.
    - Behavior assertion: missing decision for a recommendation returns `ok: false`.
    - Behavior assertion: the original verdict file content remains byte-identical after `recordExecutorDecision`.
    - CLI assertion: `.advisor/audit/events.jsonl` gets one JSONL event with `event: "executor.final_review_decision.recorded"` in the temp fixture.
    - Test-command assertion: `node --test .claude/advisor-mode/tests/disposition.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/disposition.test.js</automated>
  </verify>
  <done>Executor follow-up decisions are separate, per-recommendation, timestamped, rationale-backed, and audit-noted before completion decisions proceed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                        |
| --------------------------------- | ---------------------------------------------------------------------------------- |
| executor→local runtime            | Executor decision artifacts become completion evidence.                            |
| advisor verdict→executor decision | Advisor recommendations are transformed into executor accept/reject/defer entries. |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                 |
| --------- | ---------------------- | -------------------------- | ----------- | ------------------------------------------------------------------------------- |
| T-03-06   | Repudiation            | recordExecutorDecision     | mitigate    | Require rationale, evidence refs, timestamps, and append concise audit event.   |
| T-03-07   | Tampering              | executor-decision artifact | mitigate    | Write a separate artifact and test advisor verdict remains unmodified per D-09. |
| T-03-08   | Information Disclosure | evidence_refs              | mitigate    | Store references only, not raw logs or full transcript content.                 |
| T-03-SC   | Tampering              | npm/pip/cargo installs     | accept      | No package-manager install task exists in this plan.                            |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/disposition.test.js`.
</verification>

<success_criteria>

- VERD-02 is satisfied by per-recommendation executor decisions.
- D-09 dedicated artifact boundary is preserved.
- D-10/D-11 fields are enforced.
- D-12 timing is represented by decision artifact existing before completion freshness is later marked satisfied.
  </success_criteria>

<output>
Create `.planning/phases/03-verdict-handoff-and-verification-evidence/03-03-SUMMARY.md` when done.
</output>
