---
phase: 03-verdict-handoff-and-verification-evidence
plan: 07
type: tdd
wave: 7
depends_on: [03-06]
files_modified:
  - .claude/hooks/advisor-final-review-gate.js
  - .claude/advisor-mode/final-review.js
  - .claude/advisor-mode/tests/final-review-gate.test.js
  - .claude/advisor-mode/tests/disposition.test.js
  - .claude/advisor-mode/tests/verdict-handoff.test.js
autonomous: true
gap_closure: true
requirements: [GATE-03, VERD-01, VERD-02]
user_setup: []
must_haves:
  truths:
    - "Stop hook completion gate blocks empty stdin, malformed stdin, and stdin timeout instead of exiting 0."
    - "Executor-decision validation rejects duplicate recommendation_id entries so exactly one disposition exists per advisor recommendation."
    - "Runtime verdict validation rejects nested extra fields and invalid created_at values that the documented schema disallows."
  artifacts:
    - path: ".claude/hooks/advisor-final-review-gate.js"
      provides: "fail-closed CLI entrypoint for Stop hook final-review enforcement"
      exports: ["evaluateFinalReviewGate"]
    - path: ".claude/advisor-mode/final-review.js"
      provides: "strict verdict and executor-decision validators"
      exports: ["validateVerdict", "validateExecutorDecision"]
    - path: ".claude/advisor-mode/tests/final-review-gate.test.js"
      provides: "regression coverage for Stop hook empty/malformed/timeout fail-closed behavior"
    - path: ".claude/advisor-mode/tests/disposition.test.js"
      provides: "regression coverage for duplicate recommendation_id rejection"
    - path: ".claude/advisor-mode/tests/verdict-handoff.test.js"
      provides: "regression coverage for verdict runtime/schema drift"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/advisor-final-review-gate.js"
      via: "Stop hook command already wired by Phase 03"
      pattern: "advisor-final-review-gate.js"
    - from: ".claude/hooks/advisor-final-review-gate.js"
      to: ".claude/advisor-mode/final-review.js"
      via: "fresh final-review and artifact validation helpers"
      pattern: "validateVerdict|validateExecutorDecision|isFinalReviewFresh"
    - from: ".claude/advisor-mode/final-review.js"
      to: ".claude/advisor-mode/verdict.schema.json"
      via: "runtime validation aligned to documented schema constraints"
      pattern: "additionalProperties|created_at"
---

<objective>
Close the Phase 03 verification gaps found in `03-VERIFICATION.md` without expanding Phase 03 scope.

Purpose: make the final-review completion gate fail closed at the process boundary, make executor follow-up artifacts internally non-contradictory, and align verdict runtime validation with the documented schema constraints needed for trusted completion decisions.
Output: targeted tests and fixes for the Stop hook CLI entrypoint, executor-decision duplicate detection, and verdict validator strictness.
</objective>

## Phase Goal

**As a** user, **I want to** make completion decisions from structured advisor verdicts, concise context packets, executor rationale, and verification evidence, **so that** I can complete guarded work with auditable confidence.

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/ROADMAP.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/REQUIREMENTS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-RESEARCH.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-PATTERNS.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-VERIFICATION.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-05-SUMMARY.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.planning/phases/03-verdict-handoff-and-verification-evidence/03-06-SUMMARY.md
@.claude/hooks/advisor-final-review-gate.js
@.claude/advisor-mode/final-review.js
@.claude/advisor-mode/verdict.schema.json
@.claude/advisor-mode/disposition.schema.json
@.claude/advisor-mode/tests/final-review-gate.test.js
@.claude/advisor-mode/tests/disposition.test.js
@.claude/advisor-mode/tests/verdict-handoff.test.js

<read_first>

- .planning/phases/03-verdict-handoff-and-verification-evidence/03-VERIFICATION.md
- .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
- .claude/hooks/advisor-final-review-gate.js
- .claude/advisor-mode/final-review.js
- .claude/advisor-mode/verdict.schema.json
- .claude/advisor-mode/disposition.schema.json
- .claude/advisor-mode/tests/final-review-gate.test.js
- .claude/advisor-mode/tests/disposition.test.js
- .claude/advisor-mode/tests/verdict-handoff.test.js
  </read_first>

<interfaces>
Phase 03 locked scope remains: fresh final review, structured verdict, executor decision artifact, minimized context packet, and verification evidence. Do not add provider routing, budget controls, rollback, audit viewer UX, or doctor/conformance flows.

Existing exports to preserve from `.claude/advisor-mode/final-review.js`:

- `validateVerdict(verdict)` returns an object with `ok`, `errors`, and `direct_completion_allowed` where direct completion is true only for `status === "PASS"` per D-04.
- `validateExecutorDecision(artifact, verdict)` validates dedicated `executor-decision` artifacts per D-09 through D-12.
- Final-review gate code consumes these validators; changing return shapes requires updating existing tests in the same task.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail closed on invalid Stop hook input</name>
  <files>.claude/hooks/advisor-final-review-gate.js, .claude/advisor-mode/tests/final-review-gate.test.js</files>
  <read_first>
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-VERIFICATION.md
    - .claude/hooks/advisor-final-review-gate.js
    - .claude/advisor-mode/tests/final-review-gate.test.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
  </read_first>
  <behavior>
    - Running `.claude/hooks/advisor-final-review-gate.js` with empty stdin exits non-zero and emits a blocking Stop-hook response.
    - Running the hook with malformed JSON exits non-zero and emits a blocking Stop-hook response.
    - If stdin does not end before the hook timeout, the process exits non-zero and emits a blocking Stop-hook response.
    - Valid low-risk or already-fresh final-review paths keep their existing allow/block semantics from Plan 03-05.
  </behavior>
  <action>Start with failing tests in `.claude/advisor-mode/tests/final-review-gate.test.js` that spawn `.claude/hooks/advisor-final-review-gate.js` as a child process for the three verification gaps: empty stdin, malformed stdin, and no stdin end before timeout. Use a short test-controlled timeout option or environment variable only if the hook already supports it or can support it without changing production semantics. Implement the CLI `main()` path so invalid or unavailable Stop payloads fail closed: write a JSON Stop-hook blocking response to stdout or a concise blocking error to stderr, then exit non-zero. The blocking message must tell the executor that a fresh advisor final review is required before completion. Keep valid Stop payload handling connected to `evaluateFinalReviewGate()` and do not change `.claude/settings.json` unless a test proves existing wiring is broken.</action>
  <acceptance_criteria>
    - Empty stdin child-process test asserts non-zero exit and blocking output.
    - Malformed stdin child-process test asserts non-zero exit and blocking output.
    - Timeout/no-end stdin child-process test asserts non-zero exit and blocking output.
    - Existing final-review freshness tests still pass.
    - No provider routing, budget, rollback, or audit viewer behavior is added.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/final-review-gate.test.js</automated>
  </verify>
  <done>GATE-03 no longer fails open at the Stop hook process boundary for empty, malformed, or delayed stdin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject duplicate executor recommendation decisions</name>
  <files>.claude/advisor-mode/final-review.js, .claude/advisor-mode/tests/disposition.test.js</files>
  <read_first>
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-VERIFICATION.md
    - .claude/advisor-mode/final-review.js
    - .claude/advisor-mode/disposition.schema.json
    - .claude/advisor-mode/tests/disposition.test.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
  </read_first>
  <behavior>
    - `validateExecutorDecision()` rejects two or more entries with the same `recommendation_id`, even when all individual entries otherwise have valid fields.
    - Duplicate entries with contradictory dispositions return `ok: false` and include an error mentioning the duplicate `recommendation_id`.
    - A valid artifact with exactly one decision per normalized recommendation remains accepted.
  </behavior>
  <action>Add a failing regression test to `.claude/advisor-mode/tests/disposition.test.js` using a verdict with a stable recommendation ID and an `executor-decision` artifact containing duplicate `recommendation_id` entries with different dispositions. Update `validateExecutorDecision()` in `.claude/advisor-mode/final-review.js` so it tracks seen IDs and records a validation error when an ID appears more than once. The validator must require exactly one decision per normalized advisor recommendation and must not allow later duplicate entries to overwrite or mask earlier ones. Preserve the dedicated executor artifact path and do not mutate advisor verdict artifacts per D-09.</action>
  <acceptance_criteria>
    - Duplicate `recommendation_id` regression test fails before the validator change and passes after it.
    - Validation result for duplicates is `ok: false` and includes a duplicate-ID error.
    - Existing missing-decision, invalid-disposition, rationale, evidence_refs, and audit event tests still pass.
    - `recordExecutorDecision()` continues writing `.advisor/decisions/executor/{correlationKey}.json` only.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/disposition.test.js</automated>
  </verify>
  <done>VERD-02 executor rationale artifacts cannot validate with contradictory duplicate dispositions for the same advisor recommendation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align verdict runtime validation with schema strictness</name>
  <files>.claude/advisor-mode/final-review.js, .claude/advisor-mode/tests/verdict-handoff.test.js</files>
  <read_first>
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-VERIFICATION.md
    - .claude/advisor-mode/final-review.js
    - .claude/advisor-mode/verdict.schema.json
    - .claude/advisor-mode/tests/verdict-handoff.test.js
    - .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
  </read_first>
  <behavior>
    - `validateVerdict()` rejects verdicts with extra top-level fields or extra nested fields inside documented nested objects/arrays such as recommended actions and validation checklist entries.
    - `validateVerdict()` rejects invalid `created_at` values and accepts ISO 8601 date-time strings.
    - `validateVerdict()` keeps `direct_completion_allowed: true` only for valid `PASS` verdicts and never for invalid PASS-shaped payloads.
  </behavior>
  <action>Add failing tests to `.claude/advisor-mode/tests/verdict-handoff.test.js` for the verification warning: a verdict with an extra nested field under `recommended_actions` must return `ok: false`, and a verdict with an invalid `created_at` string must return `ok: false`. Tighten `validateVerdict()` in `.claude/advisor-mode/final-review.js` so runtime validation matches the documented strict schema at least for top-level additional properties, nested recommended action fields, nested validation checklist fields, required field types, and `created_at` date-time format. Use the existing CommonJS/no-new-dependency pattern. Preserve `PASS`, `CONCERNS`, `FAIL`, and `BLOCKED` semantics from D-04.</action>
  <acceptance_criteria>
    - Extra nested field under a recommended action returns `ok: false`.
    - Extra nested field under a validation checklist entry returns `ok: false` if checklist entries are object-shaped in the current schema.
    - Invalid `created_at` returns `ok: false`; a valid ISO 8601 timestamp returns `ok: true` when all other fields are valid.
    - Invalid PASS-shaped verdicts return `direct_completion_allowed: false` or omit direct allow through `ok: false` handling; they must not satisfy completion.
    - Existing PASS and non-PASS verdict tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/verdict-handoff.test.js</automated>
  </verify>
  <done>VERD-01 runtime verdict validation no longer accepts schema-invalid nested extras or invalid final-review timestamps.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                                               |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| Claude Code Stop event→local hook process             | Stop-hook stdin controls whether completion can proceed. Invalid or missing payloads are untrusted input. |
| advisor verdict artifact→executor completion decision | Advisor-produced JSON controls completion semantics and must match the documented schema.                 |
| executor decision artifact→final-review gate          | Executor-produced dispositions authorize non-PASS completion follow-up and must be internally consistent. |

## STRIDE Threat Register

| Threat ID   | Category    | Component                                 | Disposition | Mitigation Plan                                                                                         |
| ----------- | ----------- | ----------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------- |
| T-03-GAP-01 | Tampering   | advisor-final-review-gate.js stdin parser | mitigate    | Empty, malformed, and timed-out Stop payloads emit a blocking response and exit non-zero.               |
| T-03-GAP-02 | Repudiation | validateExecutorDecision                  | mitigate    | Reject duplicate `recommendation_id` entries so one recommendation has one executor disposition.        |
| T-03-GAP-03 | Tampering   | validateVerdict                           | mitigate    | Enforce documented strict nested fields and ISO `created_at` before allowing PASS completion semantics. |
| T-03-SC     | Tampering   | npm/pip/cargo installs                    | accept      | No package-manager install task exists in this plan.                                                    |

</threat_model>

<verification>
Run the targeted gap tests first, then the full advisor-mode regression:
- `node --test .claude/advisor-mode/tests/final-review-gate.test.js`
- `node --test .claude/advisor-mode/tests/disposition.test.js`
- `node --test .claude/advisor-mode/tests/verdict-handoff.test.js`
- `node --test .claude/advisor-mode/tests/*.test.js`
</verification>

<success_criteria>

- `03-VERIFICATION.md` blocker 1 is closed: Stop hook fails closed for empty stdin, malformed stdin, and stdin timeout.
- `03-VERIFICATION.md` blocker 2 is closed: duplicate `recommendation_id` executor decisions are rejected.
- `03-VERIFICATION.md` warning is addressed: `validateVerdict()` rejects extra nested fields and invalid `created_at` values.
- Phase 03 scope remains limited to final review, structured verdicts, executor decisions, minimized context packets, and verification evidence.
  </success_criteria>

<output>
Create `.planning/phases/03-verdict-handoff-and-verification-evidence/03-07-SUMMARY.md` when done.
</output>
