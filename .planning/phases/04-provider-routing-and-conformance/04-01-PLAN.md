---
phase: 04-provider-routing-and-conformance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/advisor-mode/provider-routes.example.json
  - .claude/advisor-mode/provider-routes.schema.json
  - .claude/advisor-mode/provider-routing.js
  - .claude/advisor-mode/tests/provider-routing.test.js
  - .claude/hooks/advisor-gate.js
  - .claude/hooks/executor-route-audit.js
  - .claude/settings.json
autonomous: false
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-04
user_setup: []
must_haves:
  truths:
    - "Project maintainer can define semantic alias routes for sonnet, opus, and haiku without editing advisor gates, verdict code, or agent prompts."
    - "Project maintainer can configure sonnet as the GLM executor route and opus as the GPT-5.5 advisor route through declarative route data."
    - "Operator can inspect requested alias plus resolved provider/model metadata from route resolution artifacts without exposing credential values."
    - "Existing advisor consultation runtime requests include resolved advisor route metadata by default, so ROUT-02 advisor-call visibility is in the real audit chain rather than only helper-only artifacts."
    - "Executor runtime turns emit sanitized resolved executor route metadata for the `sonnet` alias into `.advisor/runtime/executor-calls/*.json` and `.advisor/audit/events.jsonl`, so ROUT-02 executor-call visibility is covered by a real Claude Code hook surface rather than conformance artifacts."
  artifacts:
    - path: ".claude/advisor-mode/provider-routes.schema.json"
      provides: "Strict route config contract for semantic alias to provider/model mapping"
      contains: "schemaVersion"
    - path: ".claude/advisor-mode/provider-routes.example.json"
      provides: "Declarative reference routes for sonnet, opus, and haiku"
      contains: "sonnet"
    - path: ".claude/advisor-mode/provider-routing.js"
      provides: "Pure route validation, alias resolution, and audit-safe metadata helpers"
      exports:
        [
          "validateRouteConfig",
          "loadRouteConfig",
          "resolveRoute",
          "buildResolvedRouteAuditEvent",
          "writeResolvedRouteArtifact",
        ]
    - path: ".claude/advisor-mode/tests/provider-routing.test.js"
      provides: "Nyquist tests for route schema, resolver behavior, protected surfaces, advisor request wiring, executor hook audit wiring, and audit-safe metadata"
    - path: ".claude/hooks/advisor-gate.js"
      provides: "Existing advisor consultation runtime chain enriched with default resolved advisor route metadata"
      exports:
        ["evaluateGatePolicy", "buildRuntimeRouteMetadata", "buildGateEvent"]
    - path: ".claude/hooks/executor-route-audit.js"
      provides: "Claude Code executor runtime hook that records resolved `sonnet` provider/model metadata for executor turns"
      exports: ["buildExecutorRouteAuditEvent", "recordExecutorRouteResolution"]
    - path: ".claude/settings.json"
      provides: "Project-local hook registration that invokes executor route audit on the actual executor runtime lifecycle surface"
      contains: "executor-route-audit.js"
  key_links:
    - from: ".claude/advisor-mode/provider-routing.js"
      to: ".claude/advisor-mode/provider-routes.schema.json"
      via: "schema-driven validation"
      pattern: "provider-routes.schema.json"
    - from: ".claude/advisor-mode/provider-routing.js"
      to: ".advisor/audit/events.jsonl"
      via: "resolved route audit event helper"
      pattern: "provider_route.resolved"
    - from: ".claude/hooks/advisor-gate.js"
      to: ".claude/advisor-mode/provider-routing.js"
      via: "buildRuntimeRouteMetadata() loading route config and resolving alias `opus` for advisor consultation requests"
      pattern: "buildRuntimeRouteMetadata|resolveRoute"
    - from: ".claude/hooks/advisor-gate.js"
      to: ".advisor/consultations/requests/*.json"
      via: "buildRequest()/buildDecisionPacket() default routeResolution payload"
      pattern: "routeResolution"
    - from: ".claude/hooks/executor-route-audit.js"
      to: ".claude/advisor-mode/provider-routing.js"
      via: "recordExecutorRouteResolution() resolving alias `sonnet` for executor runtime hook events"
      pattern: "resolveRoute"
    - from: ".claude/hooks/executor-route-audit.js"
      to: ".advisor/runtime/executor-calls/*.json"
      via: "per-executor-turn sanitized runtime artifact write"
      pattern: "provider_route.executor_call"
    - from: ".claude/hooks/executor-route-audit.js"
      to: ".advisor/audit/events.jsonl"
      via: "executor route audit JSONL append"
      pattern: "provider_route.executor_call"
    - from: ".claude/settings.json"
      to: ".claude/hooks/executor-route-audit.js"
      via: "Claude Code hook registration for executor runtime turns"
      pattern: "executor-route-audit.js"
    - from: ".claude/advisor-mode/tests/provider-routing.test.js"
      to: ".claude/advisor-mode/policy.example.json"
      via: "provider-routes protected surface classification"
      pattern: "provider-routes"
---

<objective>
Create the thin provider-route vertical slice: failing tests first, strict declarative route config, pure alias resolver, audit-safe resolved provider/model artifacts, real runtime request wiring for advisor consultation audit visibility, and real executor runtime hook audit visibility.

Purpose: Satisfy ROUT-01, ROUT-02, and ROUT-04 while preserving D-01/D-02 thin-routing boundaries and D-05/D-06 declarative route defaults.
Output: Provider route schema, reference route example, route resolver utility, route tests, advisor-gate request enrichment, and executor runtime route audit hook registration for resolved route metadata.
</objective>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-provider-routing-and-conformance/04-CONTEXT.md
@.planning/phases/04-provider-routing-and-conformance/04-RESEARCH.md
@.planning/phases/04-provider-routing-and-conformance/04-PATTERNS.md
@.planning/phases/04-provider-routing-and-conformance/04-VALIDATION.md
@.claude/advisor-mode/policy.example.json
@.claude/advisor-mode/gate-event.schema.json
@.claude/advisor-mode/final-review.js
@.claude/advisor-mode/tests/protected-surface.test.js
@.claude/hooks/advisor-gate.js
@.claude/settings.json

<interfaces>
Create these CommonJS exports in `.claude/advisor-mode/provider-routing.js` for downstream plans:
- `validateRouteConfig(config): { ok: boolean, errors: string[] }`
- `loadRouteConfig(filePath, options?): { ok: boolean, config?: object, errors: string[] }`
- `resolveRoute(config, alias, options?): { ok: boolean, alias: string, provider?: string, model?: string, endpointRef?: string, credentialEnv?: string, requiredConformance?: string[], metadata?: object, reasonCode?: string, errors?: string[] }`
- `buildResolvedRouteAuditEvent(resolution, input?): object`
- `writeResolvedRouteArtifact(resolution, options?): object`
Resolved audit output must use `requestedAlias`, `resolvedProvider`, `resolvedModel`, `endpointRef`, `routeConfigPath`, `conformanceStatus`, `recordedAt`, and `event: "provider_route.resolved"`.

Extend `.claude/hooks/advisor-gate.js` without moving gate decisions into the route layer:

- `buildRuntimeRouteMetadata(input?, options?): object` loads `.claude/advisor-mode/provider-routes.json` when present, otherwise `.claude/advisor-mode/provider-routes.example.json`, resolves `input.requestedAlias || "opus"`, and returns sanitized `requestedAlias`, `resolvedProvider`, `resolvedModel`, `endpointRef`, `routeConfigPath`, and `conformanceStatus`.
- `buildRequest()` and `buildDecisionPacket()` include this object as `routeResolution` on advisor consultation request artifacts by default. They must not include credential values, headers, request bodies, or bearer tokens.

Create `.claude/hooks/executor-route-audit.js` as a metadata-only runtime hook surface for executor calls:

- `buildExecutorRouteAuditEvent(input?, options?): object` loads the same route config, resolves `input.requestedAlias || "sonnet"`, and returns sanitized `requestedAlias`, `resolvedProvider`, `resolvedModel`, `endpointRef`, `routeConfigPath`, `conformanceStatus`, `event: "provider_route.executor_call"`, and a non-secret runtime correlation id when available from hook input.
- `recordExecutorRouteResolution(input?, options?): object` writes a per-turn artifact under `.advisor/runtime/executor-calls/` and appends `.advisor/audit/events.jsonl` without credential values, headers, prompts, responses, request bodies, or bearer tokens.
- Register the hook in `.claude/settings.json` on the Claude Code lifecycle surface that represents executor runtime turns in this repo, so ROUT-02 executor visibility is tied to a real runtime hook invocation rather than provider conformance artifacts.
  </interfaces>
  </context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify provider IDs and route distribution mechanics</name>
  <read_first>
    - `.planning/phases/04-provider-routing-and-conformance/04-CONTEXT.md`
    - `.planning/phases/04-provider-routing-and-conformance/04-RESEARCH.md`
    - `.claude/settings.json`
    - `.claude/advisor-mode/policy.example.json`
  </read_first>
  <acceptance_criteria>
    - Exact provider/model identifiers for the reference `sonnet` executor GLM route are confirmed from current provider documentation or operator configuration.
    - Exact provider/model identifiers for the reference `opus` advisor GPT-5.5 route are confirmed from current provider documentation or operator configuration.
    - The implementer records whether Phase 4 route defaults are delivered as plugin/skill-owned assets or repo-local `.claude/advisor-mode/` assets, with repo-local fallback allowed per D-04 when no supported plugin/skill mechanism exists in this repo.
    - No API key, bearer token, or credential value is written into any repository file.
  </acceptance_criteria>
  <action>Before locking the route example values, verify current provider docs or operator-provided gateway config for the concrete GLM executor and GPT-5.5 advisor model identifiers per D-06. Use plugin/skill-owned route assets if a supported mechanism exists in this repo per D-03; otherwise implement repo-local `.claude/advisor-mode/provider-routes.example.json` as the auditable fallback per D-04. Record only provider IDs, model IDs, `endpointRef`, and `credentialEnv` names.</action>
  <verify>
    <human-check>Confirm the exact provider/model identifiers and distribution choice before Task 3 edits route defaults.</human-check>
  </verify>
  <done>Provider/model IDs and distribution mechanics are confirmed, and implementation can proceed without storing secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing route schema, resolver, advisor runtime, and executor runtime audit tests</name>
  <read_first>
    - `.planning/phases/04-provider-routing-and-conformance/04-VALIDATION.md`
    - `.planning/phases/04-provider-routing-and-conformance/04-PATTERNS.md`
    - `.claude/advisor-mode/tests/protected-surface.test.js`
    - `.claude/advisor-mode/policy.example.json`
    - `.claude/hooks/advisor-gate.js`
    - `.claude/settings.json`
  </read_first>
  <files>.claude/advisor-mode/tests/provider-routing.test.js</files>
  <behavior>
    - Test 1: `provider-routes.example.json` contains `schemaVersion: 1` and semantic aliases `sonnet`, `opus`, and `haiku`.
    - Test 2: `validateRouteConfig()` rejects unknown top-level fields, missing provider/model, unsupported conformance names, and credential fields containing literal secret-looking values.
    - Test 3: `resolveRoute()` maps aliases to provider/model/endpoint metadata and returns `missing-route` without fallback when an alias is absent.
    - Test 4: reference `sonnet` and `opus` route data are declarative and no workflow module needs concrete GLM/GPT-5.5 identifiers.
    - Test 5: `buildResolvedRouteAuditEvent()` includes requested alias plus resolved provider/model and excludes headers, tokens, request bodies, and credential values.
    - Test 6: existing `provider-routes` protected surface classification covers `.claude/advisor-mode/provider-routes.example.json` and `.claude/advisor-mode/provider-routes.json`.
    - Test 7: `advisor-gate.js` `buildRuntimeRouteMetadata()` resolves advisor alias `opus` through provider routes and returns `requestedAlias`, `resolvedProvider`, `resolvedModel`, `endpointRef`, `routeConfigPath`, and `conformanceStatus` without secrets.
    - Test 8: `evaluateGatePolicy()` writes or returns advisor consultation request data whose real runtime request surface includes `routeResolution` by default for the advisor call path.
    - Test 9: `.claude/hooks/executor-route-audit.js` `recordExecutorRouteResolution()` resolves executor alias `sonnet` through provider routes and writes a sanitized `.advisor/runtime/executor-calls/*.json` artifact plus `provider_route.executor_call` JSONL audit event without prompts, responses, headers, bodies, tokens, or credential values.
    - Test 10: `.claude/settings.json` registers `executor-route-audit.js` on the actual Claude Code executor runtime hook surface used by this repo, proving executor route visibility is wired to runtime execution rather than conformance-only artifacts.
  </behavior>
  <acceptance_criteria>
    - `node --test .claude/advisor-mode/tests/provider-routing.test.js` fails because `.claude/advisor-mode/provider-routing.js`, route schema, route example, `advisor-gate.js` runtime wiring, executor audit hook, and settings hook registration do not yet implement the expected behavior.
    - Test names explicitly mention ROUT-01, ROUT-02, or ROUT-04.
    - Tests use Node built-in `node:test` and `node:assert/strict`; no package install is introduced.
  </acceptance_criteria>
  <action>Create `.claude/advisor-mode/tests/provider-routing.test.js` using the existing Node test style from `protected-surface.test.js`. Require `../provider-routing.js`, `../../hooks/advisor-gate.js`, and `../../hooks/executor-route-audit.js`; load `.claude/advisor-mode/provider-routes.example.json`, `.claude/advisor-mode/policy.example.json`, and `.claude/settings.json`; use temp roots for artifact/request writes. Define tests for strict route validation, alias resolution, no silent fallback, reference route declarativity per D-05/D-06, audit-safe resolved metadata per D-09/D-10, protected-surface coverage, real advisor consultation request wiring for ROUT-02, and real executor runtime hook audit wiring for ROUT-02. Keep assertions deterministic and filesystem-local.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/provider-routing.test.js</automated>
  </verify>
  <done>Failing route tests exist and encode the Phase 4 route/audit/runtime request contract before implementation, including executor-call provider/model visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement route schema, reference config, resolver, advisor metadata wiring, and executor runtime audit hook</name>
  <read_first>
    - `.claude/advisor-mode/tests/provider-routing.test.js`
    - `.claude/advisor-mode/gate-event.schema.json`
    - `.claude/advisor-mode/final-review.js`
    - `.claude/advisor-mode/policy.example.json`
    - `.claude/hooks/advisor-gate.js`
    - `.claude/settings.json`
    - `.planning/phases/04-provider-routing-and-conformance/04-CONTEXT.md`
  </read_first>
  <files>.claude/advisor-mode/provider-routes.example.json, .claude/advisor-mode/provider-routes.schema.json, .claude/advisor-mode/provider-routing.js, .claude/hooks/advisor-gate.js, .claude/hooks/executor-route-audit.js, .claude/settings.json</files>
  <acceptance_criteria>
    - `provider-routes.schema.json` is draft 2020-12 JSON Schema with `additionalProperties: false`, required `schemaVersion`, and route objects requiring `provider`, `model`, `endpointRef`, and `requiredConformance`.
    - `provider-routes.example.json` defines `sonnet`, `opus`, and `haiku`; `sonnet` uses the verified GLM executor target and `opus` uses the verified GPT-5.5 advisor target from Task 1.
    - `provider-routing.js` exports `validateRouteConfig`, `loadRouteConfig`, `resolveRoute`, `buildResolvedRouteAuditEvent`, and `writeResolvedRouteArtifact`.
    - Missing aliases return a failed result with `reasonCode: "missing-route"`; no fallback provider/model is selected silently.
    - Audit/artifact helpers write only alias/provider/model/endpoint/conformance metadata and reject or omit credential values, headers, prompts, responses, request bodies, and token-like fields.
    - `advisor-gate.js` exports `buildRuntimeRouteMetadata`, resolves the advisor route alias `opus` for the real consultation request path, and includes `routeResolution` in advisor consultation request artifacts by default.
    - `.claude/hooks/executor-route-audit.js` resolves the executor route alias `sonnet` for the actual executor runtime hook path, writes `.advisor/runtime/executor-calls/*.json`, and appends `.advisor/audit/events.jsonl` with `event: "provider_route.executor_call"`.
    - `.claude/settings.json` registers `.claude/hooks/executor-route-audit.js` on the Claude Code hook lifecycle surface used for executor runtime turns in this repo.
    - Runtime route metadata failures are recorded as explicit `routeResolution.ok: false` or executor audit `ok: false` metadata and do not silently invent provider/model values.
    - `node --test .claude/advisor-mode/tests/provider-routing.test.js` passes.
  </acceptance_criteria>
  <action>Implement the thin route layer per D-01/D-02 and wire it into both real runtime visibility paths required by ROUT-02/D-09. Create a strict schema and a declarative route example whose concrete IDs come from Task 1, while workflow code continues to consume `sonnet`, `opus`, and `haiku` aliases. Implement CommonJS resolver functions in `.claude/advisor-mode/provider-routing.js` using manual validation patterns from `final-review.js`; include allowed conformance names `base-message`, `streaming`, `tool-use`, `usage-fields`, and `error-shape`. Add `writeResolvedRouteArtifact()` to write newline-terminated JSON under `.advisor/state/provider-route.json` by default and append JSONL audit events only when explicitly requested through options. Extend `.claude/hooks/advisor-gate.js` with `buildRuntimeRouteMetadata()` and call it from `buildRequest()` and `buildDecisionPacket()` so advisor consultation request artifacts carry `routeResolution` by default. Add `.claude/hooks/executor-route-audit.js` and register it in `.claude/settings.json` so actual executor runtime hook invocations resolve `sonnet` and record sanitized provider/model metadata in `.advisor/runtime/executor-calls/` plus `.advisor/audit/events.jsonl`. Keep this as metadata enrichment only; do not add gate, verdict, budget, fallback, or workflow-control decisions to the route layer.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/provider-routing.test.js</automated>
  </verify>
  <done>Semantic aliases resolve to declarative provider/model targets, reference routes are configurable without workflow coupling, and real advisor plus executor runtime audit surfaces expose resolved provider/model metadata without secrets.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                    |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| maintainer route config -> route resolver       | Repo/plugin-owned route data becomes trusted provider/model metadata.                          |
| route resolver -> `.advisor/` artifacts         | Runtime metadata is persisted for operator inspection.                                         |
| route resolver -> advisor consultation requests | Existing runtime request artifacts receive sanitized resolved route metadata by default.       |
| route resolver -> executor runtime audit hook   | Executor runtime hook events receive sanitized resolved route metadata by default.             |
| route config -> existing protected-surface gate | Governance-sensitive route file changes must remain under existing advisor/human review chain. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                                                                                                                    | Disposition | Mitigation Plan                                                                                                                                                               |
| --------- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Tampering / Elevation of Privilege | `.claude/advisor-mode/provider-routes.example.json`, `.claude/advisor-mode/provider-routes.json`                             | mitigate    | Keep route files covered by existing `provider-routes` protected surface tests and require strict route schema validation before resolution.                                  |
| T-04-02   | Information Disclosure             | `provider-routing.js` audit/artifact helpers, `advisor-gate.js` routeResolution payload, `executor-route-audit.js` artifacts | mitigate    | Store only `requestedAlias`, `resolvedProvider`, `resolvedModel`, `endpointRef`, route path, and conformance status; reject or omit prompt/response/token/header/body fields. |
| T-04-03   | Repudiation / Tampering            | `resolveRoute()`, `buildRuntimeRouteMetadata()`, and `recordExecutorRouteResolution()`                                       | mitigate    | Return `missing-route`/`ok: false` on absent aliases and never silently fallback to another provider/model for advisor-critical or executor route visibility aliases.         |
| T-04-SC   | Tampering                          | npm/pip/cargo installs                                                                                                       | accept      | No external packages are installed in this plan; keep package install count at zero.                                                                                          |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/provider-routing.test.js` after Task 2 to see RED and after Task 3 to see GREEN. Run `node --test .claude/advisor-mode/tests/*.test.js` before marking the plan complete.
</verification>

<success_criteria>

- ROUT-01: `sonnet`, `opus`, and `haiku` map through declarative provider route config.
- ROUT-02: resolved route artifact/audit helpers, the real advisor consultation request chain, and the real executor runtime hook audit surface expose concrete provider/model without secrets.
- ROUT-04: GLM executor and GPT-5.5 advisor reference routes are configurable without changing workflow logic.
- D-01/D-02: route code does not contain advisor gate decisions, verdict handling, final-review policy, budget, or rollback logic.
  </success_criteria>

<source_audit>
GOAL: Maintainers can map Claude semantic aliases to concrete third-party models and prove gateway safety — covered by this plan for routing and real runtime/audit metadata.
REQ: ROUT-01, ROUT-02, ROUT-04 — covered, including executor-call and advisor-call route visibility.
RESEARCH: thin resolver, strict schema, protected route governance, no silent fallback, no secret logging, real runtime/audit chain visibility — covered.
CONTEXT: D-01, D-02, D-03, D-04, D-05, D-06, D-09, D-10 — covered or checkpointed.
Deferred Ideas: none.
</source_audit>

<output>
Create `.planning/phases/04-provider-routing-and-conformance/04-01-SUMMARY.md` when done.
</output>
