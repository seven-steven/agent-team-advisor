---
phase: 04-provider-routing-and-conformance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/advisor-mode/provider-routes.example.json
  - .claude/advisor-mode/provider-routes.schema.json
  - .claude/advisor-mode/provider-routing.js
  - .claude/advisor-mode/tests/provider-routing.test.js
  - .claude/hooks/advisor-gate.js
  - .claude/hooks/executor-route-audit.js
  - .claude/settings.json
autonomous: false
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-04
user_setup: []
must_haves:
  truths:
    - "Project maintainer can define semantic alias routes for sonnet, opus, and haiku without editing advisor gates, verdict code, or agent prompts."
    - "Project maintainer can configure sonnet as the GLM executor route and opus as the GPT-5.5 advisor route through declarative route data."
    - "Runtime artifacts clearly distinguish configured route metadata from actually served provider/model observations."
    - "Existing advisor consultation request/audit surfaces carry configured route metadata and a served-route observation field that is populated only from provider response metadata, otherwise explicitly marked not observed."
    - "Executor runtime audit artifacts carry configured route metadata and a served-route observation field that is populated only from provider response metadata, otherwise explicitly marked not observed."
  artifacts:
    - path: ".claude/advisor-mode/provider-routes.schema.json"
      provides: "Strict route config contract for semantic alias to provider/model mapping"
      contains: "schemaVersion"
    - path: ".claude/advisor-mode/provider-routes.example.json"
      provides: "Declarative reference routes for sonnet, opus, and haiku"
      contains: "sonnet"
    - path: ".claude/advisor-mode/provider-routing.js"
      provides: "Pure route validation, alias resolution, configured-route metadata helpers, and served-route observation normalization"
      exports:
        [
          "validateRouteConfig",
          "loadRouteConfig",
          "resolveRoute",
          "buildConfiguredRouteMetadata",
          "buildServedRouteObservation",
          "buildResolvedRouteAuditEvent",
          "writeResolvedRouteArtifact",
        ]
    - path: ".claude/advisor-mode/tests/provider-routing.test.js"
      provides: "Nyquist tests for route schema, resolver behavior, configured-vs-served separation, protected surfaces, advisor request wiring, executor hook audit wiring, and audit-safe metadata"
    - path: ".claude/hooks/advisor-gate.js"
      provides: "Existing advisor consultation runtime chain enriched with configured route metadata and served-route observation fields that cannot be populated from config alone"
      exports:
        ["evaluateGatePolicy", "buildRuntimeRouteMetadata", "buildGateEvent"]
    - path: ".claude/hooks/executor-route-audit.js"
      provides: "Claude Code executor runtime hook that records configured executor route metadata and served-route observation fields when response data is supplied"
      exports: ["buildExecutorRouteAuditEvent", "recordExecutorRouteResolution"]
    - path: ".claude/settings.json"
      provides: "Project-local hook registration that invokes executor route audit on the actual executor runtime lifecycle surface"
      contains: "executor-route-audit.js"
  key_links:
    - from: ".claude/advisor-mode/provider-routing.js"
      to: ".claude/advisor-mode/provider-routes.schema.json"
      via: "schema-driven validation"
      pattern: "provider-routes.schema.json"
    - from: ".claude/hooks/advisor-gate.js"
      to: ".claude/advisor-mode/provider-routing.js"
      via: "buildRuntimeRouteMetadata() using configuredRoute plus empty servedRouteObservation unless response data is supplied"
      pattern: "configuredRoute|servedRouteObservation"
    - from: ".claude/hooks/advisor-gate.js"
      to: ".advisor/consultations/requests/*.json"
      via: "advisor consultation request artifact route metadata"
      pattern: "configuredRoute"
    - from: ".claude/hooks/executor-route-audit.js"
      to: ".claude/advisor-mode/provider-routing.js"
      via: "recordExecutorRouteResolution() resolving alias sonnet and normalizing served response metadata when present"
      pattern: "buildServedRouteObservation|configuredRoute"
    - from: ".claude/hooks/executor-route-audit.js"
      to: ".advisor/runtime/executor-calls/*.json"
      via: "per-executor-turn sanitized runtime artifact write"
      pattern: "provider_route.executor_call"
    - from: ".claude/settings.json"
      to: ".claude/hooks/executor-route-audit.js"
      via: "Claude Code hook registration for executor runtime turns"
      pattern: "executor-route-audit.js"
---

<objective>
Revise the thin provider-route vertical slice to make ROUT-02 precise: configured route metadata is useful but is not treated as proof of the provider/model that actually served a call.

Purpose: Preserve ROUT-01/ROUT-04 routing behavior while adding an explicit ROUT-02 contract that separates configured route from served-route observation and marks missing provider response evidence as not observed rather than inferring it from static config.
Output: Provider route schema, route example, resolver utility, routing tests, advisor-gate request enrichment, and executor route audit hook with configured-vs-served fields.
</objective>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-provider-routing-and-conformance/04-CONTEXT.md
@.planning/phases/04-provider-routing-and-conformance/04-RESEARCH.md
@.planning/phases/04-provider-routing-and-conformance/04-PATTERNS.md
@.planning/phases/04-provider-routing-and-conformance/04-VERIFICATION.md
@.claude/advisor-mode/policy.example.json
@.claude/advisor-mode/gate-event.schema.json
@.claude/advisor-mode/final-review.js
@.claude/advisor-mode/tests/protected-surface.test.js
@.claude/hooks/advisor-gate.js
@.claude/settings.json

<interfaces>
Create or revise these CommonJS exports in `.claude/advisor-mode/provider-routing.js`:
- `validateRouteConfig(config): { ok: boolean, errors: string[] }`
- `loadRouteConfig(filePath, options?): { ok: boolean, config?: object, errors: string[] }`
- `resolveRoute(config, alias, options?): { ok: boolean, alias: string, provider?: string, model?: string, endpointRef?: string, credentialEnv?: string, requiredConformance?: string[], metadata?: object, reasonCode?: string, errors?: string[] }`
- `buildConfiguredRouteMetadata(resolution, input?): { ok: boolean, requestedAlias: string, configuredProvider?: string, configuredModel?: string, endpointRef?: string, routeConfigPath?: string, conformanceStatus?: string, reasonCode?: string }`
- `buildServedRouteObservation(responseOrEvent, configuredRoute?, options?): { observed: boolean, observedProvider?: string, observedModel?: string, rawModelField?: string, responseId?: string, observationSource: "provider-response"|"gateway-header"|"hook-input"|"not-observed", observedAt: string, confidence: "provider-reported"|"gateway-reported"|"not-observed", mismatch?: boolean }`
- `buildResolvedRouteAuditEvent(resolution, input?): object` must emit `configuredRoute` and `servedRouteObservation` as separate objects. Static route config may populate only `configuredRoute`; it must never populate `observedProvider` or `observedModel`.
- `writeResolvedRouteArtifact(resolution, options?): object`

Extend `.claude/hooks/advisor-gate.js` without moving gate decisions into the route layer:

- `buildRuntimeRouteMetadata(input?, options?): object` loads `.claude/advisor-mode/provider-routes.json` when present, otherwise `.claude/advisor-mode/provider-routes.example.json`, resolves `input.requestedAlias || "opus"`, and returns `{ configuredRoute, servedRouteObservation }`.
- `servedRouteObservation.observed` is `false` and `observationSource` is `not-observed` unless the caller supplies explicit provider response metadata. Do not infer actual served model from static route config.
- `buildRequest()` and `buildDecisionPacket()` include this object as `routeResolution` on advisor consultation request artifacts by default.

Create or revise `.claude/hooks/executor-route-audit.js`:

- `buildExecutorRouteAuditEvent(input?, options?): object` loads the same route config, resolves `input.requestedAlias || "sonnet"`, returns `configuredRoute` plus `servedRouteObservation`, and uses provider response metadata only when present in hook input.
- `recordExecutorRouteResolution(input?, options?): object` writes `.advisor/runtime/executor-calls/*.json` and appends `.advisor/audit/events.jsonl` with separate `configuredRoute` and `servedRouteObservation` fields.
  </interfaces>
  </context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify provider IDs and route distribution mechanics</name>
  <read_first>
    - `.planning/phases/04-provider-routing-and-conformance/04-CONTEXT.md`
    - `.planning/phases/04-provider-routing-and-conformance/04-RESEARCH.md`
    - `.planning/phases/04-provider-routing-and-conformance/04-VERIFICATION.md`
    - `.claude/settings.json`
    - `.claude/advisor-mode/policy.example.json`
  </read_first>
  <acceptance_criteria>
    - Exact provider/model identifiers for the reference `sonnet` executor GLM route are confirmed from current provider documentation or operator configuration.
    - Exact provider/model identifiers for the reference `opus` advisor GPT-5.5 route are confirmed from current provider documentation or operator configuration.
    - The implementer records whether Phase 4 route defaults are delivered as plugin/skill-owned assets or repo-local `.claude/advisor-mode/` assets, with repo-local fallback allowed per D-04 when no supported plugin/skill mechanism exists in this repo.
    - No API key, bearer token, or credential value is written into any repository file.
    - The implementer records that configured route metadata is not actual served-route proof; served provider/model fields require provider response metadata.
  </acceptance_criteria>
  <action>Before locking route example values, verify current provider docs or operator-provided gateway config for the concrete GLM executor and GPT-5.5 advisor model identifiers per D-06. Preserve the thin-routing boundary per D-01/D-02. Use plugin/skill-owned route assets if a supported mechanism exists per D-03; otherwise implement repo-local `.claude/advisor-mode/provider-routes.example.json` as the auditable fallback per D-04. Record only provider IDs, model IDs, `endpointRef`, and `credentialEnv` names. Treat `configuredProvider/configuredModel` as configured route metadata only; do not label them as `observedProvider/observedModel` unless actual provider response metadata is available.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/provider-routing.test.js || true</automated>
    <human-check>Confirm exact provider/model identifiers, distribution choice, and configured-vs-served terminology before Task 3 edits route defaults.</human-check>
  </verify>
  <done>Provider/model IDs, distribution mechanics, and configured-vs-served terminology are confirmed without storing secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing route schema, configured-vs-served, advisor runtime, and executor runtime audit tests</name>
  <read_first>
    - `.planning/phases/04-provider-routing-and-conformance/04-VERIFICATION.md`
    - `.planning/phases/04-provider-routing-and-conformance/04-PATTERNS.md`
    - `.claude/advisor-mode/tests/protected-surface.test.js`
    - `.claude/advisor-mode/policy.example.json`
    - `.claude/hooks/advisor-gate.js`
    - `.claude/settings.json`
  </read_first>
  <files>.claude/advisor-mode/tests/provider-routing.test.js</files>
  <behavior>
    - Test 1: `provider-routes.example.json` contains `schemaVersion: 1` and semantic aliases `sonnet`, `opus`, and `haiku`.
    - Test 2: `validateRouteConfig()` rejects unknown top-level fields, missing provider/model, unsupported conformance names, and credential fields containing literal secret-looking values.
    - Test 3: `resolveRoute()` maps aliases to provider/model/endpoint metadata and returns `missing-route` without fallback when an alias is absent.
    - Test 4: reference `sonnet` and `opus` route data are declarative and no workflow module needs concrete GLM/GPT-5.5 identifiers.
    - Test 5: `buildConfiguredRouteMetadata()` emits `configuredProvider` and `configuredModel`; it never emits `observedProvider` or `observedModel`.
    - Test 6: `buildServedRouteObservation()` emits `observedProvider`, `observedModel`, `rawModelField`, `responseId`, `observationSource`, `observedAt`, `confidence`, and `mismatch` only from explicit provider response or gateway header metadata.
    - Test 7: `buildResolvedRouteAuditEvent()` contains separate `configuredRoute` and `servedRouteObservation` objects and excludes headers, tokens, request bodies, prompts, responses, and credential values.
    - Test 8: existing `provider-routes` protected surface classification covers `.claude/advisor-mode/provider-routes.example.json` and `.claude/advisor-mode/provider-routes.json`.
    - Test 9: `advisor-gate.js` `buildRuntimeRouteMetadata()` resolves advisor alias `opus` through provider routes and returns `routeResolution.configuredRoute` plus `routeResolution.servedRouteObservation.observed === false` when no provider response is supplied.
    - Test 10: `.claude/hooks/executor-route-audit.js` `recordExecutorRouteResolution()` resolves executor alias `sonnet`, writes `.advisor/runtime/executor-calls/*.json`, appends `provider_route.executor_call` JSONL, and keeps configured route separate from served-route observation.
    - Test 11: when explicit provider response metadata is supplied to executor route audit, the artifact records `servedRouteObservation.observedProvider/observedModel` and flags `mismatch: true` if it differs from `configuredRoute.configuredProvider/configuredModel`.
    - Test 12: `.claude/settings.json` registers `executor-route-audit.js` on the actual Claude Code executor runtime hook surface used by this repo.
  </behavior>
  <acceptance_criteria>
    - `node --test .claude/advisor-mode/tests/provider-routing.test.js` fails because configured-vs-served separation and served-route observation normalization are not yet implemented.
    - Test names explicitly mention ROUT-01, ROUT-02, or ROUT-04.
    - Tests use Node built-in `node:test` and `node:assert/strict`; no package install is introduced.
  </acceptance_criteria>
  <action>Create or revise `.claude/advisor-mode/tests/provider-routing.test.js` using the existing Node test style from `protected-surface.test.js`. Require `../provider-routing.js`, `../../hooks/advisor-gate.js`, and `../../hooks/executor-route-audit.js`; load route config, policy, and settings fixtures; use temp roots for artifact/request writes. Encode the verification gap directly: static `resolveRoute()` output must be named `configuredRoute`, while `servedRouteObservation` must remain `observed: false` until explicit provider response metadata is supplied. Keep assertions deterministic and filesystem-local.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/provider-routing.test.js</automated>
  </verify>
  <done>Failing route tests encode the Phase 4 route/audit/runtime contract and prevent static config from satisfying actual served-route visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement route schema, resolver, configured-vs-served metadata, advisor wiring, and executor runtime audit hook</name>
  <read_first>
    - `.claude/advisor-mode/tests/provider-routing.test.js`
    - `.claude/advisor-mode/gate-event.schema.json`
    - `.claude/advisor-mode/final-review.js`
    - `.claude/advisor-mode/policy.example.json`
    - `.claude/hooks/advisor-gate.js`
    - `.claude/settings.json`
    - `.planning/phases/04-provider-routing-and-conformance/04-CONTEXT.md`
    - `.planning/phases/04-provider-routing-and-conformance/04-VERIFICATION.md`
  </read_first>
  <files>.claude/advisor-mode/provider-routes.example.json, .claude/advisor-mode/provider-routes.schema.json, .claude/advisor-mode/provider-routing.js, .claude/hooks/advisor-gate.js, .claude/hooks/executor-route-audit.js, .claude/settings.json</files>
  <acceptance_criteria>
    - `provider-routes.schema.json` is draft 2020-12 JSON Schema with `additionalProperties: false`, required `schemaVersion`, and route objects requiring `provider`, `model`, `endpointRef`, and `requiredConformance`.
    - `provider-routes.example.json` defines `sonnet`, `opus`, and `haiku`; `sonnet` uses the verified GLM executor target and `opus` uses the verified GPT-5.5 advisor target from Task 1.
    - `provider-routing.js` exports `validateRouteConfig`, `loadRouteConfig`, `resolveRoute`, `buildConfiguredRouteMetadata`, `buildServedRouteObservation`, `buildResolvedRouteAuditEvent`, and `writeResolvedRouteArtifact`.
    - Missing aliases return `reasonCode: "missing-route"`; no fallback provider/model is selected silently.
    - Audit/artifact helpers write separate `configuredRoute` and `servedRouteObservation` objects and reject or omit credential values, headers, prompts, responses, request bodies, and token-like fields.
    - `advisor-gate.js` exports `buildRuntimeRouteMetadata`, resolves the advisor route alias `opus` for the real consultation request path, and includes `routeResolution.configuredRoute` plus `routeResolution.servedRouteObservation` in advisor consultation request artifacts by default.
    - `.claude/hooks/executor-route-audit.js` resolves the executor route alias `sonnet`, writes `.advisor/runtime/executor-calls/*.json`, and appends `.advisor/audit/events.jsonl` with `event: "provider_route.executor_call"`, `configuredRoute`, and `servedRouteObservation`.
    - Runtime route metadata failures are recorded as explicit `configuredRoute.ok: false` or executor audit `ok: false`; they do not silently invent provider/model values.
    - `node --test .claude/advisor-mode/tests/provider-routing.test.js` passes.
  </acceptance_criteria>
  <action>Implement the thin route layer per D-01/D-02 and close the ROUT-02 naming gap. Create strict schema and declarative route example using Task 1 IDs while workflow code continues to consume `sonnet`, `opus`, and `haiku` aliases. Implement CommonJS resolver functions in `.claude/advisor-mode/provider-routing.js` using manual validation patterns from `final-review.js`. Rename static route output to `configuredRoute.configuredProvider/configuredModel`; add `buildServedRouteObservation()` that only reads explicit provider response metadata such as `response.model`, `response.provider`, `providerHeaders["x-openrouter-provider"]`, `providerHeaders["x-litellm-model-id"]`, or hook input fields named `servedProvider` and `servedModel`. Extend `advisor-gate.js` and `executor-route-audit.js` to persist both objects. Keep this as metadata enrichment only; do not add gate, verdict, budget, fallback, or workflow-control decisions to routing.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/provider-routing.test.js</automated>
  </verify>
  <done>Semantic aliases resolve to declarative provider/model targets, reference routes remain configurable without workflow coupling, and runtime audit surfaces no longer confuse configured route metadata with observed served provider/model data.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                               | Description                                                                                                                       |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| maintainer route config -> route resolver              | Repo/plugin-owned route data becomes configured provider/model metadata.                                                          |
| route resolver -> `.advisor/` artifacts                | Runtime metadata is persisted for operator inspection.                                                                            |
| provider response metadata -> served-route observation | Untrusted gateway/provider response fields become operator-facing evidence.                                                       |
| route resolver -> advisor consultation requests        | Existing runtime request artifacts receive sanitized configured route metadata and an unobserved served-route slot by default.    |
| route resolver -> executor runtime audit hook          | Executor runtime hook events receive sanitized configured route metadata and served-route observations when response data exists. |
| route config -> existing protected-surface gate        | Governance-sensitive route file changes remain under existing advisor/human review chain.                                         |

## STRIDE Threat Register

| Threat ID | Category                           | Component                                                                                        | Disposition | Mitigation Plan                                                                                                                                                                                          |
| --------- | ---------------------------------- | ------------------------------------------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Tampering / Elevation of Privilege | `.claude/advisor-mode/provider-routes.example.json`, `.claude/advisor-mode/provider-routes.json` | mitigate    | Keep route files covered by existing `provider-routes` protected surface tests and require strict route schema validation before resolution.                                                             |
| T-04-02   | Information Disclosure             | `provider-routing.js`, `advisor-gate.js`, `executor-route-audit.js` artifacts                    | mitigate    | Store only alias/provider/model/endpoint/conformance status plus sanitized observation fields; reject or omit prompt/response/token/header/body fields except allowed non-secret provider/model headers. |
| T-04-03   | Repudiation / Tampering            | `buildConfiguredRouteMetadata()` and `buildServedRouteObservation()`                             | mitigate    | Use separate configured and observed field names, set `observed: false` when no response metadata exists, and flag configured-vs-served mismatch explicitly.                                             |
| T-04-SC   | Tampering                          | npm/pip/cargo installs                                                                           | accept      | No external packages are installed in this plan; keep package install count at zero.                                                                                                                     |

</threat_model>

<verification>
Run `node --test .claude/advisor-mode/tests/provider-routing.test.js` after Task 2 to see RED and after Task 3 to see GREEN. Run `node --test .claude/advisor-mode/tests/*.test.js` before marking the plan complete.
</verification>

<success_criteria>

- ROUT-01: `sonnet`, `opus`, and `haiku` map through declarative provider route config.
- ROUT-02: advisor and executor runtime artifacts separate `configuredRoute` from `servedRouteObservation`, and observed provider/model fields require actual response or gateway metadata.
- ROUT-04: GLM executor and GPT-5.5 advisor reference routes are configurable without changing workflow logic.
- D-01/D-02: route code does not contain advisor gate decisions, verdict handling, final-review policy, budget, or rollback logic.
  </success_criteria>

<source_audit>
GOAL: Maintainers can map Claude semantic aliases to concrete third-party models and prove gateway safety — covered here for routing and configured-vs-served runtime/audit metadata.
REQ: ROUT-01, ROUT-02, ROUT-04 — covered, with ROUT-02 explicitly distinguishing configured route from actual served-route observation.
RESEARCH: thin resolver, strict schema, protected route governance, no silent fallback, no secret logging, real runtime/audit chain visibility — covered.
CONTEXT: D-01, D-02, D-03, D-04, D-05, D-06, D-09, D-10 — covered or checkpointed.
VERIFICATION gaps: ROUT-02 static-config ambiguity — covered by configuredRoute vs servedRouteObservation contract.
Deferred Ideas: none.
</source_audit>

<output>
Create `.planning/phases/04-provider-routing-and-conformance/04-01-SUMMARY.md` when done.
</output>
