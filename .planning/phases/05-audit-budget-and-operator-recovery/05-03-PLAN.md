---
phase: 05-audit-budget-and-operator-recovery
plan: 03
type: tdd
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - .claude/advisor-mode/operator-recovery.js
  - .claude/advisor-mode/rollback.md
  - .claude/advisor-mode/tests/rollback.test.js
  - .claude/hooks/advisor-gate.js
  - .claude/hooks/advisor-final-review-gate.js
autonomous: true
requirements: [SAFE-03]
user_setup: []
must_haves:
  truths:
    - "Maintainer can switch Advisor Mode between enforce, warning-only, and disabled modes."
    - "Layered recovery controls allow implemented enforcement capability classes to be controlled independently."
    - "Over-limit degraded mode preserves final review and critical human approval gates."
    - "Recovery controls remain treated as protected governance surfaces."
  artifacts:
    - path: ".claude/advisor-mode/operator-recovery.js"
      provides: "Mode reader/evaluator for enforce, warning-only, disabled, degraded, and implemented capability class controls"
      exports:
        [
          "readOperatorMode",
          "evaluateOperatorRecovery",
          "isCapabilityEnabled",
          "buildRecoveryAuditEvent",
        ]
    - path: ".claude/advisor-mode/rollback.md"
      provides: "Documented kill switch, warning-only mode, restore path, and protected-surface warning"
    - path: ".claude/advisor-mode/tests/rollback.test.js"
      provides: "Node integration tests for recovery modes and preserved hard gates"
    - path: ".claude/hooks/advisor-gate.js"
      provides: "PreToolUse recovery-mode integration"
    - path: ".claude/hooks/advisor-final-review-gate.js"
      provides: "Stop hook preservation during degraded over-limit mode"
  key_links:
    - from: ".claude/hooks/advisor-gate.js"
      to: ".claude/advisor-mode/operator-recovery.js"
      via: "evaluateOperatorRecovery"
      pattern: "operator-recovery\\.js"
    - from: ".claude/hooks/advisor-final-review-gate.js"
      to: ".claude/advisor-mode/operator-recovery.js"
      via: "readOperatorMode"
      pattern: "operator-recovery\\.js"
    - from: ".claude/advisor-mode/operator-recovery.js"
      to: ".claude/advisor-mode/audit-log.js"
      via: "appendAuditEvent({ event: 'operator_recovery.mode_checked', ... })"
      pattern: "operator_recovery\\.mode_checked"
---

<objective>
Deliver the operator recovery vertical slice for Phase 5.

Purpose: Maintainers need safe rollback controls that preserve mandatory safety gates while allowing degraded or warning-only continuity.
Output: Tested recovery module, hook integration, and operator rollback documentation.
</objective>

<phase_goal>
**As a** user, **I want to** operate Advisor Mode with correlated audit history, bounded advisor usage, install validation, and safe rollback controls, **so that** I can keep advisor enforcement reliable and recoverable during real work.
</phase_goal>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-audit-budget-and-operator-recovery/05-CONTEXT.md
@.planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
@.planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
@.planning/phases/05-audit-budget-and-operator-recovery/05-01-SUMMARY.md
@.planning/phases/05-audit-budget-and-operator-recovery/05-02-SUMMARY.md
@.claude/advisor-mode/audit-log.js
@.claude/advisor-mode/budget-state.js
@.claude/hooks/advisor-gate.js
@.claude/hooks/advisor-final-review-gate.js
@.claude/advisor-mode/policy.example.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED recovery mode tests</name>
  <files>.claude/advisor-mode/tests/rollback.test.js</files>
  <read_first>
    .claude/hooks/advisor-gate.js
    .claude/hooks/advisor-final-review-gate.js
    .claude/advisor-mode/budget-state.js
    .claude/advisor-mode/audit-log.js
    .planning/phases/05-audit-budget-and-operator-recovery/05-CONTEXT.md
    .planning/phases/02-enforced-trigger-gates/02-CONTEXT.md
    .planning/phases/03-verdict-handoff-and-verification-evidence/03-CONTEXT.md
  </read_first>
  <behavior>
    - readOperatorMode maps `.planning/config.json` hooks `{ advisor_mode: true, advisor_mode_strict: true }` to `mode: "enforce"`.
    - readOperatorMode maps `{ advisor_mode: true, advisor_mode_strict: false }` to `mode: "warning-only"`.
    - readOperatorMode maps `{ advisor_mode: false }` to `mode: "disabled"`.
    - evaluateOperatorRecovery respects layered capability classes `advisorConsultation`, `finalReview`, `criticalHumanApproval`, and `protectedSurfaces` per D-10.
    - Degraded over-limit mode downgrades non-critical advisor consultation to advisory but keeps critical human approval and final review mandatory per D-07, D-08, D-09.
    - Recovery mode changes and disabled/warning-only status are auditable as `operator_recovery.mode_checked` events.
  </behavior>
  <action>Create `.claude/advisor-mode/tests/rollback.test.js` with Node `node:test`. Import future `../operator-recovery.js`, existing `../../hooks/advisor-gate.js`, and `../../hooks/advisor-final-review-gate.js`. Use temporary roots with `.planning/config.json`. Tests must cover enforce, warning-only, disabled/kill-switch, degraded over-limit, critical human approval preservation using a destructive `git push --force origin main` event, final review preservation using explicit non-trivial completion event state, and protected-surface classification for `.claude/advisor-mode/policy.example.json`. This implements D-07 through D-12 and SAFE-03.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/rollback.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test file asserts exact modes `enforce`, `warning-only`, and `disabled`.
    - Test file asserts capability class names `advisorConsultation`, `finalReview`, `criticalHumanApproval`, and `protectedSurfaces`.
    - Initial test run fails because `.claude/advisor-mode/operator-recovery.js` does not yet provide required exports.
  </acceptance_criteria>
  <done>Failing tests define operator recovery behavior and preserved mandatory gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN recovery evaluator and hook preservation</name>
  <files>.claude/advisor-mode/operator-recovery.js, .claude/hooks/advisor-gate.js, .claude/hooks/advisor-final-review-gate.js, .claude/advisor-mode/tests/rollback.test.js</files>
  <read_first>
    .claude/advisor-mode/tests/rollback.test.js
    .claude/advisor-mode/audit-log.js
    .claude/advisor-mode/budget-state.js
    .claude/hooks/advisor-gate.js
    .claude/hooks/advisor-final-review-gate.js
    .claude/advisor-mode/policy.example.json
  </read_first>
  <action>Create `.claude/advisor-mode/operator-recovery.js` as a CommonJS module exporting `readOperatorMode(root, options)`, `evaluateOperatorRecovery(event, options)`, `isCapabilityEnabled(mode, capability)`, and `buildRecoveryAuditEvent(mode, options)`. Use `.planning/config.json` hooks flags as the current config source: `advisor_mode === false` means `disabled`; `advisor_mode === true && advisor_mode_strict === false` means `warning-only`; `advisor_mode === true && advisor_mode_strict === true` means `enforce`. Support optional `hooks.advisor_mode_capabilities` with boolean keys `advisorConsultation`, `finalReview`, `criticalHumanApproval`, `protectedSurfaces`; missing keys default true except in disabled mode. Integrate recovery evaluation into `advisor-gate.js` before normal gate enforcement so disabled mode returns `gateAction: "none", reasonCode: "advisor-mode-disabled"`, warning-only mode returns advisory for non-critical gates, and critical/protected governance surfaces still follow existing protected-surface model when enabled. Integrate `advisor-final-review-gate.js` so budget degraded mode does not bypass strict final review; only global disabled mode returns none. Append `operator_recovery.mode_checked` through `appendAuditEvent`. Follow the 05-PATTERNS.md analogs for `.claude/hooks/advisor-gate.js` mode switch, advisory/warning-only result, and hook decision response shapes, and for `.claude/advisor-mode/policy.example.json` protected-surface config classification; implement D-10, D-11, and D-12.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/rollback.test.js .claude/advisor-mode/tests/final-review-gate.test.js .claude/advisor-mode/tests/failure-and-human-gates.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Automated command exits 0.
    - `operator-recovery.js` exports all required identifiers.
    - Disabled mode returns `reasonCode: "advisor-mode-disabled"` without `permissionDecision: "deny"`.
    - Strict final-review tests still block missing fresh final review when Advisor Mode is enabled.
  </acceptance_criteria>
  <done>Recovery modes and layered capability switches are implemented without bypassing mandatory controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document rollback and refactor recovery semantics</name>
  <files>.claude/advisor-mode/rollback.md, .claude/advisor-mode/operator-recovery.js, .claude/advisor-mode/tests/rollback.test.js</files>
  <read_first>
    .claude/advisor-mode/operator-recovery.js
    .claude/advisor-mode/tests/rollback.test.js
    .claude/advisor-mode/README.md
    .claude/advisor-mode/policy.example.json
    .planning/phases/05-audit-budget-and-operator-recovery/05-CONTEXT.md
  </read_first>
  <action>Create `.claude/advisor-mode/rollback.md` documenting exact operator modes and config snippets: enforce uses `advisor_mode: true` and `advisor_mode_strict: true`; warning-only uses `advisor_mode: true` and `advisor_mode_strict: false`; disabled/kill-switch uses `advisor_mode: false`. Include implemented capability class keys `advisorConsultation`, `finalReview`, `criticalHumanApproval`, and `protectedSurfaces`. State that rollback controls are protected governance surfaces and changes to `.planning/config.json`, `.claude/hooks/`, `.claude/settings.json`, and `.claude/advisor-mode/*.json` continue to require protected-surface review when enforcement is enabled. Refactor `operator-recovery.js` only for clarity while keeping exports stable. Add tests that assert rollback.md contains the exact mode strings and implemented capability keys.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/rollback.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/advisor-mode/rollback.md` contains `advisor_mode`, `advisor_mode_strict`, `enforce`, `warning-only`, and `disabled/kill-switch`.
    - `.claude/advisor-mode/rollback.md` contains all four implemented capability class keys.
    - Automated command exits 0 after refactor and documentation tests.
  </acceptance_criteria>
  <done>Operator rollback path is documented, tested, and tied to the protected-surface model.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                          |
| --------------------------------------- | -------------------------------------------------------------------- |
| `.planning/config.json` -> hook control | Local config controls enforcement mode and capability classes.       |
| recovery docs -> operator action        | Human operator follows rollback guidance to change runtime behavior. |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | ---------------------- | ------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-07   | Elevation of Privilege | `operator-recovery.js`   | mitigate    | Disabled mode is explicit; warning-only and degraded modes preserve critical human approval and final review unless global disabled kill-switch is active. |
| T-05-08   | Repudiation            | rollback mode checks     | mitigate    | `operator_recovery.mode_checked` audit events record active mode and capability class state.                                                               |
| T-05-09   | Tampering              | recovery config surfaces | mitigate    | Documentation and tests keep recovery controls inside Phase 2 protected-surface model for `.planning/config.json`, hooks, settings, and advisor-mode JSON. |
| T-05-SC   | Tampering              | package installs         | accept      | Phase 5 installs no packages; package legitimacy audit lists none.                                                                                         |

</threat_model>

<verification>
<automated>node --test .claude/advisor-mode/tests/rollback.test.js .claude/advisor-mode/tests/final-review-gate.test.js .claude/advisor-mode/tests/failure-and-human-gates.test.js</automated>
</verification>

<success_criteria>

- SAFE-03: maintainer can switch to enforce, warning-only, or disabled/kill-switch behavior through documented local controls.
- D-07 through D-12 are implemented in tests, code, and rollback docs.
- Degraded budget mode does not bypass mandatory final review or critical human approval gates.
  </success_criteria>

<output>
Create `.planning/phases/05-audit-budget-and-operator-recovery/05-03-SUMMARY.md` when done.
</output>
