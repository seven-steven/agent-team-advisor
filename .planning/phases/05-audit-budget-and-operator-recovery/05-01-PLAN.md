---
phase: 05-audit-budget-and-operator-recovery
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/advisor-mode/audit-log.js
  - .claude/advisor-mode/tests/audit-log.test.js
  - .claude/hooks/executor-route-audit.js
  - .claude/advisor-mode/provider-routing.js
  - .claude/advisor-mode/final-review.js
  - .claude/hooks/advisor-gate.js
  - .claude/hooks/advisor-final-review-gate.js
autonomous: true
requirements: [AUDT-01, AUDT-03]
user_setup: []
must_haves:
  truths:
    - "Operator can inspect append-only raw Advisor Mode audit events."
    - "Operator can inspect correlated task and session audit views."
    - "Audit events carry both taskId and sessionId when available, and degrade to the available key when one is missing."
    - "Operator can see provider route, advisor verdict, executor decision, advisor trigger, hook decision, and final-review gate events in raw and correlated audit outputs."
  artifacts:
    - path: ".claude/advisor-mode/audit-log.js"
      provides: "Append-only audit writer, reader, CLI raw view, task view, and session view"
      exports:
        [
          "appendAuditEvent",
          "readAuditEvents",
          "filterAuditEvents",
          "buildCorrelationFields",
          "main",
        ]
    - path: ".claude/advisor-mode/tests/audit-log.test.js"
      provides: "Node test coverage for append-only audit, dual-key correlation, and producer wiring"
    - path: ".claude/hooks/executor-route-audit.js"
      provides: "Provider route producer wired to appendAuditEvent"
    - path: ".claude/advisor-mode/final-review.js"
      provides: "Advisor verdict, executor decision, and verification evidence producers wired to appendAuditEvent"
    - path: ".claude/hooks/advisor-gate.js"
      provides: "Advisor trigger and PreToolUse gate decision producer wired to appendAuditEvent"
    - path: ".claude/hooks/advisor-final-review-gate.js"
      provides: "Stop/final-review gate decision producer wired to appendAuditEvent"
  key_links:
    - from: ".claude/advisor-mode/audit-log.js"
      to: ".claude/advisor-mode/runtime-paths.js"
      via: "runtimePath(root, ['audit', 'events.jsonl'], options)"
      pattern: "runtimePath\\(root, \\[\"audit\", \"events\\.jsonl\"\\]"
    - from: ".claude/hooks/executor-route-audit.js"
      to: ".claude/advisor-mode/audit-log.js"
      via: "appendAuditEvent({ event: 'provider_route.executor_call', correlationKey, taskId, sessionId, ... })"
      pattern: "appendAuditEvent"
    - from: ".claude/advisor-mode/final-review.js"
      to: ".claude/advisor-mode/audit-log.js"
      via: "appendAuditEvent for advisor_verdict.recorded, executor_decision.recorded, and verification_evidence.recorded"
      pattern: "advisor_verdict\\.recorded|executor_decision\\.recorded"
    - from: ".claude/hooks/advisor-gate.js"
      to: ".claude/advisor-mode/audit-log.js"
      via: "appendAuditEvent for advisor.triggered and hook_decision.recorded with correlation fields"
      pattern: "advisor\\.triggered|hook_decision\\.recorded"
    - from: ".claude/hooks/advisor-final-review-gate.js"
      to: ".claude/advisor-mode/audit-log.js"
      via: "appendAuditEvent for final_review_gate.evaluated with correlation fields"
      pattern: "final_review_gate\\.evaluated"
---

<objective>
Deliver the audit/correlation vertical slice for Phase 5.

Purpose: Operators need an official local audit trail with both raw and correlated views before budget, recovery, and doctor flows can write auditable events.
Output: Tested CommonJS audit module plus CLI-readable raw/task/session views.
</objective>

<phase_goal>
**As a** user, **I want to** operate Advisor Mode with correlated audit history, bounded advisor usage, install validation, and safe rollback controls, **so that** I can keep advisor enforcement reliable and recoverable during real work.
</phase_goal>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-audit-budget-and-operator-recovery/05-CONTEXT.md
@.claude/advisor-mode/runtime-paths.js
@.claude/hooks/executor-route-audit.js
@.claude/advisor-mode/provider-routing.js
@.claude/advisor-mode/final-review.js
@.claude/hooks/advisor-gate.js
@.claude/hooks/advisor-final-review-gate.js
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED audit append and correlation tests</name>
  <files>.claude/advisor-mode/tests/audit-log.test.js</files>
  <read_first>
    .claude/advisor-mode/runtime-paths.js
    .claude/hooks/executor-route-audit.js
    .claude/advisor-mode/provider-routing.js
    .claude/advisor-mode/final-review.js
    .planning/phases/05-audit-budget-and-operator-recovery/05-CONTEXT.md
  </read_first>
  <behavior>
    - appendAuditEvent({ event: "advisor.triggered", correlationKey: "corr-1", taskId: "task-1", sessionId: "sess-1" }) appends exactly one JSON line to runtimePath(root, ["audit", "events.jsonl"]).
    - Two appendAuditEvent calls preserve both JSON lines in chronological append order; the first line is not rewritten.
    - buildCorrelationFields preserves taskId and sessionId when both exist, derives correlationKey from sessionId, taskId, existing correlationKey, or a deterministic sha256 fallback when needed, per D-01, D-02, D-03.
    - filterAuditEvents(events, { taskId: "task-1" }) and filterAuditEvents(events, { sessionId: "sess-1" }) return only matching events.
    - Sanitization prevents serialized audit output from containing authorization, headers, requestBody, prompt, messages, raw response body, bearer tokens, or sk-* tokens.
    - Representative producer fixtures for provider routes, advisor verdicts, executor decisions, advisor triggers, and final-review gates all write inspectable events through appendAuditEvent with correlationKey, taskId, and sessionId.
  </behavior>
  <action>Create `.claude/advisor-mode/tests/audit-log.test.js` using Node built-in `node:test` and `node:assert/strict`. Import the future module as `require("../audit-log.js")`. Use `fs.mkdtempSync(path.join(os.tmpdir(), "advisor-audit-log-"))` and `runtimePath(root, ["audit", "events.jsonl"], { runtimeRoot })` so tests do not write planning docs. Tests must assert raw append behavior, task/session filters, degraded correlation when only one key exists, and secret redaction. Use the `.claude/hooks/executor-route-audit.js` analog from `05-PATTERNS.md` for append-only writes and correlation fallback. This implements D-01, D-02, and D-03.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/audit-log.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains assertions for event names `advisor.triggered`, `provider_route.executor_call`, `advisor_verdict.recorded`, and `executor_decision.recorded` as inspectable raw events.
    - Initial test run fails because `.claude/advisor-mode/audit-log.js` does not yet provide the required exports.
    - No test writes under `.planning/` or the repository `.advisor/` directory.
  </acceptance_criteria>
  <done>Failing tests define append-only raw audit inspection and dual-key correlated task/session filtering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN audit writer, reader, and CLI views</name>
  <files>.claude/advisor-mode/audit-log.js, .claude/advisor-mode/tests/audit-log.test.js</files>
  <read_first>
    .claude/advisor-mode/tests/audit-log.test.js
    .claude/advisor-mode/runtime-paths.js
    .claude/hooks/executor-route-audit.js
  </read_first>
  <action>Create `.claude/advisor-mode/audit-log.js` as a CommonJS executable module. Export `appendAuditEvent(event, options)`, `readAuditEvents(options)`, `filterAuditEvents(events, filter)`, `buildCorrelationFields(input, options)`, `sanitizeAuditEvent(event)`, and `main(argv, io)`. Use `runtimePath(root, ["audit", "events.jsonl"], options)` for the default raw stream. `appendAuditEvent` must add `timestamp`, require a non-empty `event` string, preserve `correlationKey`, `taskId`, and `sessionId`, append one compact JSON object plus newline, and return `{ ok: true, auditPath, event }`. `readAuditEvents` must tolerate blank lines and malformed historical lines by returning parsed events and a `parseErrors` array rather than rewriting history. `main` must support `raw`, `task --task-id <id>`, and `session --session-id <id>` commands that print JSON with `{ status, view, count, events }`. Do not add external packages. Follow the `.claude/hooks/executor-route-audit.js` and `.claude/advisor-mode/provider-conformance.js` append-only analogs from `05-PATTERNS.md`. Implement D-01, D-02, and D-03 exactly.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/audit-log.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/advisor-mode/audit-log.js` exports all identifiers named in this task.
    - Running the automated command exits 0.
    - CLI `node .claude/advisor-mode/audit-log.js raw --root <tmpRoot> --runtime-root <tmpRuntime>` prints JSON containing `"view":"raw"`.
    - Source contains `fs.appendFileSync` and does not contain `writeFileSync(auditPath`.
  </acceptance_criteria>
  <done>Audit raw stream and task/session views are implemented and tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN producer wiring for complete audit surface</name>
  <files>.claude/advisor-mode/audit-log.js, .claude/advisor-mode/tests/audit-log.test.js, .claude/hooks/executor-route-audit.js, .claude/advisor-mode/provider-routing.js, .claude/advisor-mode/final-review.js, .claude/hooks/advisor-gate.js, .claude/hooks/advisor-final-review-gate.js</files>
  <read_first>
    .claude/advisor-mode/audit-log.js
    .claude/advisor-mode/tests/audit-log.test.js
    .claude/hooks/executor-route-audit.js
    .claude/advisor-mode/provider-routing.js
    .claude/advisor-mode/final-review.js
    .claude/hooks/advisor-gate.js
    .claude/hooks/advisor-final-review-gate.js
    .claude/advisor-mode/provider-conformance.js
  </read_first>
  <behavior>
    - Provider route resolution writes `provider_route.executor_call` through `appendAuditEvent` and preserves configured-vs-observed provider fields.
    - Advisor verdict recording writes `advisor_verdict.recorded` through `appendAuditEvent`.
    - Executor follow-up decision recording writes `executor_decision.recorded` through `appendAuditEvent`.
    - PreToolUse gate evaluation writes `advisor.triggered` when a new advisor consultation is required and writes `hook_decision.recorded` for block/advisory/allow/hard-stop outcomes.
    - Stop/final-review gate evaluation writes `final_review_gate.evaluated` for block/advisory/allow outcomes.
    - Every wired producer passes `correlationKey`, `taskId`, and `sessionId` when available and uses `buildCorrelationFields` to degrade gracefully when one key is missing.
  </behavior>
  <action>Wire the existing Phase 2-4 event producers into `.claude/advisor-mode/audit-log.js` so AUDT-01 covers the complete event surface, not only the helper/CLI. Replace direct `fs.appendFileSync(...events.jsonl...)` writes in `.claude/hooks/executor-route-audit.js`, `.claude/advisor-mode/provider-routing.js`, and `.claude/advisor-mode/final-review.js` with `appendAuditEvent` while preserving existing per-event artifact writes. Add `appendAuditEvent` calls in `.claude/hooks/advisor-gate.js` for advisor trigger and hook decision outcomes, and in `.claude/hooks/advisor-final-review-gate.js` for final-review gate outcomes. Propagate `correlationKey`, `taskId`, and `sessionId` from raw hook input, normalized event state, artifact payloads, or environment/options into each write point. Follow the `executor-route-audit.js`, `final-review.js`, and `advisor-final-review-gate.js` analogs in `05-PATTERNS.md`; preserve Phase 04 configured-vs-observed provider semantics and do not fabricate observed model values. Refactor only direct integration lines needed for shared audit writing. Implement D-01, D-02, D-03, AUDT-01, and AUDT-03.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/audit-log.test.js .claude/advisor-mode/tests/final-review-gate.test.js .claude/advisor-mode/tests/failure-and-human-gates.test.js .claude/advisor-mode/tests/provider-conformance.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert the audit stream contains `provider_route.executor_call`, `advisor_verdict.recorded`, `executor_decision.recorded`, `advisor.triggered`, `hook_decision.recorded`, and `final_review_gate.evaluated`.
    - Tests assert each representative event preserves `correlationKey`, `taskId`, and `sessionId` when supplied.
    - Public exports from `.claude/advisor-mode/audit-log.js` remain unchanged.
    - `JSON.stringify(sanitizeAuditEvent({ authorization: "Bearer TOKEN_PLACEHOLDER" }))` in tests does not match `authorization`, `Bearer`, or `TOKEN_PLACEHOLDER`.
  </acceptance_criteria>
  <done>Shared audit writing is connected to provider route, advisor verdict, executor decision, advisor trigger, hook decision, and final-review gate event producers with task/session correlation.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                        |
| ---------------------------------- | ------------------------------------------------------------------ |
| hook/CLI input -> audit writer     | Untrusted local event data becomes persistent JSONL audit history. |
| runtime audit file -> operator CLI | Historical runtime data is parsed and displayed to operators.      |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | ---------------------- | ----------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-01   | Information Disclosure | `.claude/advisor-mode/audit-log.js` | mitigate    | `sanitizeAuditEvent` redacts credentials, headers, prompts, request bodies, raw responses, bearer-token patterns, and `sk-*` token patterns before append. |
| T-05-02   | Tampering              | `audit/events.jsonl`                | mitigate    | Writer uses append-only `fs.appendFileSync`; tests assert existing lines remain unchanged and source avoids `writeFileSync(auditPath`.                     |
| T-05-03   | Repudiation            | correlation fields                  | mitigate    | `buildCorrelationFields` records `correlationKey`, `taskId`, and `sessionId` plus degradation behavior when one key is unavailable.                        |
| T-05-SC   | Tampering              | package installs                    | accept      | Phase 5 installs no packages; package legitimacy audit lists none.                                                                                         |

</threat_model>

<verification>
<automated>node --test .claude/advisor-mode/tests/audit-log.test.js .claude/advisor-mode/tests/final-review-gate.test.js .claude/advisor-mode/tests/failure-and-human-gates.test.js .claude/advisor-mode/tests/provider-conformance.test.js</automated>
</verification>

<success_criteria>

- AUDT-01: raw append-only audit stream is inspectable through the module and CLI, and existing provider route, advisor verdict, executor decision, advisor trigger, hook decision, and final-review gate producers write through it.
- AUDT-03: taskId and sessionId filters return correlated event views across helper-created and producer-created events.
- D-01, D-02, D-03 are implemented in task actions and tests.
  </success_criteria>

<output>
Create `.planning/phases/05-audit-budget-and-operator-recovery/05-01-SUMMARY.md` when done.
</output>
