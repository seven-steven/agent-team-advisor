---
phase: 01-repo-scoped-advisor-foundation
plan: 2
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - .claude/advisor-mode/tests/advisor-agent.test.js
  - .claude/advisor-mode/tests/boundary.test.js
  - .claude/agents/advisor-reviewer.md
  - .claude/agents/executor-guidance.md
  - .claude/hooks/advisor-boundary-check.js
autonomous: true
requirements: [AGNT-01, AGNT-02]
user_setup: []
must_haves:
  truths:
    - "Advisor role remains structurally read-only after scaffold installation."
    - "Executor role guidance clearly keeps implementation tools and workspace mutation with executor only."
    - "Local validation detects advisor boundary drift before later enforcement phases build on it."
  artifacts:
    - path: ".claude/advisor-mode/tests/advisor-agent.test.js"
      provides: "advisor frontmatter validation"
    - path: ".claude/advisor-mode/tests/boundary.test.js"
      provides: "executor/advisor mutation-boundary validation"
    - path: ".claude/hooks/advisor-boundary-check.js"
      provides: "non-blocking boundary drift hook"
      exports: ["validateAdvisorBoundary", "main"]
  key_links:
    - from: ".claude/hooks/advisor-boundary-check.js"
      to: ".claude/agents/advisor-reviewer.md"
      via: "frontmatter scan"
      pattern: "advisor-reviewer\\.md"
    - from: ".claude/advisor-mode/tests/boundary.test.js"
      to: ".claude/agents/executor-guidance.md"
      via: "executor authority assertions"
      pattern: "executor"
---

<objective>
Harden the role-boundary slice of the skeleton so the installed advisor/executor assets are locally testable and drift is visible before trigger enforcement exists.

Purpose: satisfy AGNT-01 and AGNT-02 with automated proof that advisor is read-only and executor remains the only mutating actor.
Output: advisor/boundary tests and exported hook validation helpers.
</objective>

<phase_goal>
**As a** maintainer, **I want to** scaffold repo-scoped Advisor Mode assets, **so that** I can safely evolve the workflow.
</phase_goal>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
@.claude/agents/advisor-reviewer.md
@.claude/agents/executor-guidance.md
@.claude/hooks/advisor-boundary-check.js
@.claude/advisor-mode/tests/init.test.js

<interfaces>
Plan 01 creates `.claude/hooks/advisor-boundary-check.js`. In this plan, export `validateAdvisorBoundary(rootDir)` returning an object with `ok`, `findings`, and `advisorTools` fields while preserving hook CLI behavior. This keeps the hook testable without adding dependencies.

Locked decisions implemented here: D-04, D-05, D-06. Advisor outputs must remain verdict-first; advisor tools must remain exactly read-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED role-boundary tests</name>
  <files>.claude/advisor-mode/tests/advisor-agent.test.js, .claude/advisor-mode/tests/boundary.test.js</files>
  <read_first>
    - .claude/agents/advisor-reviewer.md
    - .claude/agents/executor-guidance.md
    - .claude/hooks/advisor-boundary-check.js
    - .claude/advisor-mode/tests/init.test.js
    - .planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
    - .claude/advisor-mode/tests/advisor-agent.test.js
    - .claude/advisor-mode/tests/boundary.test.js
  </read_first>
  <behavior>
    - `advisor-agent.test.js` verifies `advisor-reviewer.md` has `name: advisor-reviewer`, `model: opus`, `tools: Read, Grep, Glob`, verdict-first output language, and no mutating tools per D-04 and D-06.
    - `boundary.test.js` verifies `executor-guidance.md` names executor as the actor for Bash/Write/Edit/MultiEdit-style mutation and verifies `validateAdvisorBoundary(rootDir)` reports failure for a temp advisor file containing `Write`.
  </behavior>
  <action>Create both Node built-in tests. Use small local frontmatter parsing helpers inside tests rather than adding YAML dependencies. The tests should import `validateAdvisorBoundary` from `.claude/hooks/advisor-boundary-check.js`; if the export is missing, the RED failure should make that clear. Commit only the RED tests after confirming they fail for missing export or missing stricter content, not syntax.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/advisor-agent.test.js .claude/advisor-mode/tests/boundary.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist.
    - RED run fails for expected boundary implementation gaps.
    - RED commit follows `test(01-02): add failing role boundary tests`.
  </acceptance_criteria>
  <done>Role-boundary behavior is specified before hardening the assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN boundary validation and asset wording</name>
  <files>.claude/agents/advisor-reviewer.md, .claude/agents/executor-guidance.md, .claude/hooks/advisor-boundary-check.js</files>
  <read_first>
    - .claude/advisor-mode/tests/advisor-agent.test.js
    - .claude/advisor-mode/tests/boundary.test.js
    - .claude/agents/advisor-reviewer.md
    - .claude/agents/executor-guidance.md
    - .claude/hooks/advisor-boundary-check.js
    - .planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
  </read_first>
  <action>Update `advisor-boundary-check.js` to export `validateAdvisorBoundary(rootDir)` and keep CLI hook behavior non-blocking. The validator must read `.claude/agents/advisor-reviewer.md`, extract the frontmatter between leading `---` delimiters, require `model: opus`, require exact tools set `Read`, `Grep`, `Glob`, and add findings for `Bash`, `Write`, `Edit`, `MultiEdit`, missing tools, or omitted model. Update `advisor-reviewer.md` wording so its response contract is verdict-first with risk, blocking findings, recommended actions, and verification guidance per D-06. Update `executor-guidance.md` wording so executor owns workspace mutation and implementation tools per D-05. Do not implement high-risk blocking gates; this hook is advisory/non-blocking in Phase 1.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/advisor-agent.test.js .claude/advisor-mode/tests/boundary.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Role-boundary tests pass.
    - `validateAdvisorBoundary(rootDir)` returns `ok: false` and at least one finding for a temp advisor definition containing `Write`.
    - Hook CLI exits 0 for malformed/missing input and does not block tool execution in Phase 1.
    - GREEN commit follows `feat(01-02): validate advisor role boundary`.
  </acceptance_criteria>
  <done>Advisor boundary is locally provable and executor-only mutation is explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR boundary parser clarity</name>
  <files>.claude/hooks/advisor-boundary-check.js, .claude/advisor-mode/tests/advisor-agent.test.js, .claude/advisor-mode/tests/boundary.test.js</files>
  <read_first>
    - .claude/hooks/advisor-boundary-check.js
    - .claude/advisor-mode/tests/advisor-agent.test.js
    - .claude/advisor-mode/tests/boundary.test.js
    - .planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
  </read_first>
  <action>After GREEN, simplify only the parser/test code touched in this plan. Keep the parser dependency-free and deterministic. Run the required code-review then simplify refactor checks from project instructions before committing a refactor if changes are made.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/advisor-agent.test.js .claude/advisor-mode/tests/boundary.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Boundary tests remain green.
    - Refactor does not change generated agent names, model alias, tool allowlist, or hook non-blocking behavior.
    - Optional REFACTOR commit follows `refactor(01-02): simplify boundary validation` if changes are made.
  </acceptance_criteria>
  <done>Boundary validator stays clear enough for later gate phases to extend.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                         |
| --------------------------------- | ----------------------------------------------------------------------------------- |
| advisor asset to validator        | Project-maintained Markdown controls advisor tool permissions and model alias.      |
| hook runtime to executor workflow | Hook observes tool activity and emits advisory context without blocking in Phase 1. |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                        |
| --------- | ---------------------- | -------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-05   | Elevation of Privilege | advisor subagent tools           | mitigate    | Tests and validator require exact `Read`, `Grep`, `Glob` allowlist and reject mutating tools.                                          |
| T-01-06   | Spoofing               | advisor model alias              | mitigate    | Tests and validator require `model: opus`; later routing phases map alias to provider.                                                 |
| T-01-07   | Repudiation            | executor/advisor handoff wording | mitigate    | Executor guidance states executor owns mutation and decisions; advisor output contract includes blocking findings and recommendations. |
| T-01-SC   | Tampering              | npm/pip/cargo installs           | accept      | No package-manager installs in this plan.                                                                                              |

</threat_model>

<verification>
<automated>node --test .claude/advisor-mode/tests/advisor-agent.test.js .claude/advisor-mode/tests/boundary.test.js</automated>
<automated>grep -v '^#' .claude/agents/advisor-reviewer.md | grep -E 'model: opus'</automated>
<automated>grep -v '^#' .claude/agents/advisor-reviewer.md | grep -E 'tools: Read, Grep, Glob'</automated>
</verification>

<success_criteria>

- Advisor has structural read-only boundary, not prompt-only boundary.
- Executor-only mutation is explicit in repo-scoped assets.
- Boundary drift is automatically detectable with Node tests and hook helper.
  </success_criteria>

<output>
Create `.planning/phases/01-repo-scoped-advisor-foundation/01-02-SUMMARY.md` when done.
</output>
