---
phase: 01-repo-scoped-advisor-foundation
plan: 1
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/advisor-mode/tests/init.test.js
  - .claude/advisor-mode/init.js
  - .claude/agents/advisor-reviewer.md
  - .claude/agents/executor-guidance.md
  - .claude/hooks/advisor-boundary-check.js
  - .claude/hooks/advisor-install-audit.js
  - .claude/advisor-mode/policy.example.json
  - .claude/advisor-mode/verdict.schema.json
  - .claude/settings.json
  - .advisor/audit/.gitkeep
  - .advisor/state/.gitkeep
  - .gitignore
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, SETP-01]
user_setup: []
must_haves:
  truths:
    - "Maintainer can run one repo-local scaffold flow that installs Advisor Mode foundation assets."
    - "Advisor role exists as a project subagent using model alias opus with only Read, Grep, and Glob tools."
    - "Executor guidance preserves executor-only workspace mutation authority."
    - "Versioned policy/schema examples live under .claude/advisor-mode while runtime placeholders live under .advisor."
  artifacts:
    - path: ".claude/advisor-mode/init.js"
      provides: "idempotent scaffold/init flow"
      exports: ["main", "scaffoldAdvisorMode"]
    - path: ".claude/advisor-mode/tests/init.test.js"
      provides: "end-to-end scaffold regression test"
    - path: ".claude/agents/advisor-reviewer.md"
      provides: "read-only advisor subagent"
      contains: "tools: Read, Grep, Glob"
    - path: ".claude/settings.json"
      provides: "Claude Code hook wiring"
      contains: "advisor-boundary-check.js"
  key_links:
    - from: ".claude/advisor-mode/init.js"
      to: ".claude/settings.json"
      via: "idempotent settings merge"
      pattern: "advisor-boundary-check\\.js|advisor-install-audit\\.js"
    - from: ".claude/settings.json"
      to: ".claude/hooks/advisor-boundary-check.js"
      via: "PreToolUse command hook"
      pattern: "PreToolUse"
    - from: ".claude/advisor-mode/init.js"
      to: ".advisor/audit/.gitkeep"
      via: "runtime directory creation"
      pattern: "\\.advisor"
---

<objective>
Create the Walking Skeleton: a single local scaffold command that proves the repo-scoped Advisor Mode stack end-to-end by writing project-local agent assets, hook scripts, settings integration, policy/schema examples, and runtime placeholders.

Purpose: satisfy the Phase 1 user story with the thinnest real maintainer capability while preserving locked decisions D-01 through D-12.
Output: `.claude/advisor-mode/init.js`, initial Advisor Mode assets, runtime placeholders, and a failing-then-passing scaffold test.
</objective>

<phase_goal>
**As a** maintainer, **I want to** scaffold repo-scoped Advisor Mode assets, **so that** I can safely evolve the workflow.
</phase_goal>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
@.claude/settings.json
@.claude/package.json
@README.md

<interfaces>
Existing `.claude/settings.json` shape uses top-level `hooks` with arrays for `SessionStart`, `PostToolUse`, and `PreToolUse`. Preserve all existing `gsd-*` hook commands and unknown keys. Add Advisor Mode hook entries by command path only when absent.

Existing `.claude/package.json` is `{ "type": "commonjs" }`. Use CommonJS for `.claude/advisor-mode/init.js` and `.claude/hooks/*.js`.

Locked decisions implemented here: D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-09, D-10, D-12. Use `.advisor/` for runtime placeholders under Claude's discretion in CONTEXT.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED scaffold end-to-end test</name>
  <files>.claude/advisor-mode/tests/init.test.js</files>
  <read_first>
    - .planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
    - .claude/settings.json
    - .claude/package.json
    - .claude/advisor-mode/tests/init.test.js
  </read_first>
  <behavior>
    - Running `node .claude/advisor-mode/init.js --root <tempdir>` creates `.claude/agents/advisor-reviewer.md`, `.claude/agents/executor-guidance.md`, `.claude/hooks/advisor-boundary-check.js`, `.claude/hooks/advisor-install-audit.js`, `.claude/advisor-mode/policy.example.json`, `.claude/advisor-mode/verdict.schema.json`, `.advisor/audit/.gitkeep`, and `.advisor/state/.gitkeep`.
    - Generated advisor agent frontmatter contains `model: opus` and exactly the read-only tools `Read`, `Grep`, and `Glob` per D-04.
    - Generated settings preserve a seeded existing `gsd-context-monitor.js` hook and add Advisor Mode command hooks once, even when init runs twice per D-03 and D-07.
    - Runtime JSONL/state placeholders are under `.advisor/`, not `.planning/`, per D-10 and D-12.
  </behavior>
  <action>Create `.claude/advisor-mode/tests/init.test.js` using Node built-in `node:test`, `node:assert/strict`, `node:fs`, `node:os`, `node:path`, and `node:child_process`. The test must create a temporary repo root, seed `.claude/settings.json` with at least one existing `gsd-context-monitor.js` PostToolUse command, run `process.execPath` against `.claude/advisor-mode/init.js --root <tempdir>`, and assert the concrete files and content listed in the behavior block. Commit only the RED test after confirming it fails because `init.js` or scaffold outputs are absent.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/init.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test exists at `.claude/advisor-mode/tests/init.test.js`.
    - Test fails before implementation for the expected missing scaffold command/output reason, not because of syntax errors.
    - RED commit follows `test(01-01): add failing scaffold test`.
  </acceptance_criteria>
  <done>RED gate proves the maintainer scaffold flow is specified before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN scaffold command and baseline assets</name>
  <files>.claude/advisor-mode/init.js, .claude/agents/advisor-reviewer.md, .claude/agents/executor-guidance.md, .claude/hooks/advisor-boundary-check.js, .claude/hooks/advisor-install-audit.js, .claude/advisor-mode/policy.example.json, .claude/advisor-mode/verdict.schema.json, .claude/settings.json, .advisor/audit/.gitkeep, .advisor/state/.gitkeep, .gitignore</files>
  <read_first>
    - .claude/advisor-mode/tests/init.test.js
    - .planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
    - .claude/settings.json
    - .claude/package.json
    - .gitignore
    - .claude/advisor-mode/init.js
    - .claude/agents/advisor-reviewer.md
    - .claude/agents/executor-guidance.md
    - .claude/hooks/advisor-boundary-check.js
    - .claude/hooks/advisor-install-audit.js
    - .claude/advisor-mode/policy.example.json
    - .claude/advisor-mode/verdict.schema.json
    - .advisor/audit/.gitkeep
    - .advisor/state/.gitkeep
  </read_first>
  <action>Implement `.claude/advisor-mode/init.js` as a zero-new-dependency CommonJS scaffold command exporting `scaffoldAdvisorMode(rootDir)` and `main(argv)`. It must accept `--root <path>` with default `process.cwd()`, create directories, write deterministic templates, merge `.claude/settings.json` without deleting existing hooks, and be idempotent by Advisor Mode command path. Write `.claude/agents/advisor-reviewer.md` with `name: advisor-reviewer`, `model: opus`, and `tools: Read, Grep, Glob` per D-04; write `.claude/agents/executor-guidance.md` stating executor retains mutation/tool execution authority per D-05; write versioned `.claude/advisor-mode/policy.example.json` and `verdict.schema.json` per D-06 and D-10; write non-blocking CommonJS hook placeholders for boundary/audit integration; create `.advisor/audit/.gitkeep` and `.advisor/state/.gitkeep`; add ignore rules for `.advisor/audit/*.jsonl` and `.advisor/state/*.json` while keeping `.gitkeep` trackable. Do not add provider routing, high-risk gate blocking, budgets, or hosted services because those are later roadmap phases.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/init.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test .claude/advisor-mode/tests/init.test.js` passes.
    - `.claude/settings.json` still contains every pre-existing `gsd-*` hook command and now contains exactly one Advisor Mode boundary hook command and one install audit hook command.
    - Advisor frontmatter does not contain `Bash`, `Write`, `Edit`, or `MultiEdit`.
    - `.advisor/audit/.gitkeep` and `.advisor/state/.gitkeep` exist; generated runtime JSONL/state file patterns are ignored.
    - GREEN commit follows `feat(01-01): implement advisor scaffold skeleton`.
  </acceptance_criteria>
  <done>Maintainer can run the repo-local scaffold flow and receive real project assets plus runtime placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR scaffold internals without changing behavior</name>
  <files>.claude/advisor-mode/init.js, .claude/advisor-mode/tests/init.test.js</files>
  <read_first>
    - .claude/advisor-mode/init.js
    - .claude/advisor-mode/tests/init.test.js
    - .planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
  </read_first>
  <action>After GREEN, simplify only the scaffold/test code touched in this plan. Extract small helpers only when they reduce duplication in path creation, file writes, or settings hook merge logic. Keep behavior and file formats stable. Run the required code-review then simplify refactor checks from project instructions before committing a refactor if changes are made.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/init.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test remains green after refactor.
    - Refactor touches only `.claude/advisor-mode/init.js` and/or `.claude/advisor-mode/tests/init.test.js`.
    - Optional REFACTOR commit follows `refactor(01-01): simplify scaffold internals` if changes are made.
  </acceptance_criteria>
  <done>Scaffold code is maintainable without expanding Phase 1 scope.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                 |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| maintainer CLI to filesystem          | `init.js --root` writes project-local files from a caller-provided root path.                               |
| scaffold to Claude Code settings      | scaffold mutates `.claude/settings.json`, which controls hook execution.                                    |
| versioned assets to runtime directory | versioned `.claude/advisor-mode/*` templates remain separate from generated `.advisor/*` runtime artifacts. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                            | Disposition | Mitigation Plan                                                                                               |
| --------- | ---------------------- | ---------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- |
| T-01-01   | Tampering              | `.claude/settings.json` merge                        | mitigate    | Preserve existing JSON keys and hook arrays; append Advisor Mode commands idempotently by exact command path. |
| T-01-02   | Elevation of Privilege | `.claude/agents/advisor-reviewer.md`                 | mitigate    | Scaffold explicit `tools: Read, Grep, Glob`; test rejects mutating tool names.                                |
| T-01-03   | Information Disclosure | `.advisor/audit/*.jsonl` and `.advisor/state/*.json` | mitigate    | Store under `.advisor/`, ignore generated runtime artifacts, keep only `.gitkeep` trackable.                  |
| T-01-04   | Denial of Service      | hook script registration                             | mitigate    | Use short settings timeouts matching existing hook style; Phase 1 hooks fail open/non-blocking.               |
| T-01-SC   | Tampering              | npm/pip/cargo installs                               | accept      | Phase 1 installs no external packages; package audit lists `none`.                                            |

</threat_model>

<verification>
<automated>node --test .claude/advisor-mode/tests/init.test.js</automated>
<automated>grep -v '^#' .claude/agents/advisor-reviewer.md | grep -E 'tools: Read, Grep, Glob'</automated>
<automated>grep -v '^#' .claude/agents/advisor-reviewer.md | grep -E 'Bash|Write|Edit|MultiEdit' && exit 1 || exit 0</automated>
</verification>

<success_criteria>

- Phase 1 Walking Skeleton exists: one maintainer command creates the repo-scoped Advisor Mode foundation.
- AGNT-01, AGNT-02, AGNT-03, and SETP-01 have executable scaffold coverage.
- Locked decisions D-01 through D-12 are not contradicted.
- No external packages are installed.
  </success_criteria>

<output>
Create `.planning/phases/01-repo-scoped-advisor-foundation/01-01-SUMMARY.md` when done.
</output>
