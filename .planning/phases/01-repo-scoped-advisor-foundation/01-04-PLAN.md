---
phase: 01-repo-scoped-advisor-foundation
plan: 4
type: tdd
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - .claude/advisor-mode/init.js
  - .claude/advisor-mode/tests/init.test.js
autonomous: true
gap_closure: true
requirements: [AGNT-01, AGNT-02, AGNT-03, SETP-01]
requirements_addressed: [AGNT-01, AGNT-02, AGNT-03, SETP-01]
user_setup: []
must_haves:
  truths:
    - "Fresh scaffold output reproduces the hardened advisor/executor boundary assets currently committed under .claude/."
    - "Generated advisor-boundary-check.js exports validateAdvisorBoundary and main, matching the checked-in hardened boundary hook contract."
    - "Generated executor-guidance.md explicitly keeps Bash, Write, Edit, and MultiEdit-style workspace mutation with the executor only."
    - "Generated advisor-reviewer.md preserves the final verdict-first response contract wording."
  artifacts:
    - path: ".claude/advisor-mode/init.js"
      provides: "scaffold templates synchronized with hardened checked-in assets"
      exports: ["main", "scaffoldAdvisorMode"]
    - path: ".claude/advisor-mode/tests/init.test.js"
      provides: "fresh scaffold generated-output regression coverage"
  key_links:
    - from: ".claude/advisor-mode/init.js"
      to: ".claude/hooks/advisor-boundary-check.js"
      via: "embedded scaffold template"
      pattern: "validateAdvisorBoundary"
    - from: ".claude/advisor-mode/init.js"
      to: ".claude/agents/executor-guidance.md"
      via: "embedded scaffold template"
      pattern: "Bash|Write|Edit|MultiEdit"
    - from: ".claude/advisor-mode/tests/init.test.js"
      to: "fresh scaffold temp root"
      via: "run init.js --root <tempdir> and inspect generated files"
      pattern: "validateAdvisorBoundary|executor"
---

<objective>
Close the Phase 1 verification gap by making the documented scaffold flow reproduce the hardened advisor/executor boundary assets from the committed repository.

Purpose: AGNT-02 remains blocked because the checked-in assets are hardened but fresh scaffold output is stale. This gap-closure plan updates only the scaffold templates and generated-output regression assertions needed to make a fresh install preserve the executor-only mutation boundary.
Output: synchronized `.claude/advisor-mode/init.js` templates and regression coverage in `.claude/advisor-mode/tests/init.test.js` that inspects scaffold-generated files, not only checked-in files.
</objective>

<phase_goal>
**As a** maintainer, **I want to** scaffold repo-scoped Advisor Mode assets, **so that** I can safely evolve the workflow.
</phase_goal>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/references/tdd.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/references/planner-gap-closure.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-VERIFICATION.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-01-SUMMARY.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-02-SUMMARY.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-03-SUMMARY.md
@.claude/advisor-mode/init.js
@.claude/advisor-mode/tests/init.test.js
@.claude/agents/advisor-reviewer.md
@.claude/agents/executor-guidance.md
@.claude/hooks/advisor-boundary-check.js

<interfaces>
Existing completed Phase 1 assets to mirror in scaffold templates:
- `.claude/hooks/advisor-boundary-check.js` exports `validateAdvisorBoundary(rootDir)` and `main`, while preserving non-blocking Phase 1 hook behavior.
- `.claude/agents/executor-guidance.md` explicitly names executor ownership of Bash, Write, Edit, and MultiEdit-style mutation.
- `.claude/agents/advisor-reviewer.md` uses `model: opus`, `tools: Read, Grep, Glob`, and the final verdict-first response contract.
- `.claude/advisor-mode/init.js` already exports `scaffoldAdvisorMode(rootDir)` and `main(argv)`; keep this interface unchanged.

Gap source: `01-VERIFICATION.md` failed truth #3 because fresh scaffold output generated stale copies of these three hardened assets. This plan addresses only that gap.

Locked decisions implemented here: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09. Do not add provider routing, high-risk trigger enforcement, budgets, hosted services, or new dependencies.
</interfaces>
</context>

<source_audit>
| Source | Item | Coverage |
| ------ | ---- | -------- |
| GOAL | Maintainer can scaffold repo-scoped Advisor Mode assets and safely evolve the workflow | Covered by Task 1 and Task 2 fresh scaffold regression and template synchronization. |
| REQ | AGNT-01 read-only stronger-model advisor | Covered by generated advisor-reviewer.md preserving model opus, tools Read/Grep/Glob, and verdict-first contract. |
| REQ | AGNT-02 executor-led workflow with executor-only mutation | Covered by generated executor-guidance.md wording and generated boundary hook export regression. |
| REQ | AGNT-03 repo-scoped versioned assets | Covered by retaining .claude/advisor-mode/init.js as the versioned scaffold source. |
| REQ | SETP-01 scaffold documented setup flow | Covered by making the existing scaffold flow reproduce hardened assets. |
| RESEARCH | Zero-new-dependency CommonJS scaffold and Node built-in tests | Covered by modifying existing CommonJS init.js and node:test file only. |
| CONTEXT | D-04 advisor has no mutating tools | Covered by generated advisor template and generated boundary validator assertion. |
| CONTEXT | D-05 executor owns mutation tools | Covered by generated executor guidance assertion for Bash, Write, Edit, MultiEdit. |
| CONTEXT | D-06 verdict-first advisor output | Covered by generated advisor-reviewer.md wording assertion. |
| VERIFICATION | Missing: sync init.js templates with hardened assets | Covered by Task 2. |
| VERIFICATION | Missing: generated-output regression for validateAdvisorBoundary export and executor tool ownership wording | Covered by Task 1. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED generated scaffold boundary regression</name>
  <files>.claude/advisor-mode/tests/init.test.js</files>
  <read_first>
    - .planning/phases/01-repo-scoped-advisor-foundation/01-VERIFICATION.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-02-SUMMARY.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-03-SUMMARY.md
    - .claude/advisor-mode/tests/init.test.js
    - .claude/advisor-mode/init.js
    - .claude/agents/advisor-reviewer.md
    - .claude/agents/executor-guidance.md
    - .claude/hooks/advisor-boundary-check.js
  </read_first>
  <behavior>
    - After running `node .claude/advisor-mode/init.js --root <tempdir>`, the generated `.claude/hooks/advisor-boundary-check.js` can be required and exposes functions named `validateAdvisorBoundary` and `main`.
    - The generated boundary hook's `validateAdvisorBoundary(tempRoot)` succeeds against the generated advisor asset and returns an object whose `ok` field is true.
    - The generated `.claude/agents/executor-guidance.md` explicitly names executor ownership for Bash, Write, Edit, and MultiEdit-style workspace mutation per D-05.
    - The generated `.claude/agents/advisor-reviewer.md` contains the final verdict-first response contract wording asserted by the checked-in hardened asset per D-06.
  </behavior>
  <action>Add regression assertions to the existing scaffold integration test so it inspects files generated in the test temp root after `init.js --root <tempdir>` runs. The assertions must target the generated files under the temp root, not the checked-in files. Use Node built-in modules already used by `init.test.js`; do not add packages. The RED failure must directly show the verification gap: missing generated `validateAdvisorBoundary` export, missing generated executor tool ownership wording, or missing generated advisor verdict-first wording.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/init.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/advisor-mode/tests/init.test.js` fails before implementation because fresh scaffold output does not reproduce the hardened boundary assets.
    - The failing assertions inspect generated temp-root files, not only `.claude/` files in the repository root.
    - The test checks `validateAdvisorBoundary`, `main`, Bash, Write, Edit, MultiEdit, and verdict-first response wording.
    - RED commit follows `test(01-04): assert scaffolded boundary assets`.
  </acceptance_criteria>
  <done>Verification gap is encoded as a failing generated-output regression for AGNT-02 and SETP-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN synchronize scaffold templates with hardened assets</name>
  <files>.claude/advisor-mode/init.js</files>
  <read_first>
    - .planning/phases/01-repo-scoped-advisor-foundation/01-VERIFICATION.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
    - .claude/advisor-mode/tests/init.test.js
    - .claude/advisor-mode/init.js
    - .claude/agents/advisor-reviewer.md
    - .claude/agents/executor-guidance.md
    - .claude/hooks/advisor-boundary-check.js
  </read_first>
  <action>Update only the embedded templates and supporting template wiring in `.claude/advisor-mode/init.js` so a fresh scaffold writes the same hardened boundary semantics as the committed assets. The generated `advisor-boundary-check.js` must export `validateAdvisorBoundary(rootDir)` and `main` and preserve fail-open/non-blocking Phase 1 hook behavior. The generated `executor-guidance.md` must explicitly state that the executor owns workspace mutation and implementation tools, naming Bash, Write, Edit, and MultiEdit. The generated `advisor-reviewer.md` must retain `model: opus`, `tools: Read, Grep, Glob`, and the final verdict-first response contract with risk, blocking findings, recommended actions, and verification guidance. Keep `scaffoldAdvisorMode(rootDir)` and `main(argv)` stable. Do not change checked-in hardened assets unless the generated-output tests reveal a direct mismatch caused by this template sync.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/init.test.js .claude/advisor-mode/tests/advisor-agent.test.js .claude/advisor-mode/tests/boundary.test.js .claude/advisor-mode/tests/scaffold-layout.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 1 Node test set passes.
    - Fresh scaffold output under a temp root contains a boundary hook where `typeof require(generatedHook).validateAdvisorBoundary === "function"` and `typeof require(generatedHook).main === "function"`.
    - Fresh scaffold output under a temp root contains executor guidance naming Bash, Write, Edit, and MultiEdit as executor-owned mutation/implementation tools.
    - Fresh scaffold output under a temp root contains advisor reviewer wording for a verdict-first response with risk, blocking findings, recommended actions, and verification guidance.
    - GREEN commit follows `fix(01-04): sync scaffold boundary templates`.
  </acceptance_criteria>
  <done>Fresh scaffold installation reproduces the hardened Phase 1 executor/advisor boundary and closes the failed AGNT-02 truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR generated-output assertions without broadening scope</name>
  <files>.claude/advisor-mode/init.js, .claude/advisor-mode/tests/init.test.js</files>
  <read_first>
    - .claude/advisor-mode/init.js
    - .claude/advisor-mode/tests/init.test.js
    - .planning/phases/01-repo-scoped-advisor-foundation/01-VERIFICATION.md
  </read_first>
  <action>After GREEN, simplify only duplication introduced by this gap-closure plan in template text handling or generated-output assertions. Keep the generated hardened semantics unchanged and keep the test focused on scaffold output. Run the project-required code-review then simplify refactor checks before committing a refactor if changes are made.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/init.test.js .claude/advisor-mode/tests/advisor-agent.test.js .claude/advisor-mode/tests/boundary.test.js .claude/advisor-mode/tests/scaffold-layout.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 1 Node test set remains green.
    - Refactor touches only `.claude/advisor-mode/init.js` and `.claude/advisor-mode/tests/init.test.js`.
    - Refactor does not add provider routing, trigger enforcement, budgets, telemetry expansion, packages, or new files.
    - Optional REFACTOR commit follows `refactor(01-04): simplify scaffold boundary regression` if changes are made.
  </acceptance_criteria>
  <done>Gap closure stays minimal, maintainable, and limited to stale scaffold-template drift.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                    |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| scaffold templates to generated repo assets  | `init.js` writes agent and hook files that define the advisor/executor authority boundary for a fresh install. |
| generated hook module to validation tests    | Tests require generated JavaScript from a temp scaffold root to prove exported validation behavior.            |
| executor guidance to human/operator behavior | Generated Markdown tells executors and maintainers which actor owns workspace mutation tools.                  |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                       |
| --------- | ---------------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-11   | Elevation of Privilege | generated advisor-reviewer.md       | mitigate    | Generated advisor template keeps exact `tools: Read, Grep, Glob` and no mutating tools.                                               |
| T-01-12   | Elevation of Privilege | generated executor-guidance.md      | mitigate    | Generated executor guidance explicitly assigns Bash, Write, Edit, and MultiEdit mutation authority to executor only.                  |
| T-01-13   | Tampering              | generated advisor-boundary-check.js | mitigate    | Generated hook exports `validateAdvisorBoundary(rootDir)` so drift is testable in fresh installs.                                     |
| T-01-14   | Repudiation            | generated advisor verdict contract  | mitigate    | Generated advisor reviewer preserves verdict-first contract with risk, blocking findings, recommendations, and verification guidance. |
| T-01-SC   | Tampering              | npm/pip/cargo installs              | accept      | No package-manager installs in this gap-closure plan.                                                                                 |

</threat_model>

<verification>
<automated>node --test .claude/advisor-mode/tests/init.test.js .claude/advisor-mode/tests/advisor-agent.test.js .claude/advisor-mode/tests/boundary.test.js .claude/advisor-mode/tests/scaffold-layout.test.js</automated>
<automated>node -e 'const fs=require("fs"),os=require("os"),path=require("path"),cp=require("child_process"); const root=fs.mkdtempSync(path.join(os.tmpdir(),"advisor-gap-")); cp.execFileSync(process.execPath,[".claude/advisor-mode/init.js","--root",root],{stdio:"inherit"}); const hook=require(path.join(root,".claude/hooks/advisor-boundary-check.js")); if(typeof hook.validateAdvisorBoundary!=="function"||typeof hook.main!=="function") process.exit(1); const guidance=fs.readFileSync(path.join(root,".claude/agents/executor-guidance.md"),"utf8"); for (const token of ["Bash","Write","Edit","MultiEdit"]) if(!guidance.includes(token)) process.exit(1);'</automated>
</verification>

<success_criteria>

- `01-VERIFICATION.md` failed truth #3 is directly addressed: fresh scaffold output preserves hardened executor/advisor boundary assets.
- AGNT-02 is closed by generated-output proof that mutation and implementation tools remain executor-owned and boundary drift is detectable from a fresh scaffold install.
- AGNT-01, AGNT-03, and SETP-01 remain covered because the scaffold still creates repo-scoped advisor assets with read-only advisor tooling and documented local install behavior.
- No unrelated Phase 1 scope is replanned or expanded.

</success_criteria>

<output>
Create `.planning/phases/01-repo-scoped-advisor-foundation/01-04-SUMMARY.md` when done.
</output>
