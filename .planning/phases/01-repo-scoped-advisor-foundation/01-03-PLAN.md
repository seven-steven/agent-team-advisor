---
phase: 01-repo-scoped-advisor-foundation
plan: 3
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - .claude/advisor-mode/tests/scaffold-layout.test.js
  - .claude/advisor-mode/tests/init.test.js
  - .claude/advisor-mode/init.js
  - .claude/advisor-mode/README.md
  - .claude/advisor-mode/policy.example.json
  - .claude/advisor-mode/verdict.schema.json
autonomous: true
requirements: [AGNT-03, SETP-01]
user_setup: []
must_haves:
  truths:
    - "Maintainer can inspect a documented local setup flow for the scaffold."
    - "Advisor Mode behavior is versioned as project-scoped `.claude` assets."
    - "Runtime audit/state examples remain separated from versioned policy/schema files."
    - "The scaffold layout test proves Phase 1 install correctness without provider routing checks."
  artifacts:
    - path: ".claude/advisor-mode/README.md"
      provides: "documented setup flow"
    - path: ".claude/advisor-mode/tests/scaffold-layout.test.js"
      provides: "repo-scoped layout validation"
    - path: ".claude/advisor-mode/policy.example.json"
      provides: "versioned policy example"
    - path: ".claude/advisor-mode/verdict.schema.json"
      provides: "versioned verdict schema example"
  key_links:
    - from: ".claude/advisor-mode/README.md"
      to: ".claude/advisor-mode/init.js"
      via: "documented command"
      pattern: "node \\.claude/advisor-mode/init\\.js"
    - from: ".claude/advisor-mode/tests/scaffold-layout.test.js"
      to: ".claude/advisor-mode/policy.example.json"
      via: "versioned asset assertion"
      pattern: "policy\\.example\\.json"
    - from: ".claude/advisor-mode/tests/scaffold-layout.test.js"
      to: ".advisor/audit"
      via: "runtime separation assertion"
      pattern: "\\.advisor/audit"
---

<objective>
Complete the documented setup and versioned-layout slice of the skeleton so maintainers can understand, rerun, and validate the repo-scoped installation.

Purpose: satisfy AGNT-03 and SETP-01 beyond file creation by making the scaffold flow reviewable, repeatable, and limited to Phase 1 validation scope.
Output: README setup flow, layout regression test, and refined policy/schema examples.
</objective>

<phase_goal>
**As a** maintainer, **I want to** scaffold repo-scoped Advisor Mode assets, **so that** I can safely evolve the workflow.
</phase_goal>

<execution_context>
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/templates/summary.md
@/home/seven/data/coding/projects/seven/agent-team-advisor/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
@.planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
@.claude/advisor-mode/init.js
@.claude/advisor-mode/policy.example.json
@.claude/advisor-mode/verdict.schema.json
@.claude/advisor-mode/tests/init.test.js

<interfaces>
Plan 01 creates the scaffold command and initial examples. This plan adds documentation and a layout test while preserving the same command interface: `node .claude/advisor-mode/init.js --root <path>`.

Locked decisions implemented here: D-01, D-02, D-03, D-07, D-08, D-09, D-10, D-11, D-12. Do not document or test provider routing conformance, high-risk gate enforcement, full telemetry, or budgets as Phase 1 deliverables.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED documented layout test</name>
  <files>.claude/advisor-mode/tests/scaffold-layout.test.js</files>
  <read_first>
    - .claude/advisor-mode/init.js
    - .claude/advisor-mode/tests/init.test.js
    - .claude/advisor-mode/policy.example.json
    - .claude/advisor-mode/verdict.schema.json
    - .planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
    - .claude/advisor-mode/tests/scaffold-layout.test.js
  </read_first>
  <behavior>
    - Layout test asserts all versioned Advisor Mode assets live under `.claude/` per D-01 and D-02.
    - Layout test asserts runtime placeholders live under `.advisor/audit` and `.advisor/state`, not under `.planning`, per D-10 and D-12.
    - Layout test asserts `README.md` documents the exact local command `node .claude/advisor-mode/init.js` and lists created agents, hooks, settings, policy/schema examples, and audit directories per D-07.
    - Layout test asserts `policy.example.json` includes append-only JSONL audit baseline event names for scaffold installation, advisor verdict receipt, and executor follow-up per D-11.
  </behavior>
  <action>Create `.claude/advisor-mode/tests/scaffold-layout.test.js` using Node built-in `node:test`. Reuse the temp-root scaffold execution pattern from `init.test.js`. Assert concrete paths, README command text, versioned/runtime separation, and policy JSON event names. Commit only the RED test after confirming it fails because README or refined policy content is absent, not because the scaffold command cannot run after Plan 01.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/scaffold-layout.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists.
    - RED run fails for missing documented setup/layout content.
    - RED commit follows `test(01-03): add failing scaffold layout test`.
  </acceptance_criteria>
  <done>Versioned-layout and setup-flow expectations are specified before documentation/content changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN setup documentation and examples</name>
  <files>.claude/advisor-mode/init.js, .claude/advisor-mode/README.md, .claude/advisor-mode/policy.example.json, .claude/advisor-mode/verdict.schema.json, .claude/advisor-mode/tests/init.test.js</files>
  <read_first>
    - .claude/advisor-mode/tests/scaffold-layout.test.js
    - .claude/advisor-mode/tests/init.test.js
    - .claude/advisor-mode/init.js
    - .claude/advisor-mode/README.md
    - .claude/advisor-mode/policy.example.json
    - .claude/advisor-mode/verdict.schema.json
    - .planning/phases/01-repo-scoped-advisor-foundation/01-CONTEXT.md
    - .planning/phases/01-repo-scoped-advisor-foundation/01-RESEARCH.md
  </read_first>
  <action>Update scaffold templates so `.claude/advisor-mode/README.md` is created and documents one setup flow: run `node .claude/advisor-mode/init.js` from the repository root, then run `node --test .claude/advisor-mode/tests/*.test.js` for Phase 1 local install correctness. README must list created files and state that provider routing, high-risk trigger enforcement, budgets, and full telemetry are later phases. Refine `policy.example.json` to include explicit baseline audit event names `scaffold.install`, `advisor.verdict.received`, and `executor.followup.recorded`, plus runtime target `.advisor/audit/events.jsonl`. Refine `verdict.schema.json` to require verdict-first fields `status`, `risk`, `confidence`, `blocking_findings`, `recommended_actions`, and `verification_guidance` per D-06. Keep schema/example under `.claude/advisor-mode/` and runtime paths under `.advisor/`.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/scaffold-layout.test.js .claude/advisor-mode/tests/init.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Layout and init tests pass together.
    - README contains the exact command `node .claude/advisor-mode/init.js` and the exact validation command `node --test .claude/advisor-mode/tests/*.test.js`.
    - `policy.example.json` contains `scaffold.install`, `advisor.verdict.received`, `executor.followup.recorded`, and `.advisor/audit/events.jsonl`.
    - `verdict.schema.json` requires all verdict-first fields listed in the action.
    - GREEN commit follows `feat(01-03): document advisor scaffold layout`.
  </acceptance_criteria>
  <done>Maintainer can inspect and validate the local-first scaffold flow from versioned repo assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR README/test duplication</name>
  <files>.claude/advisor-mode/tests/scaffold-layout.test.js, .claude/advisor-mode/tests/init.test.js, .claude/advisor-mode/init.js</files>
  <read_first>
    - .claude/advisor-mode/tests/scaffold-layout.test.js
    - .claude/advisor-mode/tests/init.test.js
    - .claude/advisor-mode/init.js
    - .planning/phases/01-repo-scoped-advisor-foundation/01-PATTERNS.md
  </read_first>
  <action>After GREEN, simplify only duplicated test setup or scaffold template helpers introduced by this plan. Keep command names, filenames, policy event names, and schema required fields unchanged. Run the required code-review then simplify refactor checks from project instructions before committing a refactor if changes are made.</action>
  <verify>
    <automated>node --test .claude/advisor-mode/tests/scaffold-layout.test.js .claude/advisor-mode/tests/init.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Tests remain green.
    - Refactor does not broaden Phase 1 into provider routing, gate blocking, budgets, or telemetry.
    - Optional REFACTOR commit follows `refactor(01-03): simplify scaffold layout tests` if changes are made.
  </acceptance_criteria>
  <done>Setup-flow slice is documented and maintainable without scope creep.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                             |
| ------------------------------------- | --------------------------------------------------------------------------------------- |
| documentation to maintainer execution | README instructions tell maintainers what local command and validation to run.          |
| versioned examples to runtime logs    | Policy/schema examples are committed; generated JSONL/state remains local runtime data. |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                            |
| --------- | ---------------------- | -------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-01-08   | Spoofing               | README setup command             | mitigate    | Document only repo-local `node .claude/advisor-mode/init.js`, no remote bootstrap service.                 |
| T-01-09   | Information Disclosure | audit baseline documentation     | mitigate    | Document `.advisor/audit/events.jsonl` as runtime local path and ignore generated logs.                    |
| T-01-10   | Tampering              | versioned policy/schema examples | mitigate    | Keep deterministic JSON examples under `.claude/advisor-mode/`; tests assert concrete event/schema fields. |
| T-01-SC   | Tampering              | npm/pip/cargo installs           | accept      | No package-manager installs in this plan.                                                                  |

</threat_model>

<verification>
<automated>node --test .claude/advisor-mode/tests/scaffold-layout.test.js .claude/advisor-mode/tests/init.test.js</automated>
<automated>grep -v '^#' .claude/advisor-mode/README.md | grep -F 'node .claude/advisor-mode/init.js'</automated>
<automated>grep -v '^#' .claude/advisor-mode/policy.example.json | grep -F 'scaffold.install'</automated>
</verification>

<success_criteria>

- Setup flow is documented as a single local command.
- Versioned Advisor Mode behavior is inspectable under `.claude/`.
- Runtime audit/state location is explicit and separated from planning docs.
- Phase 1 validation focuses on local install correctness only.
  </success_criteria>

<output>
Create `.planning/phases/01-repo-scoped-advisor-foundation/01-03-SUMMARY.md` when done.
</output>
